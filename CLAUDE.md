# CLAUDE.md

このファイルは Claude Code がこのリポジトリで作業する際に読み込む指示書です。

---

## プロジェクト概要

小説執筆を支援するVSCode拡張機能。テキスト/Markdownで書かれた小説作品を管理し、AI（Ollama / Gemini / Claude）で登場人物・世界観・あらすじの生成、誤字脱字検知、矛盾検知などを行う。

**開発者は小説の作者本人**であり、プログラマではない。技術的な説明は必要だが、専門用語だけで押し切らず、なぜその判断をしたのかを日本語で説明すること。

**言語**：ユーザーとのやり取り、コード内コメント、UI文言はすべて日本語。

---

## 必読ドキュメント

作業前に以下を読むこと。設計判断の根拠がすべて書かれている。

| ファイル | 内容 |
|---|---|
| `docs/設計書.md` | 全体設計。データモデル、機能仕様、外部ツール共存、GitHub同期対策 |
| `docs/プロンプト設計書.md` | AI機能ごとのプロンプト本文（P-01〜P-17） |
| `docs/進捗と引継ぎ.md` | 現在の実装状況、次にやること、既知の課題 |

特に設計書の **3.4節（外部ツールとの共存）** と **3.5節（GitHub同期）** は、実装時に必ず守るべき制約が書かれている。

---

## 開発コマンド

```powershell
npm install          # 依存インストール
npm run build        # ビルド（dist/extension.js を生成）
npm run watch        # 監視ビルド（開発中はこれ）
npm run typecheck    # 型チェックのみ
```

デバッグ起動は VSCode で **F5**。「拡張機能開発ホスト」ウィンドウが立ち上がる。

---

## アーキテクチャ

```
src/
├─ extension.ts          エントリポイント。コマンド登録とステータスバー
├─ models/               データ型の定義のみ。VSCode APIに依存しない
│  ├─ types.ts           作品・ファイル・文字数
│  └─ character.ts       登場人物（型 + 純粋関数）
├─ core/                 ドメインロジック
│  ├─ charCount.ts       文字数計測
│  ├─ episodeParser.ts   ファイル名から話数を解析
│  ├─ metadataParser.ts  投稿サイトのDLファイルのヘッダー解析
│  ├─ textFile.ts        文字コード・改行を保持した安全な読み書き
│  ├─ chunker.ts         本文のチャンク分割
│  ├─ chunkCache.ts      処理済みチャンクのキャッシュ
│  ├─ scanner.ts         作品フォルダの走査
│  ├─ workRegistry.ts    作品の登録管理
│  ├─ characterStore.ts  登場人物の永続化（1人1ファイル）
│  └─ characterMerge.ts  抽出結果のマージ
├─ ai/                   AIプロバイダ抽象化
│  ├─ types.ts           AIProvider インターフェース
│  ├─ ollamaProvider.ts  Ollamaアダプタ
│  └─ registry.ts        プロバイダ選択・セットアップウィザード
├─ prompts/              プロンプト定義（バージョン管理あり）
├─ features/             機能単位のオーケストレーション
└─ views/                VSCode UI（TreeView等）
```

**依存の方向**：`views` / `features` → `core` → `models`。逆流させない。`models` は VSCode API に依存させない（テストしやすくするため）。

---

## 絶対に守る実装ルール

### 1. 作者の原稿を壊さない

これが最優先。以下を必ず守る。

- **AI修正を適用する前に、読み込み時のハッシュと現在のハッシュを照合する。** 不一致なら適用を中止する（`textFile.ts` の `writeTextFilePreservingFormat` の `expectedHash`）。AI応答には数十秒かかり、その間に外部ツールで編集される可能性がある。
- **文字コード・改行コードを保持して書き戻す。** UTF-8/LFに勝手に変換しない。
- **変更箇所のみを置換する。** ファイル全体を再生成しない。
- **Gitの競合マーカー（`<<<<<<<`）を含むファイルはAI処理をブロックする。**

### 2. 作者が書いたデータを上書きしない

- `authorNotes`、`exportNote` は絶対に自動更新しない。
- `autoGenerated: false` のレコードは、登場話数の追記以外行わない。
- `authorLocked: true` の呼称エントリは変更しない。
- 既存の記述と食い違う情報が出たら、上書きせず `conflicts` に記録して作者に判断させる。
- 壊れたJSONを勝手に修復・上書きしない。エラーを表示して止める。

### 3. AIの出力を信用しない

- 文字数制限（サブタイトル15字など）はコード側で再検証する。
- AIが指摘した箇所の文字列が本文に実在するか照合してから適用する。
- **マージ処理はAIではなくコードで行う。** AIには「本文から取り出す」ことだけをさせる。
- 同じ相手への複数の呼び方を、AIは1つにまとめたがる。プロンプトで明示的に禁止し、マージ側でも統合しない。

### 4. 処理量を節約する

- チャンク単位でキャッシュし、変更のないファイルは再処理しない。
- キャッシュのキーには**内容ハッシュ + モデル名 + プロンプトversion**を含める。
- プロンプトを変更したら、そのファイルの `_VERSION` 定数を上げる。

### 5. Ollama固有の注意

- **`num_ctx` を必ず明示する。** 指定しないと既定の短いコンテキストで動き、入力が黙って切り捨てられる。128k対応モデルでも同じ。
- `format` にJSONスキーマを渡すと形式が強制でき、パース失敗がほぼ無くなる。
- `think: false` で思考モードを無効化する（抽出タスクでは不要で遅くなるため）。
- モデル情報は `/api/tags` と `/api/show` から動的に取得する。**モデル名やコンテキスト長をハードコードしない。** 新しいモデルが次々出るため。

---

## 実装スタイル

- TypeScript strict モード。`any` は使わない。
- コメントは「なぜそうしたか」を書く。「何をしているか」はコードを読めば分かる。
- エラーは握りつぶさない。ただしチャンク単位の失敗で全体を止めない（失敗を記録して続行し、最後にまとめて報告する）。
- 長時間処理は `vscode.window.withProgress` でキャンセル可能にする。
- ユーザーへの確認は、処理量とコストを提示してから行う。

---

## テスト

正式なテストフレームワークは未導入。ロジックの検証は Node の型ストリップ機能で行える。

```powershell
node --experimental-strip-types テストファイル.ts
```

ただし**パラメータプロパティ（`constructor(private x: T)`）は非対応**。そのため `models/` にはクラスを置かず、型と純粋関数のみにしている。

テストフレームワークの導入は歓迎するが、その場合はユーザーに理由を説明してから行うこと。

---

## 現在の状況

`docs/進捗と引継ぎ.md` を参照。要約すると：

- フェーズ0（作品管理・文字数計測）：完了、実データで検証済み
- フェーズ1（AI連携・登場人物抽出）：実装完了、**実データでの動作検証はこれから**
- フェーズ2以降：未着手

次にやるべきことは、実際の作品で登場人物抽出を実行し、結果の質を見てプロンプトを調整すること。

---

## ユーザーの環境

- Windows / PowerShell
- Node.js 24.x、Git 2.55
- Ollama（`gemma4:e4b` 8B / コンテキスト131072 / tools・thinking対応）
- テストに使える実データ：19話・約41,000字のカクヨム形式作品
