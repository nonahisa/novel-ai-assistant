# 開発環境の移行手順

チャットでの開発から、VSCode上のClaude Codeへ移行するための手順です。

---

## 1. 事前準備

以下がインストール済みであることを確認してください。すでに完了しているはずです。

```powershell
node -v          # v24.x
git --version    # 2.x
ollama --version # 0.32.x
code --version   # 1.128.x
```

---

## 2. プロジェクトの配置

zipを解凍し、プロジェクトフォルダを任意の場所に置きます。既存の `novel-ai-assistant` フォルダがある場合は、**上書きせず別名で解凍してから中身を比較**することをおすすめします。

現在の想定パス：

```
C:\Users\nonah\Documents\novel-ai-assistant
```

含まれるもの：

```
novel-ai-assistant/
├─ CLAUDE.md              ← Claude Codeが自動で読む指示書
├─ README.md              ← 使い方
├─ docs/
│  ├─ 設計書.md
│  ├─ プロンプト設計書.md
│  └─ 進捗と引継ぎ.md
├─ src/                   ← ソースコード
├─ package.json
└─ 移行手順.md            ← このファイル
```

---

## 3. Gitリポジトリの初期化

設計書でGitHub同期を前提にしているため、この段階でリポジトリにしておきます。

```powershell
cd C:\Users\nonah\Documents\novel-ai-assistant
git init
git add .
git commit -m "初期コミット: フェーズ0完了、フェーズ1実装済み"
```

GitHubへ上げる場合（任意、後からでも可）：

1. GitHubで空のリポジトリを作成（READMEなどは追加しない）
2. 表示されたURLで以下を実行

```powershell
git remote add origin https://github.com/ユーザー名/リポジトリ名.git
git branch -M main
git push -u origin main
```

**注意**：作品の原稿は別リポジトリにしてください。拡張機能のコードと原稿は用途が違うため、混ぜると管理が煩雑になります。

---

## 4. Claude Codeのインストール

PowerShellで実行します。

```powershell
npm install -g @anthropic-ai/claude-code
```

インストール後、確認します。

```powershell
claude --version
```

---

## 5. VSCodeで開いて起動

```powershell
cd C:\Users\nonah\Documents\novel-ai-assistant
code .
```

VSCodeが開いたら、ターミナル（`Ctrl + @`）で以下を実行します。

```powershell
npm install
claude
```

初回はブラウザが開いてAnthropicアカウントでのログインを求められます。認証後、ターミナル上でClaude Codeが起動します。

VSCode拡張として使う場合は、拡張機能マーケットプレイスで「Claude Code」を検索してインストールしてください。サイドバーから使えるようになります。

---

## 6. 最初の指示

Claude Codeは起動時に `CLAUDE.md` を自動で読み込みます。設計の意図や守るべきルールはそこに書いてあるので、いきなり作業を頼めます。

最初の指示としては、以下のような形が適しています。

```
docs/進捗と引継ぎ.md を読んで、現在の状況を教えてください。
```

続けて、次にやるべきこと（登場人物抽出の実データ検証）に進みます。

```
npm run build を実行して、F5で起動できる状態か確認してください。
```

---

## 7. モデルの選択

Claude Codeでモデルを切り替えるには、起動後に以下を入力します。

```
/model
```

Opus 5 を選択してください。設計判断を伴う作業では、より高性能なモデルの方が結果が安定します。

---

## 8. 移行後にまずやること

`docs/進捗と引継ぎ.md` の「3. 次にやること」に書いてありますが、要点は以下です。

**登場人物抽出をまだ一度も実行していません。** 実際のAI呼び出しを通していないため、そこが最大の未検証部分です。

1. `npm run build` してF5で起動
2. 「AIの設定」でOllamaに接続、gemma4:e4b を選択
3. 作品を登録して「登場人物を抽出」を実行
4. 生成された `設定/characters/*.json` を確認
5. 結果を見てプロンプト（`src/prompts/characterExtract.ts`）を調整

プロンプトを変更したら `CHARACTER_EXTRACT_VERSION` を上げてください。キャッシュが自動で無効になり、再処理されます。

---

## 9. 作業の進め方（提案）

Claude Codeはファイルを直接編集できるため、チャットより反復が速くなります。一方で、**大きな変更を一度に任せると意図とずれたときの手戻りが大きい**という性質があります。

以下のような進め方が扱いやすいと思います。

- 1回の指示で1つの機能・1つの修正に絞る
- 変更後は必ず `npm run build` して動作を確認する
- 動いたらこまめに `git commit` する（戻れる地点を作る）
- 設計に関わる判断が必要になったら、実装前に相談する

設計書を更新すべき変更を加えた場合は、`docs/設計書.md` も合わせて更新するようClaude Codeに指示してください。設計と実装が乖離すると、後から経緯が分からなくなります。

---

## 10. 困ったときの確認先

| 症状 | 確認 |
|---|---|
| `npm run build` が失敗する | `npm install` を再実行。esbuildのインストールスクリプトがブロックされている場合がある |
| F5で拡張機能が出てこない | `dist/extension.js` が生成されているか確認 |
| Ollamaに接続できない | Ollamaが起動しているか。`ollama list` が通るか |
| AI処理が途中で止まる | `novelai.ollama.timeoutSeconds` を延ばす |
| 入力が切り捨てられている様子 | `novelai.ollama.numCtx` を明示的に大きくする |
