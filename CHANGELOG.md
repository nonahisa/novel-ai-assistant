# Change Log

## 0.32.12 - 2026-09-05

### セキュリティ修正（設計書6.78）

- **共有リポジトリからの設定の乗っ取りを塞ぎました。** AIの接続先・実行ファイル・作者/編集者モードの8設定を、作品フォルダー内の設定ファイル（.vscode/settings.json）からは変えられないようにしました。編集部と共有するリポジトリに仕込まれても、鍵と原稿の行き先も権限も変わりません
- 信頼していないフォルダーでも拡張機能が動くようになりました（これまでは丸ごと無効）。ただし上記の設定は読みません
- 既定と違うAIの宛先を使っているときだけ、接続の知らせに宛先を表示するようにしました
- **GitHubトークン入りのURLを貼ってもログに残らないよう、伏せ字を拡張しました。** リポジトリURLはhttps/ssh限定にしました（トークン入り・http://は断って案内します）
- VSIXの出口走査の網を全AI鍵形式へ広げました。ログの伏せ字が `task-list-…` のような普通の語を潰していた既存の不具合も直しました

## 0.32.11 - 2026-09-05

### 出力上限と関所の整合（設計書6.77第2段その1）

- **AIへ渡す出力上限の欄を「実際に送る上限」と「場所の確保の見込み」の2つに分け、Ollama以外の5社（LM Studio・Gemini・さくらのAI・ChatGPT・Claude）も渡された上限を尊重するようにしました。** これまでは1つの欄を兼用しており、指定しても無視されていました
- **送る前の関所（入るかどうかの判定）も、実送信と同じ値で数えるようにしました。** 従来は関所と実送信で見ている上限が2倍ずれていました
- **普段の使い勝手はほぼ変わりません。** 実上限が下がるのは独り言だけです。**AIチューニングで実測を記録したモデルでは、その実測値が実際の出力上限になります**
- **応答が出力上限で切れたとき、逸脱検知・作品紹介文・相談でも理由が正しく表示されるようになりました。** 従来は「読み取れません」などとしか出ず、原因が上限切れなのか他の失敗なのか区別できませんでした

## 0.32.10 - 2026-09-05

### 大きさの予算の共通化・第1段

- **AI呼び出しの「大きさ」の計算で、字↔トークン換算に使う係数を1か所（`core/sizeBudget.ts`）へ集約しました。** 同じ意味の0.7が4か所に散らばっていたのを1つの関数にまとめました
- **「モデル比◯%・頭打ち◯字」型の上限（世界観・過去場面）も同じ関数を通すようにしました**
- **挙動は一切変わりません。** 内部の整理のみで、使い勝手の変化はありません

## 0.32.9 - 2026-09-05

### AI呼び出しの全体キュー

- **AI機能を複数起動しても、AIへの送信が全体で1件ずつになるようにしました。** これまでは無制御の同時処理で、Ollamaではモデルの読み込み直しの往復が起き、遅くなったり落ちたりする原因になっていました
- **一括処理（誤字脱字・推敲・逸脱・矛盾・伏線・抽出・各話あらすじ）は「実行の札」を取り、先客が終わるまで丸ごと順番待ちします。** 「（先客の機能名）の完了を待っています…」と表示され、中止ボタンで列から抜けられます
- **相談・表記ゆれの1問・独り言などの単発は札を取らず、長い処理のチャンクの合間に滑り込みます。** 相談が抽出の完了まで待たされることはありません

## 0.32.8 - 2026-09-05

### 0.32.2〜0.32.7のレビューで見つかった問題を直す

- **相談パネルで作品を切り替えると、前の作品の会話を別の作品の承認待ちへ積めてしまう不具合を直しました。** 会話を「積んだ作品」に結び付け、違う作品では止めるようにしました（相談メモの保存も同様です）
- **提供先別エクスポートで、意図しない人物の情報が漏れることがある不具合を3件直しました。** 「第N話まで」の指定でも未登場の人物の紹介文が出ていた、関係・呼称・能力・所属の欄に「作者だけ」の印の人物名や後から登場する人物名が出ていた、という問題です。名指しされた相手も、本人の記録と同じ関門（公開範囲・時点）を通すようにしました
- 相談の内容を設定資料へ反映するとき、AIが自分の提案文を引用しただけで根拠として通ってしまうことがあったため、作者自身の発言にも当たることを必須にしました
- AIの答えが読めなかった回を「反映済み」にしないようにしました（もう一度実行できます）。応答はログに残します
- 独り言（AIのちゃちゃ入れ）で、検査によって見送られた一言もログに残すようにしました（品質を測れるようにするため）
- 表記ゆれ検知で、AIへ問い合わせ中に再チェックすると答えが消えることがある不具合を直しました
- 矛盾検知で、合本（1ファイルに複数話）の作品では過去場面を渡していないのに「渡します」と案内していた不具合を直しました
- 合本を単話へ割る処理で、【本文】ラベルの無い形式のファイルが結果から丸ごと消えていた不具合を直しました

## 0.32.7 - 2026-09-05

### 合本をEPUB・PDFで話ごとに割る

- **1ファイルに複数話が入った原稿（投稿サイトのダウンロードした合本）を、EPUBとPDFで話ごとの章に分けるようにしました。** これまでは丸ごと1章に組まれ、区切り行・【エピソードタイトル】・後書き・リアクションまで本に入っていました
- 切り分けはEPUB書き出し・EPUBエディターのプレビュー・PDFで同じ処理を通すようにしたので、3つで結果が揃います
- **単話ファイルだけの作品は、EPUB・PDFとも今までと1バイトも変わりません。**
- 合本では話ごとに改ページが入るため、紙のページ数が増えます（1話=1章になった分、正しい向きです）
- 挿絵・改ページの指定は、合本では最初の話にだけ効きます。2話目以降へ置きたいときは「合本を単話へ割る」を先に使ってください

## 0.32.6 - 2026-09-05

### 本文を読んで言う独り言

- **独り言（AIのちゃちゃ入れ）に「本文を読んで言う感想の一言」が加わりました。** 直近に保存した話の末尾をAIが読み、「続きが気になります」のような60字以内の感想・応援だけを、他に言うことが無いときに言います
- **無料のAI（Ollama・LM Studioなど）だけで動きます。** 有料のAIを割り当てている場合は黙ります
- AIが繋がらない、応答が読めないなど、失敗はどんな種類でも黙ります（エラー表示は出ません）
- 同じ話については1日1回までです
- 発話の品質（感想としてふさわしいか）は実データではまだ測っていません

## 0.32.5 - 2026-09-05

### 設定資料の提供先別エクスポート

- **詳細メニュー「執筆AI支援→その他支援」に「提供先を選んで設定資料を書き出す」を追加しました。** 編集部向け（全項目）／イラスト・デザイン発注向け（外見の項目だけ）／あらすじ・設定の紹介向け（紹介文だけ）の3種から選べます
- **「第N話までの情報だけ」にも絞れます。** 後の話で判明した値・後から登場した人物を含めないので、ネタバレを防げます
- 書き出したファイルの冒頭に「含めた項目・含めなかった項目・第N話まで」を明記します
- 既存ファイルは上書きせず、別名で作ります。AIは使いません

## 0.32.4 - 2026-09-05

### 矛盾検知へ過去の関連場面を渡す

- **矛盾検知が、調べているチャンクより前の話の本文から、関連する場面の抜粋（語句一致検索・最大6,000字）を探してAIに渡すようになりました。** 「第3話では左腕の傷が第15話で右腕」のような、設定資料に載らない食い違いを拾うためです
- 関連する場面が無い回は、これまでと送る内容が同じです。増える分は実行前の確認と `usage.md` に出ます
- **プロンプトの版が上がったため、矛盾検知のキャッシュはこの版で一度だけ作り直しになります。** 次回の矛盾検知は全チャンクを新しく調べ直します
- 検知の質（見逃し・誤検出）は、実データではまだ測っていません

## 0.32.3 - 2026-09-05

### 表記ゆれの指摘にAIへ訊くボタン

- **表記ゆれの指摘に「AIに訊く」ボタンを付けました。** どちらの表記に揃えるとよいか・その理由をAIが答え、指摘の下に表示します。**本文は書き換えません**——答えるだけです
- **答えは、提示した表記のいずれか、または「揃えない」に限られます。** それ以外の言い換えや新しい表記をAIが返しても採用せず、コード側で照合して弾きます
- 有料AIのときは、押すたびに費用の確認が出ます。断っても何も起きません（エラーにはなりません）
- 答えの質（どれだけ的確に揃える先を選べているか）は、実データではまだ測っていません

## 0.32.2 - 2026-09-05

### 相談から設定資料への書き込み

- **相談パネルに「相談を反映」の役割を持たせました。** 横のパネルと大きい画面の両方に「相談を資料へ反映」ボタンが付き、いまの会話からAIが「作者が決めた人物の設定」を拾い出して承認待ちへ積みます（出どころは「相談から」と分かるようにしています）。**資料を直接書き換えることはありません**——これまでどおり、承認して初めて反映されます
- **拾った内容の根拠が、実際の会話にあるかを照合します。** 会話に無い引用は採用せず、落とした件数は理由別に通知します。作者が確定させた人物（自動生成ではない人物）は書き換え候補にせず、見送ったことを知らせます
- **同じ会話でもう一度押しても、二重に課金しません。** 会話の内容をダイジェスト化して覚えておき、変化が無ければ「反映済みです」と伝えるだけでAIを呼びません。実行中は中止ボタンで止められます
- AIの拾い出しの品質（見逃し・誤検出）は、実データではまだ測っていません

## 0.32.1 - 2026-09-05

### 0.32.0のレビューで見つかった5件の修正

- **投稿サイトの設定でサイトのチェックを外すと、そのサイトの作品情報（作品ID・作品ページURL・ジャンル・メモ）が消えていた不具合を直しました。** 作品情報を台帳直下の独立した場所へ移し、サイトのチェックの有無に左右されないようにしました（旧形式のデータは読み込み時に引き継がれ、値は失われません）
- **保留の表紙があるとき表紙をもう1枚挿せてしまい、中身が同じ面が2枚並ぶ不具合を直しました。**「1冊に1つ」の数えに保留中の面も含めるようにしました
- **ランキングを記録しても、開いたままの執筆統計パネルに反映されなかった不具合を直しました。** 記録するとその場でパネルを作り直します
- **コマンドパレットに残っていた「EPUBを書き出す（試作）」を隠しました。** EPUBエディター内の書き出しボタンに完全に一本化しています
- **保留にした口絵・扉絵の画像が無いとき、本には入らないのに警告が出ていた不具合を直しました。** 保留中の面は警告と人物紹介の案内の対象から外しています

## 0.32.0 - 2026-09-04

### サイトごとの作品情報・ランキング記録／EPUB書き出しの一本化／ブロックの保留

- **投稿キットの設定でサイトごとの作品情報を保存できるようになりました。** 作品ID・作品ページURL・ジャンルの3つ（2段構えで訊かれ、2問目以降は空で飛ばせます）
- **作品の右クリックに「ランキングを記録する」を追加しました。** サイト→種別→順位→メモの順に手入力で書き留めます（自動取得はしません。サイトの規約に配慮しています）。執筆統計パネルに「サイトの記録」の節が出て、作品ページを開くリンク・最新順位・履歴表が見られます
- **「EPUBを書き出す（試作）」を詳細メニューと簡単ステップメニューから外しました。** EPUBエディター内の書き出しボタンに一本化しています（コマンド自体は残っています）
- **EPUBエディターの右側の並びで、面を「保留」にできるようになりました。** 保留した面は薄く表示され、プレビューでは見られますが本には入りません（比較用）。解除は同種の有効な面があると断られます。本文は保留にできません

### 0.31.1〜0.31.2の変更を含む配布版です

## 0.31.2 - 2026-09-04

### EPUBエディターと書き出しのメニュー順を入れ替えた

- 詳細メニューと簡単ステップメニューの両方で、「EPUBエディター（試作）」を「EPUBを書き出す（試作）」の上に（作者の指定。「本を編んでから出す」という作業の順に合わせた）

## 0.31.1 - 2026-09-04

### EPUBエディターの作り替え（段D）——本の並び・右クリック挿入・ブロックごとの独立画面、頭書き・後書きの除去

**作者の指定による作り替えです。**

- **右側が「本の並び」に変わりました。** アイコンの縦列で、ドラッグ＆ドロップで並べ替えられます（Escや枠外で離すと取りやめ）。種類を選ぶパレットは廃止しました
- **挿入は右クリックです。** 並びの行を右クリックすると「この後ろに挿入▶」（置ける種類だけが出ます）、上へ・下へ・削除が出ます（本文の行には削除がありません）
- **選んだブロックごとに、編集画面が丸ごと入れ替わります。**「表紙の編集」のように見出しが変わります。本全体の設定（題名など）は「本の設定」という固定の入口から。画面を切り替えても、打ちかけの値は消えません
- **バックアップ由来のヘッダー・フッターを本文から除きました。** `---- エピソードN開始 ----`・`【エピソードタイトル】`・`【本文】`と、後ろの`【後書き】【リアクション】`等を、EPUB・プレビュー・文字数・段落番号のすべてで取り除きます（原稿そのものは書き換えません）。**後書きの付いた作品は、作品一覧の文字数が本文だけの値まで下がります**（これまでは後書きを含んで数えていました）

### 0.30.1〜0.30.11をまとめた配布版

作者の指示によるVSIX作成です。主な機能を一行ずつ並べます。

- EPUBのブロック式エディター完成（口絵・扉絵・あとがき・縦中横・表紙1:1.4）
- 章立て一式（AI提案P-31・あらすじとEPUB目次への反映）
- 話の挿入・削除の総点検修正
- 投稿キット（規約遵守の「貼るだけ」方式）
- SNS記事のnote風エディタ
- 作品タイプ「創作メモ集」「脚本」とタイプ別表示
- 作品メモと移管
- プロットモードの画面
- plot.mdから設定資料への差分反映
- 単話プロットのAI判定（P-27・P-28。プロンプト全番号実装済みに）

## 0.30.11 - 2026-09-04

### 単話プロットのAI判定2種を実装（設計書6.36.3、P-27・P-28）

**長く予約番号のままだった最後のプロンプトです。**

- **「この話の設計を検査する」（P-27）**：本文を書く前に、単話プロットの視点・目標・展開の箇条書きだけを見て、「目標に向かっていない」「停滞・重複」「目標と矛盾」を指摘します。修正案は出しません（指摘まで）
- **「本文が設計どおりか確かめる」（P-28）**：本文を書いた後に、本文と箇条書きを照らして「無いのに起きた」「あるのに起きていない」「順序の食い違い」を指摘します。引用が本文に逐語で実在するかはコードで照合します
- 入口はプロットモードの話の行（本文の無い話では「本文と照合」を出しません）と、単話プロットの右クリックです。結果は提案パネルに出ます（分類は2つに分けました）
- **実データではまだ測っていません。** 見逃しと誤検出の両方を測るまで完成とは言いません

## 0.30.10 - 2026-09-04

### plot.md から設定資料への差分反映を実装（設計書6.4.9）

**「残る大物」の1つ。作者の依頼による実装です。**

- `plot.md` の「主要登場人物」欄を保存すると、書かれた人物を機械で読み（AIは呼びません）、承認待ちに積むようにしました。出どころが「プロットから」と分かる印がつきます。台帳へ入るのは、これまでどおり「更新分を反映」で承認したときだけです（本文からの抽出と同じ道）
- 既存の人物なら紹介文の更新案、資料に無い名前なら新規の人物案になります。新規は承認時に番号を振り、退避つきで作成します（status は「未登場」）
- 同じ内容は二度積みません。並び順を入れ替えただけでも積み直しません。読めない行・確定済み（autoGenerated:false）の人物・同名複数は、件数と理由を通知で報せます
- プロットモードのパネルに手動ボタン「プロットの人物を資料へ反映」を追加しました

## 0.30.9 - 2026-09-04

### プロットモードの画面を実装（設計書6.4.8）

**長く「残る大物」の筆頭だった機能です。作者の依頼「プロットモードを実装してください。単話プロットにも対応してください」による実装です。**

- 作品を右クリックして「プロットモードを開く」を選ぶと、左に `plot.md` が普通のエディタとして、右に作業パネルが並んで開きます。パネルは `plot.md` の中身を欄に写しません（書き込みは既存の道を通ります）
- パネルには3つの機能があります。**節の目次**（クリックでその行へ移動。未使用の見出し候補は押すと末尾に追加され、書きかけの文章は1文字も変わりません。Ctrl+Zで戻せます）、**AIの入口**（プロット逆算・対話・形式とジャンルの3つのボタン）、**話の一覧**（単話プロットの有無・本文の有無と文字数・章名・各話あらすじの冒頭が並ぶ構成の見取り図。単話プロットの無い話はその場で作れます）
- `plot.md` を保存するとパネルの目次と話の一覧が自動で追いつきます
- プロットモードは小説・脚本のタイプの作品にだけ出ます（詳細メニューには常に出ます）
- 単話プロットのAI判定（P-27/P-28）は次の段で作ります

## 0.30.8 - 2026-09-04

### 作品ごとのメモと、創作メモ集からの移管を実装（設計書6.71）

**作者の依頼。**

- 作品一覧に「メモ（N件）」の枝を追加しました（`設定/メモ/` に1メモ1ファイル。メモの無い作品には出ません）。右クリックで「メモを追加」「メモを削除（ごみ箱へ）」ができます
- 創作メモ集のメモを右クリックすると「このメモを作品へ移管」が出ます。移す先を選ぶとファイルが移動します（同名があれば連番）。内容そのものには触れません。台帳に残っている投稿・章・あらすじの記録は消さず、件数を報せます
- メモは原稿として扱いません。話数・文字数・あらすじ・投稿・校正の対象になりません
- 移管は、タイプが「小説」「脚本」など未設定の作品には出しません（原稿を誤ってメモへ移す事故を防ぐための、唯一の絞り込み例外です）

## 0.30.7 - 2026-09-04

### 投稿キット・note風エディタ・作品タイプ「創作メモ集」「脚本」を実装（設計書6.68〜6.70）

**作者の依頼（2026-09-04）による3つの大きな機能。実装はimplementer3体の並行作業です。**

- **投稿キット**（6.68）：なろう・カクヨム・アルファポリス・noteへ、新話の「貼るだけ」更新ができるようにしました。**サイトへの自動投稿はしません**（規約遵守。機械がやるのは変換・コピー・投稿ページを開く・記録だけです）。投稿状態の台帳（`設定/投稿状態.json`）を持ち、作品一覧に「未投稿N」が出ます。「新話を投稿する」「この話を投稿する」「投稿サイトの設定」を追加しました。noteはルビを括弧書きへ変換し、傍点はサイトごとに出し分けます
- **SNS記事のnote風エディタ**（6.69）：SNS記事のMarkdownを横書きで開くと、noteの読み味（中央620px・行間広め）で書けます。「貼り付け後」プレビュー面は投稿キットと同じ変換規則で、noteに貼ったときの見た目を確かめられます。noteに無い記法（斜体・表・傍点）には印を出します
- **作品タイプ「創作メモ集」「脚本」**（6.70）：作品にタイプを持たせ、タイプごとに表示を絞り込めるようにしました。**創作メモ集**は単位が「メモ」で、始め方を訊かずに無題.mdから書き始められます。**脚本**は柱・ト書き・セリフの雛形と縦書きが既定です。簡単ステップメニューと右クリックはタイプで絞り込み、詳細メニューには全機能を残しています

## 0.30.6 - 2026-09-04

### 話の挿入・削除と章立ての総点検で見つかった12件を修正（設計書6.67.5）

**出荷直後にOpusレビューへかけ、実機確認の前に塞ぎました。0.29.26〜0.30.5で入れた挿入・削除を使う前に、この版まで上げてください。**

- **数字の台帳の追従を「実際に動いた話の対応表」でだけ行うようにしました。** 途中で失敗しても台帳だけが先へ進む事故を根絶しています
- **削除された話から始まる章は、開始を次の話へ移すようにしました。** 従来は保存が黙って失敗し、次の章が別の話を静かに指し続けていました
- 合本は、名前に範囲が無いものを動かさないようにしました
- 挿入・削除の対象は同じフォルダーの話だけにしました（番外編を巻き込みません）
- 未保存のエディタがあれば中止するようにしました
- 挿入で作る新しい話のファイル名は、隣の話の書き方（桁・拡張子・様式）に合わせるようにしました
- 独立コミットはパス指定にして、他のステージ済み変更を巻き込まないようにしました。未追跡の対（新規話とその隣）は独立コミットの対象から外しました
- ほか、レビューで見つかった細部を修正しています

## 0.30.5 - 2026-09-04

### 章立ての第3段——あらすじ文書とEPUB目次への反映（設計書6.66.4）

**作者の依頼「章の名称の提案、章分けの提案…あらすじまとめへの反映」の後半です。これで章立ての依頼一式（管理・折りたたみ・AI提案・あらすじ反映）が完結しました。**

- 各話あらすじの文書（`設定/synopsis.md`）に、章立ての台帳から**章見出し**（`### 章名（第2話〜第3話・2話）`）が挟まるようにしました。章があるときだけ話の見出しが1段深くなります。**台帳の無い作品は1バイトも変わりません**
- EPUBの「章ごとに区切る」目次が、**台帳があれば台帳の章名**で束なるようにしました（プレビューも書き出しも）。台帳が空なら従来どおりです。エディターで章を足すと、開き直さずに目次が変わります
- 台帳が読めないときは、EPUBは通知に1行、あらすじはログだけに留め、**生成文書を警告で汚さない**ようにしました

## 0.30.4 - 2026-09-04

### 章立てのAI提案（設計書6.66の第2段、新プロンプトP-31）

**作者の依頼「章の名称の提案、章分けの提案…インターフェースを拡張してください」の前半**です（後半のあらすじまとめへの反映は次の版）。

- 作品の右クリック・詳細メニュー（執筆AI支援→その他支援）に**「章立てをAIに提案させる」**を追加しました。話のサブタイトルと各話あらすじ（本文は送りません）から、章分けと章名を同時に提案します
- 提案は**提案パネルで1章ずつ（まとめても）承認**して、初めて `設定/章立て.json` に入ります。承認前は何も変わりません
- 章ノードの右クリックに**「この章の名前の案を出す」**を追加しました。名前だけ最大3案を出し、選んだ1つを改名として台帳へ入れます
- 実在する話数か・昇順か・重複が無いか・名前が空でないかはコードで検証します（AIの数え・分類は信用しません）
- 「別の案を見たい」操作のため、この機能はキャッシュを使いません

## 0.30.3 - 2026-09-04

### 推敲の「語尾単調」指摘を、AIの数えではなくコードで裏取りする

**作者の報告「『〜た』が5回とありますが、並んでいないように見えます」——実際は5連続ではなく、AIの数え間違いが素通りしていました（設計書6.9.1）。**

- チャンク単位で「同じ語尾（文末1文字＋句点）の文が4連続」をコードで数え直し、無ければ指摘ごと捨てるようにしました。捨てた件数は完了報告とログに「語尾の数え違い ◯件を除外」と出ます
- 「た。」と「だ。」は別の語尾として数えます。台詞（「」『』）は数えず、地の文の連続も台詞をまたいでは切りません
- プロンプトは変更していません。真因はモデルが数を数えられないことで、コード側の裏取りが本筋です
- 副産物として、既存テストの例文自体が4連続になっていなかった不備を見つけて直しました

## 0.30.2 - 2026-09-04

### EPUBのブロック化・段C（組み替えUI）完成、「！？」後のアキ誤指摘を修正

**設計書6.65.15の段Cで、段B（0.30.1）から続けていたEPUBのブロック化が完成しました。**

- EPUBエディター画面を作り替えました。**右側に狭い縦のパレット**（表紙・中表紙・口絵・扉絵・目次・人物紹介・章区切り・本文・あとがき・奥付・裏表紙の11種、アイコン風）、**左側が作業スペース**（本の設定→本の並び→選んだ面の設定＋プレビュー）です。パレットのボタンを押すと面が並びへ挿入され、各面の「上へ・下へ・削除」で組み替えられます（本文は削除できず1つ固定。1冊に1つまでの種類は、すでに置いてあると押せません）
- **目次・人物紹介の「入れる」チェック欄を廃止しました。** 並びに面が置いてあるかどうかが本の内容そのものになります（チェック欄と並びの二重管理をやめました）
- **章区切りだけは章立ての台帳（設計書6.66）が正**です。パレットの「章区切り」を押すと、話と章名を訊いてから台帳へ書きます（本の並びは増えません）
- **縦書きの本文で「！》」「！」」など、閉じ括弧・句読点・三点リーダー・ダッシュが続くときの「1マス空けて」の誤指摘を直しました。** 空けなくてよい後続文字の一覧に「」』）以外の閉じ括弧（《》〉】〕｝］”’）・句読点・三点リーダー・ダッシュが漏れていました

## 0.30.1 - 2026-09-04

### EPUBのブロック化（段B）——本を「ブロックの列」で持てるようにした

**設計書6.65.15。作者依頼のブロック式画面の土台**です（組み替えUI＝段Cは次）。

- `book.json` に `blocks`（順序つきの面の列：表紙・中表紙・口絵・扉絵・目次・人物紹介・本文・あとがき・奥付・裏表紙）を持てるようになりました。並びがそのまま本の並びになります。既存の `book.json` は読み込み時に既定の並びを組んで動きます（ファイルは書き換えません）
- **口絵・扉絵**（画像の面。解説文を添えられます）と**あとがき**（`設定/書籍/あとがき.md` に書きます。本文と同じ組版でルビ・縦中横も効きます。無い・空なら面ごと出ません）を本へ入れられるようになりました
- エディター画面に「あとがきを書く」ボタンを追加しました（無ければ雛形を作って開きます）。プレビューはブロックの並びに従います
- 読めない画像・あとがきはその面だけ飛ばして本は出します（通知に出ます）

## 0.30.0 - 2026-09-04

### 未チューニングの安全既定＋VSIX配布版

**0.29.13〜0.29.26の機能をまとめた配布版**です（EPUBエディター一式・章立て・話の挿入と削除・AIチューニングの書ける量測定と接続・相談画面の切り替えボタン）。

- **初期状態での安全既定**を入れました。チューニング台帳に読める量の実測が無いモデルは、自動モードのチャンク上限を6,000字に抑えます（実測があるモデルは従来どおり最大20,000字のままです）。応答の見込み（num_ctxの確保）も実測を使い、実測が無ければ設定値と8,192字の小さいほうを使います。**非力なマシンのローカルLLMでも初回から動き、チューニングするとチャンクが約3倍、呼び出し回数が約1/3になって速くなったと体感できます**
- **ローカルAI（Ollama・LM Studio）の待ち時間の既定を180秒→300秒**にしました（CPUだけの環境でも1回の呼び出しを切らない長さです。クラウドAIは180秒のままです）
- 書ける量による絞り込みを、チャンクを使う全機能（誤字脱字・抽出・推敲・矛盾・伏線の検知と回収確認・更新告知）へ配線しました

## 0.29.26 - 2026-09-04

### 話の挿入・削除、まとめ送信の上限接続、EPUBの3つの直し

- **話の挿入と削除**（設計書6.67）：作品一覧の話を右クリックすると「この話の前に挿入」「この話を削除（話数を詰める）」が使えます。後ろの話数は自動で付け替わり（挿入は降順・削除は先にごみ箱へ移してから昇順）、話数を指している台帳（章立て・EPUBの挿絵・登場人物・伏線・各話あらすじ・年表・保留中の更新案など）も同じ操作の中で追従し、完了通知に件数を出します。Git管理下では「名前の変更だけの独立したコミット」を確認してから作成します（内容が100%一致するのでGitHubでも改名として履歴が繋がります）。削除した本文はOSのごみ箱へ移すだけで消しません
- **書ける量をまとめ送信の上限へ接続**しました（設計書6.65.14）。AIチューニングで測った「書ける量」（例：gemma4:12bは6,500トークン）を台帳に保存し、まとめ送信の上限を「書ける量×0.8÷応答率0.3」で絞り込みます（6,500トークンなら17,333字）。完了通知に「まとめ送信の上限を◯字にしました」と出ます。測定のときだけ出力の長さ指定（num_predict）を送り、無駄に長い試行を短縮します
- **EPUBの3つの直し**（設計書6.65.15の段A）：目次に出ていた「第1話 第1話…」の重複を修正しました（ゼロ埋めや全角の話数が重複除去をすり抜けていたのが原因で、PDF出力・あらすじ見出しにも同じ直しが効きます）。目次の見出しの形（番号＋題・題だけ・番号だけ）を選べるようにしました。縦書きで半角の数字・!・?が1〜3文字連続するとき縦中横で組むようにしました。表紙の枠を横1:縦1.4に固定し、比率の違う元絵には余白（色は白・黒・任意から選択）を付けるようにしました

## 0.29.25 - 2026-09-03

### 章立て（第1段）と、AIチューニングが終わらなくなる穴の修正

- **章立て（第1段）**：話を「章」でまとめられるようになりました。作品一覧が「作品→章（折りたたみ可）→話」の3階層になります。話を右クリックして「ここから章を始める」、章を右クリックして「章の名前を変える」「章を外す」が使えます。台帳は `設定/章立て.json` に持ち、章は「名前」と「どの話から始まるか」だけを覚えます（次の章が始まるまでがその章。話を足すと自然にその場所の章へ入ります）。台帳が壊れていても話は表示され、⚠の一言つきで章なしの並びになります。AIによる章分け・章名の提案とあらすじへの反映は次の段です
- **AIチューニングが終わらなくなる穴を塞ぎました。** ストリーミング受信（開発ビルドの実験）を「入」にしていると、断片が届くたびに待ち時間を数え直してしまい、繰り返しに崩れたモデルを永遠に待ち続けることがありました。測定と接続確認は、配布と同じ受け取り方（設定の待ち時間で打ち切られる道）を必ず通るようにしました
- **話の挿入・削除の土台**を実装しました（話数の付け替え計画を組み立てる部分のみ）。画面と接続は次の版です

## 0.29.24 - 2026-09-03

### AI相談画面の行き来をボタンにし、再起動でビューが消える穴を塞いだ

- 横の細い相談パネルの入力欄の下・左端に**「メインに表示」**ボタンを追加。押すと本文の領域に大きく開き、横のパネルは残ります。大きい画面の同じ位置には**「サブに戻す」**を追加。押すと横のパネルへ戻り、大きい画面のタブは閉じます。送信中はどちらも押せません。あわせて詳細メニューの「AIに相談する（大きく開く）」を非表示にしました（簡単ステップメニューには残ります。0.29.23の「横のパネルへ移動」削除の続きです）
- **再起動で「AIに相談」ビューが消える穴を塞ぎました。** 「他のメニューを閉じる」等の印はVS Code側が持ち続けるのに、拡張機能ホストだけ再起動（クラッシュ復帰・Restart Extension Host）すると起動時に読み込んでおらず、相談ビューが隠れたまま立ち上がっていました。起動時に素の表示（全ビューあり）へ戻すようにしました（前面に出したりフォーカスを奪ったりはしません）
- マニュアルの「AIに相談する（大きく開く）」の案内を、「メインに表示」ボタンからの案内に書き換えました
- 簡単ステップメニュー「7. 電子出版等」から「EPUB出力（予定）」の準備中の行を外し、PDF出力・EPUBを書き出す（試作）・EPUBエディター（試作）の3つを並べました（6.65の実装完了を受けて）

## 0.29.23 - 2026-09-03

### 「横のパネルへ移動」の項目を消した

- 詳細メニューの「横のパネルへ移動」と、大きく開いた「AIに相談」の「…」メニューの同項目を削除（作者の指定「使い道がありません」。0.29.8で足したものの取り消し）
- 横の細い相談パネル自体は残る。入口は**本文の右クリック「AIに相談する」だけ**（範囲を選んで聞く使い方はそちらで足りる）

## 0.29.22 - 2026-09-03

### EPUB領域の総点検で見つかった14件を修正した

作者の指示でEPUB関連（第1〜3段）の総点検を行い、見つかった14件を直した段です（設計書6.65.13）。

- **確実な不具合4件**：目次の「章ごとに区切る」で束ね名の無い話が空の章見出しに包まれていたのを直しました。プレビューの目次にだけ「登場人物」が抜けていたのを直しました。詳細メニューの説明文が2版ぶん古かったのを直しました。裏表紙のエラーメッセージが表紙の欄を案内していたのを直しました
- **「見た目どおり」の食い違い**：焼いた表紙があるのにプレビューが元イラストの合成を見せていたのを直し、焼いた画像の表示と**「焼いた画像を消す」ボタン**を追加しました。改ページ直前の空行が前ページの末尾に残っていたのを、出さないようにしました。競合で外れた話の挿絵・改ページが黙って消えていたのを、通知と注記で分かるようにしました。同じ人物イラストが2回入る不具合を直しました。名前が空の人物で空要素が出る不具合ほかを直しました
- **実データで危ないもの**：XMLに不正な制御文字が1文字混ざると本ごと開けなくなる不具合を直しました（除去するようにしました）。**webpを断るようにしました**（png・jpeg・gifは受けます。EPUB 3.0では正式な形式でなく、古いリーダーが表示しない恐れがあるためです）

## 0.29.21 - 2026-09-03

### EPUBエディターに登場人物一覧と書体組み込みを実装した

作者の指示でEPUBエディターの登場人物一覧・書体組み込みに着手した段です（設計書6.65.11・6.65.12）。**これでEPUBエディターの計画分（第1〜3段）がすべて揃いました。**

- EPUBエディターに**登場人物一覧の面**（目次の後・本文の前）を追加しました。**既定では出しません**（設定資料が不意に本へ入るのはネタバレ事故のもとです）。出すときは登場済み・モブ以外・ネタバレ区分が公開（public）の人物だけに絞り、名前（読み仮名をルビで）と紹介文だけを載せます。人物イラストは台帳の `icon`（型だけあって未実装だった欄）を初めて使います
- **書体の組み込み**に対応しました。本文用・見出し用の2枠（.ttf／.otf）を指定でき、ライセンスの注意書きを欄に常時表示します。フォントを読まないリーダーでも本文が消えないよう、serifへのフォールバックを敷いています
- 台帳・書体・イラストが読めなくても本は出します（読めなかったものは通知に出します）
- エディターのプレビューにも一覧の面と書体を反映しました

## 0.29.20 - 2026-09-03

### EPUBエディターに挿絵とページ分割を実装した

作者の指示でEPUBエディターの「挿絵とページ分割」の欄を作った段です（設計書6.65.10）。

- EPUBエディターに**「挿絵とページ分割」**の欄を追加しました。話を選ぶと段落の一覧（冒頭20字）が出て、段落ごとに「ここに挿絵」（画像の場所と解説文）「ここで改ページ」を指定できます。位置は「第N話の第M段落のあと」で `book.json` に持ち、**原稿には目印を書き込みません**
- 挿絵は本文の流れの中に入り、解説文は画像の直後に添えます（重ね書きはリーダーごとに崩れるため）。ZIP内の画像名は機械名に付け替え、同じ画像は1回だけ入れます
- 改ページは次の段落から新しいページになります（話の末尾を指定した場合は入りません）
- **原稿が変わって位置がずれたら警告を出して末尾に置きます**（本は出します。黙って捨てません）。**指し先の話が見つからない指定も通知します**（改題・移動が典型で、パスを文言に含めます）。警告は書き出しの完了通知とパネルの欄の両方に出ます
- 読めない挿絵画像はその挿絵だけ飛ばして本を出します（通知には出します）

## 0.29.19 - 2026-09-03

### EPUBエディターの第3段前半（表紙・裏表紙の合成）を実装した

作者の指示でEPUBエディターの第3段、表紙・裏表紙の合成に着手した段です（設計書6.65.8・6.65.9）。

- EPUBエディターに**表紙・裏表紙の合成**を追加しました。元イラストの上に題名・作者名・イラストレーター名・レーベル名を重ねます。置き場所は9か所のプリセット、大きさ3段階、色（白・黒・任意の16進）、縦書き・横書きから選べ、要素ごとに出す・出さないも選べます
- **「表紙を焼く」**で合成結果を `設定/書籍/表紙_合成済み.png`（裏表紙は `裏表紙_合成済み.png`）へ保存します。EPUBの書き出しはこのファイルを使うので、パネルを開かないメニューからの書き出しでも合成済みの表紙が入ります
- 裏表紙（本の最終面、奥付の後ろ）にも対応しました。拾い順は表紙と同じ「焼いた画像→元イラスト→無ければ面ごと出さない」です
- 書き出しの完了通知に、焼いた画像なら「◯月◯日◯時に焼いたもの」、元イラストのままなら「焼く道があること」を表紙・裏表紙とも出すようにしました。古い表紙や焼き忘れが黙って入るのを防ぎます
- WebViewで作者の画像を表示する仕組み（asWebviewUri）をこの作品で初めて実装しました
- プレビューと「焼く」は同じcanvas・同じ描画関数を使います（描画器を2つ持ちません）

## 0.29.18 - 2026-09-03

### EPUBエディターの第2段（エディター画面）を実装した

作者の指示でEPUBエディターの土台（第1段）に続き、編集画面を作った段です（設計書6.65.6・6.65.7）。

- 詳細メニューに**「EPUBエディター（試作）」**を追加しました（「EPUBを書き出す（試作）」の隣）。左に設定の欄（書誌情報・縦書き横書き・空行の詰め・目次のありなしと配置パターン3種・目次と奥付の飾り3種）、右にプレビュー5面（表紙・タイトルページ・目次・本文の冒頭・奥付）を並べています
- プレビューは**書き出しと同じ組版CSS**で組みます（プレビュー専用の組版は持ちません）。左の欄を変えると、右のプレビューが追従します
- タイトルページ（扉）を本の1面として新設しました。表紙の直後・目次の前に入ります
- 「保存」は `設定/書籍/book.json` へ書き込みます。**外部で変更されていたら上書きせず止まります**（ハッシュ照合。`src/core/bookStore.ts`）
- 「EPUBを書き出す」ボタンは、メニューからの書き出しと同じ処理を呼びます
- 章ごとの目次は、プロローグ・幕間・エピローグ・日付（「2026年8月」など）で束ねます

## 0.29.17 - 2026-09-03

### EPUB書き出しの第1段（土台）を実装した

作者の指示でEPUBエディターを新設することになり、その最初の段です（設計書6.65）。

- 詳細メニューのPDF出力の隣に**「EPUBを書き出す（試作）」**を追加しました。作品の全話を縦書き（横書きも選べます）のEPUB3に綴じ、`.aiwriter/exports/` へ上書きせず保存します
- ルビ・傍点・章立て・話ごとの改ページ・空行の詰め（続いた空行を1つ減らす）・シンプルな目次と奥付・表紙（`設定/書籍/book.json` の `coverImagePath` があれば画像、無ければ文字だけの扉）に対応しました
- 本の設計図は `設定/書籍/book.json` です。無ければ既定値で書き出せます。壊れていれば修復せずエラーで止めます
- 原稿には一切書き込みません（空行の詰めは書き出し時だけの変換です）
- 依存に fflate（純JS）を追加しました。ブラウザ版ビルドでも動きます
- エディター画面・表紙合成・挿絵・登場人物一覧・書体組み込みは第2段・第3段（未着手）です

## 0.29.16 - 2026-09-03（開発ビルド限定）

### ストリーミング実験の入切を、メニューから押せるようにした

作者の指示「３はテスト中に配置してください」への対応です（設計書6.63.1）。

- 開発ビルド限定のコマンド「Ollamaのストリーミング受信を切り替える（実験）」を新設しました。**「テスト中」のF-53の行**と「拡張機能の設定→AI」から押せます。押すと入／切が通知で分かります
- launch.json の環境変数（`NOVELAI_OLLAMA_STREAM=1`）はもう必須ではありません（残してあります。起動時から流したい人向けです）
- **切り替えは保存しません。** ウィンドウを閉じると切に戻ります。実験の旗を設定へ永続させると、実験したことを忘れた頃に本番と違う挙動で悩むためです
- この機能一式は配布物（VSIX）には入りません。本番の「テスト中」では、この節が**総数から黙って消えないよう**、辿れない節の判定を広げてツールチップに名前つきで出るようにしました

## 0.29.15 - 2026-09-02

### AIチューニングに、手元のAIに限って「書ける量」の測定を追加した

作者の提案「チューニングに出力文字数上限の検査も入れましょうか」への回答です。

- **機能追加**：AIチューニングで「読める長さ」の測定が終わったあと、**Ollama・LM Studio に限って**続けて「書ける量」を測るようにしました。「0001から順に4桁の数字をN行」とAIに頼み、実際に返ってきた行を数える方式です（先に小さく当ててから上限へ跳び、届かなければ間を詰める——読める長さの測定と同じやり方です）
- 結果は同じ通知の末尾に「1回の応答で書けたのは 約 N トークン（M 行）です」と出ます。**設定には反映しません**——「書ける量は今回の参考値で、設定には入れません」と通知にも明記しました
- 時間切れになった回は「その量は書けなかった」として数え、測定を続けます。書ける量の測定が失敗しても、先に測った読める長さの結果は失われません
- さくらのAI・ChatGPT・Gemini・Claudeなどのクラウド勢では走りません。申告値がAPIから取れるうえ、出力トークンは単価が高いためです

## 0.29.14 - 2026-09-02

### チャンクの分割・統合の点検で見つかった不具合を5件直した

作者の指示「チャンク統合、分割あたりに不具合がないかチェックしてください」を受けた総点検です。まとめ送信を6,000字→20,000字に広げた直後（0.29.11）の領域を洗い直しました。

- **不具合の修正**：メモ行だけの話・0バイトの話・合本の中の空の話があると、チャンクの行番号の対応がずれ、**隣の話の指摘が黙って捨てられる**ことがありました。空の本文はチャンクにせず、まとめの束にも入れないようにしました
- **不具合の修正**：誤字脱字検知の進捗表示が、キャッシュ命中の多い作品で「11/2」のように分子が分母を超えることがありました。分母を全チャンク数に直しました
- **不具合の修正**：複数話をまとめて送る「合本」だけ、手動で指定したまとめ量が無視されていました
- **不具合の修正（挙動が変わります）**：モデル情報が取得できないとき、推敲・矛盾検知・伏線検知・更新告知の4機能が、チャンクを既定の8,192トークンへ黙って縮めて処理しており、キャッシュが効かなくなるほど呼び出しが細かく増えていました。誤字脱字と同じ「理由を出して止まる」に揃えました。**一覧に無いモデル名を手で設定していると、これらの機能が新たに止まるようになります**
- **不具合の修正**：チャンクが上限に入らなかったときの分け直しが、経路によって基準の字数や戻す位置が食い違っていました。基準をひとつに揃え、キャッシュに答えがあるチャンクは分け直さないようにしました

## 0.29.13 - 2026-09-02

### 待ち時間が300秒で切られていた本当の原因を直した

- **不具合の修正**：設定で待ち時間を900秒、台帳で600秒にしていても、実際には**302秒**で「応答が返ってきませんでした」というエラーになることがありました。原因は、こちらの待ち時間の指定ではなく、**Node内蔵の通信部品（undici）が応答ヘッダーを待つ上限を既定300秒で持っていた**ことでした。まとめて1回で返す形は、AIの応答ヘッダーが生成の完了後に届くため、生成が300秒を超えると、こちらの待ち時間の指定が出番を迎える前にundiciのほうが先に諦めてしまいます。`fetch` へこちらの待ち時間をそのままundiciへ渡すようにし、この壁を無くしました
- 0.29.1で直したのは「エラーの分類」だけで、302秒で切れる原因そのものは残っていました。0.29.10でまとめて送る量を増やした瞬間（13,501字・71秒だった処理が25,915字・300秒超に伸びた）、前からあったこの壁に届くようになっていました

### 確認する側が対象を壊していた不具合を2件直した

- **不具合の修正**：AI接続の疎通確認が `numCtx: 1024` を決め打ちで送っており、これでは初期化できないモデル（gemma4:26bなど）があり、確認そのものが失敗していました。決め打ちをやめました
- **不具合の修正**：測れる上限を調べる処理が、申告値をそのまま信じてよい相手（LM Studioなど）でも256Kまで律儀に試しており、1回分の測定が無駄になっていました。当て推量に頼るしかない相手（さくらのAI・ChatGPT）だけ256Kまで試すようにしました
- モデルの読み込みに失敗したとき、もう一方の手元AI（Ollama⇔LM Studio）が動いていれば「使わないほうを終了すると載ることがあります」と添えるようにしました。作者の報告「OllamaとLM Studioを同時起動していたようです」を受けたものです。両方を同時に使う状況は、この拡張機能自身の機能別AI割当が作り出しています

### 推敲の修正案が原文の後ろを落とす件の続きと、マニュアルの手直し

- 0.29.12で直した「修正案が原文の一部しか覆っていない」不具合について、マニュアルも実際に合わせました。整理で表に出さなくなった操作について、マニュアルに「実際の入口」（簡単ステップメニューのどこから／設定管理のどの説明のリンクから）を添えるようにしました。手書きではなく、簡単ステップメニューの参照一覧から導く形にしています

### 出力上限の測定の中核を作った（AIチューニングへの組み込みはまだ）

- AIへ「0001から順にN行書いてください」と頼み、返ってきた行を先頭から数えることで、実際に出せる出力量を測る仕組みの中核を作りました。トークン数は応答の実数（`eval_count`）を使います。AIチューニング機能への組み込みは次の作業です

### F5専用の実験：Ollamaの応答を流しながら受け取る

- **開発ビルド限定の実験です。** 配布物には入りません（`__DEV_HELPERS__` で囲み、環境変数 `NOVELAI_OLLAMA_STREAM=1` を立てたときだけ動きます）。Ollamaの応答を最初から最後まで待つのではなく、流れてくる端から受け取れるようにしました。日本語の多バイト文字が受信の境目で切れないよう対策しています
- 続けて、思考（考え中の内容）を大きな相談パネルの「考えています…」に流せるようにしました。流れるのは思考の末尾120字までで、答えの本文は混ぜません

## 0.29.12 - 2026-09-01

### AIチューニングを公称値から始めるようにし、推敲の修正案が本文を壊す不具合を直した

- AIチューニング（測って設定を合わせる）の測り方を、倍々（4,000→8,000→16,000→32,000→64,000→128,000→公称値）から、**1回目だけ小さく試してから公称値へ跳ぶ**方式に変えました。申告どおり読めるモデルでは、送信回数が7回から2回に減ります。1回目を小さいままにしたのは、失敗したときに「長すぎた」のか「鍵が違う・繋がらない」のかを見分ける拠り所を残すためです。いちばん大きい公称値の1回が先に来る引き換えはありますが、手元のOllamaは無料なので損はなく、有料AIでも実行前の見積もり額はほとんど変わりません
- **不具合の修正**：推敲の修正案が、原文の一部しか覆っていないことがありました。「悶絶しながらしながら沼ワニに近づくと、槍は中ほどからぽっきり折れていた。」という原文に対し、指摘（「冗長：『しながら』が重複しています」）は正しいのに、修正案が「悶絶しながら」だけ返ってくる例があり、そのまま適用すると一文が6文字になってしまいました。原文の後ろを大きく落としている修正案を見分けて、そのときは修正案だけを空にするようにしました。指摘そのものは残るので、直し方は作者が決められます

## 0.29.11 - 2026-09-01

### Ollamaの「コンテキスト長」の指定を、関所・分割・送信で揃えた

- **不具合の修正**：`novelai.ollama.numCtx` に値を入れていると、**本文が黙って切り詰められることがありました**。送る前の点検はモデルの上限（たとえば262,144）と比べて「入る」と判断するのに、Ollamaへは指定した値（たとえば8,192）で送っていたためです。指定した値をそのモデルの上限として扱うようにし、**点検・本文の分割・送信の3つが同じ値を見る**ようにしました
- この食い違いは以前からありましたが、0.29.10で指定が全機能に効くようになったぶん、**影響する範囲も広がっていました**。指定していない場合（既定の `0`）は、これまでどおりモデルの上限をそのまま使います

## 0.29.10 - 2026-09-01

### 抽出後の知らせと「直さない語」の置き場所を直し、Ollamaの設定の反映漏れを塞いだ

- **不具合の修正**：設定資料の抽出後、右下の知らせで「一覧を開く」を押すと、開いているファイルによっては関係のないファイル（`synopsis.md` など）が開いてしまうことがありました。作品フォルダをVS Codeのワークスペースの外に置いている場合に起きます。押した先を、抽出した中身がそのまま見られる設定資料パネルへ変更し、名前も**「設定資料を見る」**に改めました
- **不具合の修正**：抽出後の「更新分を反映」だけが、提案パネルではなく別のダイアログを開いていました。同じ操作なのに窓口が2か所に分かれていたことになります。**「提案を見る」に改名し、提案パネルへ**つなぎました。承認待ちがあるときは、この項目が知らせの先頭に出ます（抽出のあとに決めるのは「全部反映するか、1件ずつ選ぶか」なので）
- 「直さない語を管理」を**「指摘対象外を管理」**に改名し、「作品ごとの設定」から「校正・校閲」の「誤字脱字を検知」のすぐ下へ戻しました。指摘を見てその場で足すものなので、設定のように離れた場所へしまうのは合いませんでした。「作品ごとの設定」は5項目から4項目になりました
- **不具合の修正**：設定資料の抽出で、まとめて送る量が常に6,000字で頭打ちになっていました。「モデルによって可変」を選んでいても、束ねる量だけは既定値（6,000字）を自前で読み直していたためです。直したことで、束ねる量の目安が作品によっては6,000字から20,000字程度まで増え、抽出の呼び出し回数と送信量が減ります
- **不具合の修正**：`novelai.ollama.numCtx` の設定が、AIを呼ぶ17機能のうち誤字脱字・設定資料の抽出・設定資料パネルの3つでしか効いていませんでした。推敲・矛盾検知・伏線・あらすじ・紹介文・プロット・AI相談などでは無視されていました。読む場所を1か所へ寄せ、すべての機能で効くようにしました

## 0.29.8 - 2026-09-01

### 詳細メニューの整理の続き——設定を設定画面から呼べるようにし、ラベルのずれを直した

- 「セットアップ」「機能ごとにAIを割り当てる」「Ollamaのセットアップ」「LM Studioのセットアップ」「意味検索（ベクトルDB）の準備・索引を作る・索引を削除する」「作者／編集者を切り替える」を、VS Codeの設定画面（`@ext:nonahisa.novel-ai-assistant`）の該当する設定の説明の下から呼べるようにしました。詳細メニューからは出さなくなりましたが、**機能そのものは残っています**
- 「ルビを振る」「傍点を付ける」を詳細メニューから外しました。簡単ステップメニューの「入力を楽に」からは今までどおり使えます
- 「作品の文字数を表示」を削除しました。あちらにしかなかった**総文字数と段落数**は、執筆統計パネルの「作品の総量」カードに合流しています
- 「AIに相談する」を**「AIに相談する（大きく開く）」**に、「AIに相談する（横のパネル）」を**「横のパネルへ移動」**に改名しました。後者を押しても何も起きないように見えたのは、横のパネルが既に開いていたためで、不具合ではありませんでした。あわせて、大きく開いた相談画面の「…」メニューからも横のパネルへ移動できるようにしました
- 「編集部とやり取り」を作品管理メニューの一番下へ移しました

## 0.29.7 - 2026-08-31

### 詳細メニューの整理——散らばっていた「設定」を1か所へ集めた

- 作者の指摘「表に出ている状態のものが多くあります。たくさんありすぎて目が滑ります。設定管理画面にしまいましょう」を受け、分類をまたいで散らばっていた5つの設定を1か所へ集めました
- 「拡張機能の設定」の下に「作品ごとの設定」を新設し、次の5つをそこへ移しました（**消していません。移しただけです**）
  - この作品の目標を決める（元は執筆データ）
  - 形式とジャンルを決める（元は執筆AI支援）
  - 直さない語を管理（元は校正・校閲）
  - 一覧に項目を増やす（元は資料抽出）
  - 告知の設定（ハッシュタグ・URL）（元は広報支援）
- 判断の基準は「一度決めたらしばらく触らないか」であって、機能の近さではありません

## 0.29.6 - 2026-08-31

### 拡張機能とOllama常駐アプリが、ポートを取り合っていました（点滅の真の原因）

- 「点滅するようになりました」というご報告を実機で追い、真の原因を特定しました。**点滅していたのは拡張機能が起こしたOllamaではなく、正常に動いているそちらのポートを、Ollamaのタスクトレイ常駐アプリが1秒ごとに取り返そうとして失敗し続けていた**ものでした（4分間で232回）
- 起きていた順序：ログイン直後、拡張機能側が先にOllamaを起こし、数秒遅れて起動してきた常駐アプリがポートを取られていると気づいて、延々と取り返そうとしていました
- **起こす前に、必ず一度確かめるようにしました。** 既に動いていれば起こさずそのまま使い、応答が無くても3秒だけ待ってから起こします。この3秒のあいだに常駐アプリが立ち上がれば、取り合いそのものが起きません

### そのほかの修正

- Ollamaを起動したあと、後始末で標準エラーの受け口を閉じると、まれに起動した本体ごと終了しうることが分かったため、閉じずに「読み捨てる」方式に改めました
- AIチューニングが失敗したときの案内に「num_ctx を下げましたが」と出ることがありましたが、実際には一度も下げていませんでした。実際に試した値を伝えるよう改めました
- AIチューニングが「このモデルはこの機械には大きすぎます」と誤って伝えることがある不具合を直しました
- 申告されたコンテキスト長が4,096より小さいモデルで、測定の上限が正しく効いていなかった不具合を直しました
- 起動に失敗したとき、原因が分からないまま「終了コード null」とだけ表示されることがありましたが、「こちらの知らない理由で終了しました」という案内に改めました

## 0.29.5 - 2026-08-31

### AIチューニングが送る num_ctx を、固定から「その回に送る長さに合わせる」へ改めた

- AIチューニング（測って設定を合わせる）が送る `num_ctx` を、モデルの申告値への固定から、**その回に送る長さに合わせる**方式へ変更しました
- 申告値へ固定すると、短い回でもKVキャッシュを最大ぶん確保してしまい、GPUのメモリから溢れて黙ってCPU処理へ落ちていました。速度が10分の1になり、測定結果が実際よりずっと小さく出ていました
- モデルを読み込めなかったときの扱いを変更しました。**短い長さで一度も成功していない場合**は「このモデルはこの機械には大きすぎる」と伝えて中止し、**短い長さで成功している場合**は「その長さは入らない」と数えて探索を続けます
- 測定結果には、実際に使った `num_ctx` の最大値を添えるようにしました

## 0.29.4 - 2026-08-31

### ポートを握ったまま応答しない古いOllamaを見分けるようにした

- Ollamaの起動で、古いOllamaがポートを握ったまま応答しない状態を見分けるようにしました（新種別 `port_in_use_stale`）。起動直後の1秒だけ、プロセスの即終了と標準エラーを見張ります
- その場合の案内は「タスクトレイのOllamaアイコンから終了（無ければタスクマネージャーで ollama.exe を終了）」と終わらせ方を示し、**再試行は促しません**。ポート番号は失敗の文から実際の番号を取り出して示します
- 既にOllamaが動いていて起動時のbindに失敗しただけの場合は、応答を1回確かめて成功として扱います
- 理由が読めないまま即終了した場合も、応答を1回確かめてから失敗とし、終了コードを添えます
- 標準エラーを受け取るのはWindowsだけです（mac/LinuxではGoのSIGPIPEの仕組みにより、パイプを閉じた直後のログ書き込みでOllama本体を落としかねないため）

## 0.29.3 - 2026-08-31

### 作品が決まっているのに、選び直させていました

- **GitHubと同期したあとの「設定資料を抽出」**（作者の報告）。知らせは「〈作品名〉で N 件のファイルが更新されました」と作品を名指ししているのに、押すと「どの作品を抽出しますか」と訊いていました。押した先へ作品を渡すようにしました
- 同じ書き落としが**ほかに4か所**ありました。どれも「作品が決まっている知らせから押す」ものです
  - 本文からプロットを起こすときの「各話あらすじを作る」
  - 過去の版へ復元したあとの「GitHubへ送信」
  - 編集部の提案を取り込んだあとの「提案を見る」
  - 設定資料からルビを振るときの「本文を .md にする」

## 0.29.2 - 2026-08-31

### AIチューニング中の画面の点滅を止めた（作者のご指示）

- 測定は送る長さを4,000字→8,000字→…と倍々に変えるため、**そのたびにOllamaがモデルを読み込み直し**、画面が点滅していました
- 測定のあいだは**文脈の広さを1つに固定**します。読み込みが1回で済みます
- モデルが大きくて**その広さで読み込めないときは、半分ずつ下げて**測ります。下げたときは「申告の262,144では読み込めなかったため、131,072に下げて測りました」とお伝えします（黙って別の条件で測ると、結果の意味が変わるため）
- AIチューニングの記録が**作品フォルダに残っていません**でした（出力パネルにしか出ていませんでした）。作品が分かるときは残します

## 0.29.1 - 2026-08-31

### AIが動いているのに「接続できません」と出ることがあったのを直した

- 抽出の9チャンク目だけが失敗し、**8チャンク目まで通っているのに**「Ollamaに接続できません」と出ていました（作者のログ、00:03）。起動を確かめても直らない、案内の間違いです
- Nodeの通信部品は、こちらが決めた待ち時間とは**別に自前の待ち時間**を持っています。そちらで切れたときを「接続できない」と取り違えていました。**時間切れとして扱い**、「待ち時間を延ばすか、一度に送る量を減らしてください」と案内します
- 本当に繋がらないとき（AIが起動していない・宛先が引けない）は、これまでどおり「AIを起動してください」と案内します

### 通信の失敗の記録に、原因の符号を残すようにした

- これまで記録には「fetch failed」の5文字しか残らず、**「AIが落ちた」「接続が切れた」「時間切れ」の区別が付きません**でした。上の失敗も、何が起きたのかログから追えませんでした
- 原因の符号と説明を添えて残します。次に同じことが起きたときに追えます

## 0.29.0 - 2026-08-31

VSIX配布版。0.28.0 の配布に、0.28.1〜0.28.18 の修正とAI相談パネルの直しを加えたものです。

### AI相談パネルでも「AIを起動」できるようにした

- 手元のAIが止まっているとき、相談パネルでは**赤い文字が出るだけで起こす手立てがありません**でした。ほかの機能と同じように「Ollamaを起動」「LM Studioを起動」を出します
- 料金の確認より前に出します（繋がらないと分かっているのに料金の話をしても仕方がないため）

## 0.28.18 - 2026-08-30

### 「AIを起動」ボタンが出ない機能があったのを直した（作者の報告）

- 手元のAI（Ollama・LM Studio）が止まっているとき、**起動を促すボタンが出る機能と出ない機能がありました**（8対7）。**AIチューニング・冒頭診断・更新告知文・作品紹介文・キャッチコピー・各話あらすじ・名前の候補・設定資料パネル**では出ず、接続できないと言われるだけでした
- すべて同じ入口を通るようにしました。**料金の確認より前**に出し、**AIを1回も呼ばない回には出しません**（各話あらすじが全話最新のときなど）
- 今後この抜けが起きないよう、検査で固定しました

## 0.28.17 - 2026-08-30

### 抽出中にコンソールが何度も一瞬ずつ開く問題を直した（作者の報告）

- 「設定資料の抽出中、チャンクの度に黒い画面が一瞬立ち上がる」というご報告の原因です。Ollamaへ渡す**文脈の広さ（num_ctx）を、送る文章の長さから毎回計算**していたため、チャンクごとに値が変わっていました
- **Ollamaは値が変わるとモデルを読み込み直します**。そのたびに内部で別プロセスが起動し、コンソールが一瞬見えていました。**読み込み直しは遅いので、時間切れの原因にもなります**
- 値を4096の段に丸めて、長さの違うチャンクでも同じ値になるようにしました。必要な量は必ず確保します（切り捨てません）
- どの広さで確保したかを記録に残すようにしました。これまで残しておらず、**記録からは原因を追えません**でした

## 0.28.16 - 2026-08-30

### 伏線の回収で「張った場所そのもの」を回収と誤って挙げていたのを直した

- 実際のログで、AIが挙げた8件のうち**5件が「伏線を張った文そのもの」**でした。拡張機能側で弾いていたので誤って登録されることはありませんでしたが、**AIの答えの大半が無駄**になっていました
- AIには「張った箇所：…」と伝えて渡しているのに、**それを回収として挙げないでほしいとは書いていません**でした。指示に足しました
- 0.28.7 で入れた「退けた理由の内訳」がログに出たおかげで原因が分かりました

## 0.28.15 - 2026-08-30

### Ollamaの実行ファイルの探し方を直し、起動の記録を残すようにした

- 実行ファイルを探す順が**PATHが先**になっており、PATHに通っていない環境では**実在するインストール先が一度も試されません**でした（LM Studio 側は正しく「既定の場所を先」にしてあり、逆になっていました）
- Windowsでは、インストーラがPATHを更新しても**すでに動いているVS Codeには反映されない**ため、入れた直後だとこれで失敗します
- 起動の各段（どこを探したか・何を起動したか・応答までの時間・失敗の理由）を記録に残すようにしました。これまでは**何も残らず、うまくいかない理由を追えません**でした

## 0.28.14 - 2026-08-30

### 直らない失敗のときに、同じ失敗を何度も繰り返さないようにした

- モデルがメモリに載らない・鍵が無い・残高が無い、といった**AI側の環境の失敗**は、本文のどの部分を送っても同じ結果になります。それでも最後まで送り続けていたため、作者のログでは**伏線の回収確認が同じ失敗を9件**積んでいました
- 1回目で止めて、理由を1つだけ記録に残すようにしました。「残りのチャンクは試しません」と書くので、途中で終わったことが分かります
- 応答が読めない・長すぎて入らないといった**本文の中身が原因の失敗**は、これまでどおり次へ進みます（次の部分なら通ることがあるため）
- 誤字脱字検知と登場人物の抽出には既にこの判断がありましたが、**伏線・推敲・矛盾検知には無い**状態でした。判断を1か所へまとめ、5つの機能すべてで同じにしました

## 0.28.13 - 2026-08-30

### 誤字脱字検知の進み具合が「9/7」のように狂うことがあったのを直した

- 本文が入り切らなかったときは分割して送り直しますが、その回数が**送る側の数にだけ反映**されていました。そのため「AIへ送信: 4/9」「応答を受信: 4/7」のように分母が食い違い、最後は「9/7」と分子が分母を超えていました
- 分けて送り直したときは「入り切らず N回に分けた」と記録に残します

## 0.28.12 - 2026-08-30

### 実機確認リストを、いまの実装に合わせた

- 「AIチューニング」の確認項目が、名前も書き込み先も古いままでした。モデルごとに覚えること・押さなければ書かれないこと・時間切れの調整まで含めて書き直しました
- PDF出力に「ブラウザが実際に開くか」を足しました（0.28.1で直した箇所です）

## 0.28.11 - 2026-08-30

### AIチューニングの不具合を6件直した（作った直後の点検で発見）

- **時間切れの通知から「AIチューニングを実行」を押すと、時間切れしたのと別のモデルを測ることがありました。** 「機能ごとにAIを使い分ける」を設定していると起きます（誤字脱字だけ別のAIにしている場合など）。**測っても直らない**状態だったので、その機能のAIを測るようにしました
- **作者が設定に手で書き足した項目が、測定のあとで消えることがありました。** 台帳を書き換えるとき、こちらが知らない項目まで作り直していたためです。**知らない項目はそのまま残します**
- 設定が書き込めないときなどに、**測り直しのため一時的に延ばした待ち時間が残ったまま**になることがありました。必ず元へ戻します
- 設定を手で編集して極端な値（10万秒など）を書くと、そのまま待つようになっていました。**上限600秒で挟み**、挟んだことをログに残します
- 作品フォルダ側に設定があるとき、反映しても**無言で効かない**ことがありました。書き込み先を選ぶようにしました
- 時間切れの案内「既定は180秒」がClaudeでは正しくなかったのを直し、有料AIでの見込み額に**測り直しの1回**を足しました（少なく見せないため）

## 0.28.10 - 2026-08-30

### AIチューニング——モデルごとに測って覚えるようにした（作者のご提案）

- 「AIが実際に読める長さを測る」を「**AIチューニング（測って設定を合わせる）**」に改めました。測った値は、**いま選んでいるモデルの設定として覚えます**。モデルを切り替えれば、そのモデルの値に自動で変わります
- これまでは「さくらの設定」のようにAIごとに1つだったので、**同じAIで別のモデルに変えると、測った値が合わなくなっていました**
- **待ち時間（タイムアウト）も一緒に決めます。** 測っている最中に時間切れが起きたら、待ち時間を一時的に倍にして測り直します。「設定に反映」を押さなければ、**延ばした値は元に戻します**
- 覚えた内容は設定 `novelai.modelTuning` に入ります。中身は見られますし、消せば測る前の状態に戻ります

### 時間切れになったとき、チューニングへ誘うようにした（作者のご提案）

- 誤字脱字検知などで時間切れが起きたら、「**AIチューニングを実行**」ボタンを出します。そのモデルに合った待ち時間を測れます
- **料金のことも明示します**。お使いのAIが有料なら「AIを呼ぶので料金がかかります」、無料なら「AIを呼びますが、このAIは無料です」とお伝えします

## 0.28.9 - 2026-08-30

### 時間切れになったとき、原因を追えるようにした

- 記録に「180秒で切れました」としか残らず、**本文が大きすぎたのかAIが遅かったのか分かりませんでした**。送った本文の字数を残します
- 案内を「設定でタイムアウトを延ばすか」から、**触る場所の名前**（「タイムアウト」「1チャンクの文字数」）まで書く形にしました
- 実測（57件）では応答は中央34秒・90%点124秒で、**既定の180秒に余裕がありません**。時間切れはローカルの小さいモデルだけでなく、クラウド（さくら）でも起きています

## 0.28.8 - 2026-08-30

### 一部が失敗したときに「完了しました」と出ていたのを直した

- 誤字脱字検知で3件中2件が時間切れになった回に、「**完了しました。指摘 0件 / 失敗 2チャンク**」と出ていました。これでは「誤字が無かった」と読めますが、実際には**本文の3分の2を見ていません**
- 一部でも失敗したときは、警告として「一部を処理できませんでした（3件中2件が失敗）」「失敗した部分は見ていないので、そこに何かあっても出ていません」とお伝えします。誤字脱字・伏線・プロット逸脱・推敲・矛盾検知の6か所を揃えました
- **時間切れそのものはまだ直していません。** 小さいモデルに大きな本文を送ると180秒で切れます。設定でタイムアウトを延ばすか、チャンクを小さくしてお使いください

## 0.28.7 - 2026-08-30

### 伏線の検知で、AIの答えを退けた理由をログに残すようにした

- ログには「本文と合わない N件」としか出ておらず、**なぜ退けたのかが分かりませんでした**。実際のログで、伏線の回収の確認が「候補0件／本文と合わない10件」（＝1件も拾えていない）になっていましたが、原因を絞り込めませんでした
- 「実在しない伏線番号 10件」のように内訳を出します。理由によって直す場所が違う（AIへの指示なのか、照合の厳しさなのか）ためです
- **これは原因を直したのではなく、次に回したときに原因が分かるようにしたものです**

## 0.28.6 - 2026-08-30

### 薦めるモデルの名前が2か所にあったのを1つにまとめた

- 「最初に薦めるモデル」の名前が、セットアップの2か所に別々に書かれていました。薦めるモデルを変えたとき片方だけ古いまま残る形だったので、1か所にまとめました（いまの動きは変わりません）
- あわせて、モデル名がコードに散らばらないよう検査を足しました

## 0.28.5 - 2026-08-30

### ブラウザ版が起動しなくなる変更を、検査で止めるようにした

- ブラウザ版（vscode.dev）では使えない機能を、うっかり読み込む形で書いてしまうと、**拡張機能が開いた瞬間に落ちます**。ビルドは通ってしまうため気づけませんでした
- 手順としては文書に書いてありましたが人手頼みだったので、検査にしました。現状は問題ありません（300ファイルを調べて違反0件）

## 0.28.4 - 2026-08-30

### 内部の整理（動きは変わりません）

- AIの失敗を伝える処理が4か所に写されていたのを1つにまとめました。片方だけ直る形を無くすためです
- 送った内容が長すぎたときの判定が、作者向けの通知文から番号を読み取る形になっていました。文言を直すと黙って効かなくなるので、失敗そのものに番号を持たせるようにしました

## 0.28.3 - 2026-08-30

### 中止ボタンが効かなかった不具合を修正

- **作品紹介文・キャッチコピー・各話あらすじ・更新告知文**で、「中止」ボタンは出るのに押しても止まりませんでした。有料AIではその間ずっと課金されます
- **各話あらすじ**は、話と話の間でしか中止を見ていませんでした。1話に数十秒かかるので「押したのに、その話が終わるまで止まらない」状態でした。呼び出しの途中でも止まります
- 新しくAIを呼ぶ処理を足したときに同じ忘れが起きないよう、検査で固定しました

## 0.28.2 - 2026-08-30

### 書き込みに失敗しても黙って進んでいた箇所を直した

- **ルビ・傍点を入れる処理**で、書き込みが失敗しても確かめずに先へ進んでいました。入っていないのに入れたところを選び直すだけになり、「振ったのに反映されない」と見えます。失敗したら理由を出してログに残すようにしました（シーンメモの挿入と同じ扱いに揃えました）
- 生成した読み物を無題の文書で開くとき、中身が入らなくても空の画面が出るだけで理由が残りませんでした。ログに残します

### パッケージ導入でも環境変数を子に継がせないようにした

- 0.27.0 で LM Studio と Ollama の起動について直したものと同じ手当てを、残っていた1か所（パッケージ管理の実行）にも入れました。いま呼んでいるものには影響しませんが、同じ穴を1つだけ残さないためです

## 0.28.1 - 2026-08-30

### PDF出力で「開きました」と出るのにブラウザが開かなかった不具合を修正

- 作品フォルダの名前に日本語が入っていると、VS Code が「外部プログラムを開く際にエラーが発生しました（0x2）」を出しているのに、拡張機能は「ブラウザで開きました」と告げていました。**開けたかどうかを確かめていなかった**のが原因です
- お使いのパソコンでは OS の機能で直接開くようにしました（日本語のフォルダ名でも開きます）。それでも開けなかったときは、成功したことにせず「フォルダーの中の .html をダブルクリックしてください」とご案内し、フォルダーを開くボタンを出します

### Ollamaがモデルを読み込めないときの案内を直した

- 大きなモデル（19GBなど）でメモリが足りないと Ollama がエラーを返しますが、「出力上限とモデル設定を確認してください」という的外れな案内が出ていました。**モデルが読み込めていないので、出力上限を見ても直りません**
- 「より小さいモデルを選ぶか、読み込むときの文脈の長さを短くしてください。ほかのアプリを閉じてメモリを空けると載ることもあります」と案内するようにしました。Ollama が返した詳しい理由はログに残ります
- あわせて、この案内が「LM Studio の設定で…」と決め打ちになっていたのを直しました（Ollama を使っているのに LM Studio の設定名が出ていました）

## 0.28.0 - 2026-08-30

VSIX配布版。0.27.0 の配布に、下の修正を加えたものです。

### Ollamaのモデル選択から、埋め込み用のモデルを外した

- モデルを選ぶ一覧に `bge-m3` のような**埋め込み用のモデル**が出ていました（作者の報告）。これは意味検索の索引づくりに使うもので、選んでも文章は返りません。生成に使えるモデル（`completion` に対応するもの）だけを出すようにしました
- 判定はモデルが自分で申告する対応機能（`capabilities`）で行うので、**新しいモデルが出ても一覧から消えることはありません**。申告が取れない古いOllamaでは、名前に `embed` や `rerank` を含むものだけを外します

## 0.27.0 - 2026-08-30

### 読み上げ（音読推敲）

- 原稿エディタのツールバーに「**読み上げ**」。OSの声で本文を読み上げ、**読んでいる文を光らせて**追います（AIもネットワークも使わないので料金はかかりません）。「⚑ 引っかかった」を押すと、その文の前にシーンメモ `// 音読：ここで引っかかった` を残して一時停止します。ルビは読み仮名で読み、傍点の印やシーンメモ行は読みません。詳細メニューの「原稿を読み上げる（音読推敲）」からも開けます
- 日本語の声が無いパソコンでは理由を表示します（Windows：設定 → 時刻と言語 → 音声 → 音声を追加）。速さは設定 `novelai.manuscriptEditor.readAloudRate`（0.5〜2）

### LM Studio を起動できなかった不具合を修正

- 「LM Studioを起動」を押しても60秒後に失敗していました。VS Code の拡張機能ホストが持つ環境変数 `ELECTRON_RUN_AS_NODE` を `lms` に継がせていたため、LM Studio 本体が素の Node として起動して即終了していたのが原因です。子プロセスへは渡さないようにしました（Ollama も同様）。起動の各段をログ（`.aiwriter/logs/actions.log`）に残すようにしました

### 「AIが実際に読める長さを測る」が上限超えのエラーで止まっていた不具合を修正

- さくらのAI Engine などで、上限を超えた長さを送ったときの HTTP 400 を「失敗」として測定を打ち切っていました。上限超えの定型文だけを「入らない」と分類し、探索を続けます。あわせて、本文を溢れさせない逃げ道（話ごと→半分）もこのエラーで効くようになりました。作者の実測では、さくらの `gpt-oss-120b` は既定の 32,000 トークンではなく **131,072 トークン**まで読めました

## 0.26.10 - 2026-08-29

### 更新告知文を作る（X用・活動報告用・後書き用）

- 「執筆AI支援 → 広報支援 → **更新告知文を作る**」（AIを使います）：話を選ぶと、その本文から**ネタバレしない告知文を3種**——X（旧Twitter）用の短文・活動報告／近況ノート用・後書き用の一言——を作り、Markdownで開きます。通知の「X用をコピー」などを押すとクリップボードへ入ります（3つとも順に押せます）。**貼るのは作者です**（投稿サイトへ機械的に書き込むことはしません）
- X用は「第N話「題」 更新しました」＋本文＋ハッシュタグ＋URLに組み立て、**Xの数え方**（日本語2・英数字1・URL23、上限280）で字数を数え直します。長すぎても切り詰めず、注意を付けて見せます。結末や正体を書かないよう指示し、「伏せたもの」も一緒に出します
- 「**告知の設定（ハッシュタグ・URL）**」で作品ごとのハッシュタグと作品ページのURLを覚えます（`.aiwriter/config.json`）。未設定でも作れます（URLは `{URL}` の目印）
- 前に出した告知（直近20件）を渡して、同じ言い回しを避けます

## 0.26.9 - 2026-08-29

### チャンクキャッシュに期限と目安の上限

- `.aiwriter/cache/chunks.json` は増える一方でしたが、**180日使われていない項目**を保存時に捨て、残りが4,000件を超えていれば**1日以上使っていない項目**から古い順に減らします。捨てる基準は「作った日」ではなく「最後に使った日」なので、**使い続けている鍵は消えません**（同じAI・同じモデル・同じ版なら、これまでどおりAIは呼ばれません）。捨てたときはログに件数が残ります
- 4,000件は硬い上限ではありません。今日の走査で作った項目どうしが押し合わないよう、今日触れた項目は超えていても残します

## 0.26.8 - 2026-08-29

### AIが実際に読める長さを測る

- 「拡張機能の設定 → AI → **AIが実際に読める長さを測る**」（AIを使います）：無害な詰め物の先頭と末尾に**その回だけの合言葉**を置いて送り、両方が返るかで「入力が黙って切り捨てられていないか」を確かめます。4,000字から倍々に増やし、返らなくなったら間を詰めて、**実際に読める上限**を8回ほどで割り出します。さくらのAI Engine のように上限を公開していないサービスで役立ちます（作者の発案）
- 結果は「実効の上限：約N字（約Tトークン）」と通知し、**「設定に書く」を押したときだけ** `sakura.contextWindow`／`lmstudio.contextWindow`／`openai.contextWindow` へ書きます。有料AIでは実行前に見込み（最大で約30円）を出して確認します。片側の合言葉しか返らないときは「先頭側／末尾側が切れる」と記録します
- この測定だけは前版の関所（上限超えを送らない）を素通りします——申告値が本当かを確かめるためのものなので

## 0.26.7 - 2026-08-29

### 本文を溢れさせない——送る量は実測、上限の関所を1つ

- 作者の問い「プロンプトの量の変更で本文が溢れることのないように」への対応。**AIへ送る直前に、本文＋指示＋資料＋出力の見込みがモデルの上限を超えていないかを1か所で確かめ、超えていれば送らずに理由（必要量と上限）を出します**。Ollama が入力を黙って切り捨てる経路は無くなりました
- 誤字脱字・抽出・設定資料の再読込で「指示ぶんの見込み」を固定の12,000字にしていたのをやめ、実際に組み立てたプロンプトの長さから決めます。指示や資料が育っても本文が押し出されません
- 誤字脱字・推敲・矛盾・伏線・抽出は、走り始めに指示と資料の量を測ってから本文のチャンクの字数を決めます（進捗に「指示と資料 ◯字を差し引き」と出ます）。矛盾検知の世界観の上限は固定30,000字からモデルの上限の25%に
- 入らないときの逃げ道：まとめたチャンクを話ごとに戻す → 半分に割る（下限1,500字）→ それでも入らなければそのチャンクだけ「このモデルには入りません（必要N／上限M）」として記録し、残りを続けます
- **小さいモデル（8,192トークン）では設定資料の抽出が成立しません**（指示だけで上限を超えるため）。これまでは黙って切り捨てられて動いているように見えていました。今後は理由つきで断られます

## 0.26.6 - 2026-08-29

### シーンメモ——本文中の付箋と、横に並べるパネル

- **シーンメモ**：本文の中に、行頭 `//`（全角 `／／` も可）で始まる1行を書くと**付箋**になります（例 `// TODO ここに潮の匂いの描写を足す`、`// 伏線 銀の時計→第12話で回収`）。最初の語がタグ（TODO・要確認・伏線・アイデア・自由）。原稿エディタでは**蛍光黄色のマーカー**で場所が分かり、マウスを載せると全文が出ます。右クリック「ここにメモを足す」
- **読者向けの出力とAIからは必ず消えます**：字数・投稿用コピー・PDF・Markdownプレビューには入らず、AIへ渡す本文では空行に置き換えて行番号を保ちます（誤字脱字などの指摘の行がずれません）
- **横に並べるパネル**（執筆AI支援 → その他支援 → シーンメモを開く。簡単ステップメニューの「3. 作品執筆」にも。原稿エディタの右クリックからも）：作品の全メモを「いま開いている話 → その他」の順に並べ、押すと該当行へ。**「次へ」「戻る」**（話をまたぎ、末尾で先頭へ。コマンド `novelai.nextSceneMemo`／`prevSceneMemo` にキーを割り当てられます）。カーソルに追従して近いメモが光ります。この話だけ・タグ・文字で絞り込み。**「済みにする」**でメモ行を消せます（取り消しは Ctrl+Z か復元）。件数の内訳、Markdown書き出し
- 作品一覧の話の右に「TODO 3」のような件数の印。執筆再開の1枚に「残っているメモ」の節（TODO・要確認を上に）

## 0.26.5 - 2026-08-29

### 年表——話数順と時系列順

- **年表**（執筆データ → 年表。簡単ステップメニューの「3. 作品執筆」にも）：1行＝1話で、登場人物・作中の変化・能力の習得・呼称の始まりと終わり・伏線の配置と回収・あらすじを並べます。AIは使いません。人物で絞ると**その人物の成長の年表**、種類で絞ると変化だけ・伏線だけになります。人物名を押すと設定資料へ、話の題を押すと原稿エディタへ
- **時系列順**：「時期・系統を編集する」（執筆データ）で時期（「十年前・火事の夜」など）と系統（IF・夢・劇中劇・並行する時間）を作り、話に時期を割り当てると、作中の順に並べ替えられます。IF・夢・劇中劇は本編と別の段、時期を決めていない話も別の段に出ます（本編に混ぜません）
- 「Markdownで書き出す」で `.aiwriter/generated/` へ。壊れた `timeline.json` は読まずに止め、話数順だけを出します

## 0.26.4 - 2026-08-29

### 名前の点検・相関図・生成文書のレビューで見つかった不具合の修正

- **名前の付け替え**：ルビの読みまで旧名のまま残っていたのを、対応表に「読み」の行を足して直せるようにしました（既定は変えない。確認画面で選べます）。他の人物の「関係」「呼称の相手」に残っていた旧名も書き換わります。別の付け替えの待ちがあるときは上書きせずに確認し、資料の保存に失敗したときは待ちを消さずに「もう一度」を促します。VS Code を開き直したあとは「本文の適用が済んでいるか確かめてください」の一文が出ます
- **人物相関図**：名前の一部が一致しただけで別人に線が引かれていたのを、名前全体の一致だけに直しました。同じ名前が複数の人物に当たるときは結ばず「結べなかった相手」として数えます。所属が非常に多い作品で図が壊れていたのを修正。名前の付け替えを資料に反映したあと、開いている相関図も読み直します
- **生成した文書の掃除**：手で置いたファイル（`冒頭診断_メモ.md` など）を消さないよう、日付と時刻の形をしたものだけを対象にしました
- **名前の点検**：1音の名前どうし（「ミ」と「サ」）を「1音違い」で当てなくしました。候補（P-29 v1.1）は、説明に書いた例示名（ミナ・アリア等）が候補として返っても落とします
- ブラウザ版で「本文を見る」「登場箇所」が開けなかった経路を修正

## 0.26.3 - 2026-08-29

### 人物相関図（全体図と個人中心図）

- **人物相関図**（資料管理 → 設定資料閲覧 → 人物相関図）：設定資料の「関係」と「呼称」（誰が誰を何と呼ぶか）から、登場人物のつながりを図にします。AIは使いません。**全体図**は所属ごとに分けた円周に人物を並べ、線の太さがつながりの強さ。登場話数・関係／呼称・所属・名前で絞り込めます。関係の無い人物は畳んで「ほか N人」
- **個人中心図**：設定資料パネルの人物の「相関図」ボタンから。中心の人物と相手を環状に並べ、互いをどう呼ぶかを両方向で表示。相手を押すと中心が移り、「戻る」で戻れます。2次の相手も切り替えで出せます
- 「SVGを書き出す」で `.aiwriter/exports/` に保存（印刷・共有用）。資料に無い相手は点線で残します（抽出漏れに気づけるように）

## 0.26.2 - 2026-08-29

### 名前の点検と付け替え／生成した文書は実ファイルに

- **名前の点検**（資料管理 → 設定資料閲覧 → 名前の点検。簡単ステップメニューの「2. 新作構想」にも）：登場人物の名前・読み・別名を突き合わせ、**響きの近い組**（読みが同じ／片方がもう片方の先頭／頭2音が同じ／1音違い など）を強さと理由つきで並べます。AIは使いません。読みが無い漢字名は「見ていません」と断ります
- **響きが重ならない名前の候補**（点検画面の「候補を出す」。AIを使います）：既存の名前と、作品の文化圏の系統（和風・英語圏・北欧・中華…を選べます）に合わせた候補を10件出し、既存の名前と響きが近いものはこちらで落として理由を添えます
- **名前の付け替え**（一括／個別確認／登場箇所へのジャンプ）：新しい名前と読みを入れると、姓・名・別名の対応表を確認したうえで、本文の登場箇所を**提案パネル**に並べます。1件ずつ「適用」、まとめて「まとめて適用」、「本文を見る」で該当行へ。本文が済んだら「名前の付け替えを資料にも反映」で設定資料・あらすじ・プロット・伏線の台帳も書き換わります。2文字以下の名前は最初から一括の対象に入れません（普通名詞と重なるため）
- **生成した文書（執筆再開の1枚・マニュアル・冒頭診断・伏線の一覧など）は実ファイルとして置きます**：作品の `.aiwriter/generated/`（作品に属さないものは拡張機能の保管庫）。VS Code を閉じるときの「保存しますか」が出なくなり、2枚目を開いても1枚目が消えません。**古いものは自動で掃除**（種類ごとに20件・30日）。GitHub へは同期しません

## 0.26.1 - 2026-08-29

### レビューで見つかった不具合9件の修正

- **LM Studio**：読み込み済みのモデルにも毎回「読み込んでいます…」が出ていたのをやめました。メモリ不足で断られたら**文脈の長さを半分ずつ下げて試し直し**（下限8192）、それでも駄目なときは設定 `novelai.lmstudio.loadContextLength` を案内します。別のモデルの文脈長を設定に書いてしまう誤り、サーバー起動の待ちに上限が無い問題、起動直後にモデルを読み込まない順序の穴も修正
- **伏線**：「何を示唆しているか」などの言い回しを含む正当な説明が空扱いになっていたのを修正。「登録」を続けて押すと同じ番号が2件できる競合を直しました
- **原稿エディタ**：.md化のあと旧 .txt のタブが残る（保存すると復活して二重になる）／指摘からのジャンプ直後に二重に開くことがある／改行コードが混在するファイルで1文字打つと未変更の行の改行まで書き換わる、の3件を修正
- **執筆記録**：「次の話」「最新話を書く」「新規話数ファイルを追加」で作った新しいファイルに書いた分が「今日 +0字」のままだったのを修正
- **提案パネル**：書庫で別の作品の検知を走らせたときに進みが出なかったのを修正（作品名つきで出ます）
- 検知で読めなかった話があれば、完了報告に「読めなかった話 N件」と出ます（黙って飛ばしません）

## 0.26.0 - 2026-08-29

### VSIX配布版（0.25.1〜0.25.5 をまとめたもの）

0.25.0 の配布以降に入った変更の詰め合わせです。中身は下の各版を参照してください。

- **LM Studio**：拡張機能から起動できるようになり、モデルも文脈の長さを指定して読み込みます（「文脈 8k」表示の解消）。読み込めなかった理由をそのまま通知（0.25.5）
- **AI相談**：使い方の説明を「全操作の目次＋関係する説明だけ」にして、送る量を約4分の1に（0.25.2・0.25.4）
- **原稿エディタ**：上の帯の字数を撤去、ダッシュの混在を作法チェックで指摘、右クリックの用語判定を修正、矛盾検知の検証段の進み表示（0.25.3〜0.25.5）
- **文書**：README・設計書・プロンプト設計書を実装に合わせて総点検（0.25.1）
- 使っていないコードを約1,000行整理（動きは変わりません）

## 0.25.5 - 2026-08-29

### LM Studio を拡張機能から起動し、モデルも拡張機能が読み込みます

- **「LM Studioを起動」ボタン**——AI接続の確認と「LM Studioのセットアップ」に、Ollama と同じ形で付きました。LM Studio に同梱のコマンド（`lms`）でサーバーを開始します。コマンドが見つからない機械では、これまでどおりの案内に戻ります（場所は設定 `novelai.lmstudio.cliPath` で指定できます）
- **モデル選択で「文脈 8k」と出ていた問題**——読み込んでいないモデルは設定値（既定8192）を表示していました。これからは**拡張機能がモデルを読み込むときに文脈の長さを指定**します（モデルの最大まで。上限は設定 `novelai.lmstudio.loadContextLength`、0＝最大）。読み込んだ実測値を設定に書き戻すので、LM Studio 側の設定を変える必要がなくなります。未読込のモデルは「文脈 最大128k（選ぶと読み込みます）」と出ます
- **読み込めなかった理由をそのまま通知に**——大きすぎるモデルを LM Studio の安全装置が止めたとき、「出力上限とモデル設定を確認」ではなく、モデル名と「メモリ不足の見込みで止めました」と LM Studio の説明（必要メモリ量）が出ます。実行前に分かったときはその場で止めます
- **ダッシュ「――」の間が空いて見える件と、偶数個なのに作法チェックが指摘する件**——どちらも、欧文の「—」（EM DASH）と和文の「―」が1つずつ混ざっていたのが原因でした。作法チェックが**字の混在**を見つけて「――」（和文の字で偶数個）への統一を提案するようにし、原稿エディタの「組んで書く」面ではダッシュの字だけ和文の明朝で組んで隙間が出ないようにしました
- 使っていないコードの整理（原稿エディタの「読む」「並べる」面の機構・三点リーダの計器・全文の使い方説明・読む面専用の描画）。約1,000行減。動きは変わりません

## 0.25.4 - 2026-08-29

### 原稿エディタの上の帯の字数を撤去、AI相談の説明の付け方を単純に

- **原稿エディタの上の帯にあった字数を消しました**（下段の「このファイル」と同じ数字が2か所に出ていたため。作者の指示）
- AI相談で「使い方を教えて」のような**漠然とした質問には説明の束を付けず、全操作の目次だけ**を渡します（メニュー先頭の関係の無い説明が付いていたため）
- 伏線の台帳の注釈と CLAUDE.md の説明を、実際の保存の仕組み（退避の有無）に合わせて訂正しました。**動きは変わりません**

## 0.25.3 - 2026-08-29

### 矛盾検知の「検証」の段も、提案パネルに進みが出るようになりました

- 本文を読み終えたあとの検証（見つけた指摘を1件ずつAIに問い直す段）は、提案パネルの進みが「12/12チャンク」で止まったままに見えていました。**「検出した矛盾を検証 3/7件」**として別の札で流れます

## 0.25.2 - 2026-08-29

### AI相談で送る「使い方の説明」を軽くしました

- 相談のたびに全操作の説明（約6,200字）を送っていたのを、**全操作の名前の目次（約1,600字）＋質問に関係しそうな説明だけ**に変えました。本文の相談では目次だけになり、送る量が約4分の1になります。機能が増えても送る量が比例して増えません
- 目次には全操作の名前が常に入るので、「その機能はありません」と誤って答える心配は増えません。説明が手元に無い操作については、場所を答えたうえでマウスを載せたときの説明とマニュアルへ案内します
- 何を渡したかはログ（ヘルプ → ログを開く）に1行残ります。相談プロンプト（P-21）は3.4へ

## 0.25.1 - 2026-08-29

### ドキュメントの総点検（コードの動きは変わりません）

- README・設計書・プロンプト設計書・引継ぎ書・CLAUDE.md を実装と突き合わせ、約90か所を修正しました
- README：伏線の追跡・締切からの逆算を「未実装」と書いていたのを訂正（どちらも実装済みです）。原稿エディタの説明を現在の画面（組んで書くが標準・前後の話・字数表示）に合わせて書き直し。メニュー名を現行（詳細メニュー・資料抽出・AIで再読込・まとめて抽出）へ更新
- 設計書：執筆記録のファイル単位化（6.3.7）・MD化の促し（6.12.6）・「他のメニューを閉じる」（6.29.4）の節を新設。6.25・6.34に「組んで書くが既定になった」経緯を追記
- プロンプト設計書：キャッシュ鍵の記述に `providerId` を反映。P-23の参照誤り（P-14→P-11）を修正。各プロンプトに機能別割当のキーを明記。P-27/P-28を予約番号として確保

## 0.25.0 - 2026-08-29

### 原稿まわりの大きな整理（VSIX配布版）

- **執筆の記録がファイル単位になりました。** 原稿エディタ下段に「作品◯字／このファイル◯字／今日+◯字」が出ます（集計画面・目標・グラフの数字はこれまでと変わりません。過去の記録もそのまま使えます）
- **原稿エディタ**：「組んで書く」が標準になり、「読む」「並べる」のボタンと説明文を整理。**「← 前の話」「次の話 →」**（空白でない最終話では新しい話を作成）。誤字脱字の指摘から**開いている原稿エディタの該当行へ飛ぶ**ように修正（飛ばない原因だった内部の照合ずれと、組んで書く面がスクロールしない問題を解消）
- **本文ファイルは原稿エディタ（横書き）で開くのが既定に**（作品一覧のクリック・新規の話の作成・指摘からのジャンプ。エディタ内の「最新話」「前後の話」はいま見ている向きを保ちます）
- **検知の実行中、提案パネルにも「◯◯しています… 3/12チャンク」が出ます**（右下の通知と両方）
- **「簡単ステップメニュー」**（旧ステップメニュー）：最下段に「ヘルプ」段。各メニューの見出しから**「他のメニューを閉じる」**で1つに集中でき、「すべてのメニューを出す」で戻ります
- ルビ入りの .txt を開いたときの **.md化の促し**（変わるのは読み仮名の書き方だけ・投稿にも問題ない旨を添えて。断ったファイルでは出ません）

## 0.24.14 - 2026-08-29

### テキストファイル（.txt）でもルビ・傍点が組まれて表示されます

`｜漢字《かんじ》`（縦線省略の `漢字《かんじ》` も）と `《《強調》》` を、読む・並べる・組んで書くの各面とPDF出力で `.md` と同じ見た目に組みます。**原稿は1文字も書き換えず、表示だけ**です（.txtへルビを振る操作は今までどおり対象外）。

### そのほか

- **「組んで書く」を原稿を開くときの標準の面にしました**（「やめる」を押した原稿はその選択を覚えます。組めない原稿は安全弁が「書く」へ戻します）
- 三点リーダが行の中央に来ない原因（選んだ書体に「…」が無く欧文の字形に落ちる）への対処として、**三点リーダの字だけ和文の明朝に固定**しました
- 書く面で**スクロールや入力の直後に色つきの文字がズレて見える**問題を修正（色の層が古い間は隠す——色が一瞬消えるほうが、ズレた字が見えるより軽い）

## 0.24.13 - 2026-08-29

### 実機のご報告への修正の続きです

- **組んで書く（実験）の三点リーダ**：点列の間が開く問題を、読む面と同じ作りに揃えて修正しました
- **右クリック**：用語の上で右クリックした**その時点**で、開いている設定資料パネルが該当項目に切り替わります（開いていないときは、これまでどおり品書きの「設定資料を見る」で開きます）。品書きの先頭の用語名の行はやめました
- **「最新話を書く」を画面右下へ**移しました。下段の色の凡例はやめ、**設定資料パネルのタブ**（登場人物・能力・組織・場所）を本文の用語と同じ色分けにしました
- 縦書きの**変換中の下線**：組んで書く面には右側指定が効く可能性があり確認待ちです（打つ面のtextareaは以前の実機確認で「日本語入力側が引く線のため動かせない」と判明しています）

## 0.24.12 - 2026-08-28

### 実機のご報告への修正をまとめて入れました

- **用語の色**：打つ面のマーカー（背景塗り）をやめ、**文字そのものに色**を付けるようにしました。文字サイズを変えると色の位置がずれる問題も直しました（折り返し幅がスクロールバーのぶんずれていました）
- **誤字脱字からのジャンプ**：その原稿が原稿エディタ（縦書・横書）で開かれていれば**そちらを前面にして該当行を選択**します。開かれていなくても、いま原稿エディタで書いている最中なら同じ向きで開きます
- **右クリックの設定資料**：パネルが開いているのに該当項目が出ないことがあったのを修正（資料を読み直して再試行）。**本文の用語から開いたときは一覧を畳んだ状態**で該当項目だけを出します
- **組んで書く（実験）**：三点リーダ「…」を**行の中央**に組みます。「そう……」に傍点、のような「…」を含む範囲へのルビ・傍点も振れます
- **ステップメニュー**：最上段を「選択作品：〜」に改め、✓アイコンを外しました（押すと選び直せます）。見出し右側の作品名表示はやめました

## 0.24.11 - 2026-08-28

### 「執筆を再開する」と単話プロットの雛形を追加しました

書き始めに、**前回どこまで書いたか（最新話の末尾）・前話までのあらすじ・未回収の伏線・この話の単話プロット・今日の目標**を1枚で出します（ステップ3「執筆の場」の先頭。AIは呼ばず、その場で開きます）。「単話プロットを作る」で、話ごとのプロット（視点／この話の目標／展開の箇条書き）の雛形を `設定/episode-plots/` に作れます。書くのは作者で、AIに筋書きは作らせません。展開の緩み判定と箇条書きに対する矛盾検知（AI）は次の段で入ります。

## 0.24.10 - 2026-08-28

### 伏線の配置と回収を、AIで検知できるようになりました（伏線追跡の第2弾）

「伏線を検知する」が本文から**後の展開を示唆する記述**を拾い、「伏線の回収を確かめる」が**未回収の伏線がその後の話で回収されたか**を調べます。どちらも結果は提案パネルに並び、**作者が「登録」「回収済みにする」を押した分だけ**台帳に入ります。引用が本文に実在するかを照合してから出し、張った箇所そのものを「回収」と言い張る誤検知も弾きます。同じ話の中で張って回収する伏線も対象です。「伏線の状態を変える」で、手で「回収済み」「意図して開けたまま」にもできます。機能別AI割当に「伏線の検知」が増えました。

## 0.24.9 - 2026-08-28

### 伏線の台帳を追加しました（伏線追跡の第1弾）

張った伏線を記録して、回収漏れを防ぐ仕組みの土台です（設計書6.35）。「伏線の一覧を開く」（未回収を上に表示）と「伏線を手で追加」がステップ4と詳細メニューの校正・校閲に入りました。**矛盾検知の指摘に「伏線として登録」ボタンが付き**、矛盾に見えて実は伏線だったものを、その場で台帳へ移して指摘を片付けられます。台帳は `設定/foreshadows/` に1件1ファイルで保存されます。**AIによる配置・回収の自動検知は次の版で入ります。**

## 0.24.8 - 2026-08-28

### 改行コードがCRLFの原稿での不具合を、根本から直しました

投稿サイトからダウンロードした原稿などはWindows形式の改行（CRLF）のことがあり、原稿エディタで次の3つが起きていました：**用語の色と右クリックの位置が1行につき1文字ずつずれる**／**ルビ・傍点が「本文が変わった」と誤って断られて振れない**／**打つたびに、近くの改行コードが1つずつLFへ書き換わっていく**（原稿を保つ決まりへの違反）。画面と文書の間で改行の数え方が食い違っていたのが根で、境界で正しく変換するようにしました。0.24.7の「組んで書く（実験）」で色が出なかった原稿でも、色が出るようになります。

## 0.24.7 - 2026-08-28

### 「組んで書く（実験）」——ルビが組まれたまま打てる面を追加しました

原稿画面に4つ目の面「組んで書く（実験）」が付きました。打つ面そのものにルビ・傍点が組まれて表示され、設定資料の用語の色付け・右クリックで資料表示・ホバーのチップもこの面で使えます。ルビ・傍点は「1文字のかたまり」として扱い、中は直接編集できません（消すときは1単位で消え、振り直すときは選んで「ルビ」を押します）。

**実験と名の付くとおり、縦書きでの日本語変換や取り消し（Ctrl+Z）の効き方は実機でしか確かめられません。** 面に入るとき「表示にして戻すと本文と完全に一致するか」を必ず確かめ、一致しない原稿では開かずに理由を出します。これまでの「書く」「読む」「並べる」は一切変わっていないので、合わなければいつでも戻れます。

## 0.24.6 - 2026-08-28

### 原稿の「並べる」が、書きながらルビを確かめられるようになりました

書く面と読む面を並べたとき、**いま打っている行を読む面が自動で追いかけ**、行の始まる側に淡い印が付きます（作者の要望「原稿でルビをルビとして表示する」の第1段）。読む面を手でスクロールして読み返している間は追従を止め、書く面でカーソルが行をまたぐと再開します。また、打つたびに読む面全体を作り直していたのをやめ、**変わった段落だけを差し替える**ようにしたので、長い話でも並べたまま軽く打てます。

## 0.24.5 - 2026-08-28

### PDF出力（印刷用）を追加しました

ステップ7「電子出版等」と詳細メニューの書き出しの並びに「PDF出力（印刷用）」が入りました。本文を印刷用に組版してブラウザで開き、ブラウザの印刷（Ctrl+P）で「PDFに保存」を選ぶとPDFになります。文庫サイズ・縦書き／A5・縦書き／A4・横書きが選べ、ルビ・傍点（圏点）・場面転換の空行も組みます。原稿は書き換えず、AIも呼びません。PDFを直接作らないのは、日本語の縦書きを正しく組めるPDF部品が実質無く、ブラウザの組版が最も品質が高いためです（設計書6.33）。

## 0.24.4 - 2026-08-28

### 機能ごとに使うAIを割り当てられるようになりました

詳細メニューの「AI → 機能ごとにAIを割り当てる」で、設定資料の抽出・誤字脱字・推敲・矛盾検知・プロットからの逸脱・あらすじなどの生成・AIに相談の7つを、それぞれ別のAIで動かせます（設計書6.28）。いちばん重い抽出は手元の無料AI（Ollama）で実用になっている実測があるので、重いものを無料に・軽いものを有料にという使い分けで課金を絞れます。割り当てない機能は、これまでどおりAI設定で選んだ既定のAIを使います。割当先が使えない環境では既定のAIで実行します（有料のAIなら、これまでどおり実行前に確認が出ます）。

**ご注意：この版で処理結果の記憶（キャッシュ）の鍵が変わるため、各機能の最初の1回は、変更のないファイルも含めて全部を送り直します。** 有料のAIでは、その1回ぶんの課金が再び発生します（同じモデル名を別のサービスで使ったときに結果を取り違える穴を塞ぐための、一度きりの作り直しです）。

## 0.24.3 - 2026-08-28

### 提案パネルで、読んでいた位置が勝手に先頭へ戻らないようにしました

別の作品の検知結果が届いたり、1件適用したりするたびに一覧が描き直され、読んでいた位置が先頭へ戻っていました（0.22.24からの積み残し）。同じ作品・同じ分類の描き直しでは位置を保ち、別の作品や分類へ切り替えたときだけ先頭から表示します。

### 開発用の検査を広げました

選択画面に「取りやめる」の道があるかを見張る検査が、実機確認メニューの画面（作り方が違う2画面）も対象になりました。

## 0.24.2 - 2026-08-28

### 「AIに相談」を本文の領域に大きく開けるようにしました

メニューの「AIに相談する」で、エディター領域の大きい画面が開きます（作者の指示）。横の細いパネルもそのまま残り、会話は両方で共通です（どちらで聞いても続きから話せます）。大きい画面には「できること」（誤字脱字の検知や資料の抽出をその場で始めるボタン）、「会話をメモに保存」（`設定/相談メモ/` へ新規保存。上書きしません）、「作品を選ぶ」が付きます。範囲を選んで聞くときは、本文の右クリックか「AIに相談する（横のパネル）」で従来どおり横に開けます。

### 使い方のマニュアルを開けるようにしました

ヘルプの「使い方（マニュアル）」と相談パネルのボタンから、できることの一覧（作品づくりの流れ・全操作・画面の説明）を1つの文書で読めます。メニューの説明から自動で組み立てるので、機能を足しても古びません。AIは呼びません（課金されません）。

## 0.24.1 - 2026-08-28

### 縦書きの三点リーダの隙間と、フォントサイズ変更でのずれを直しました

「……」の間に隙間があき、フォントサイズを変えると点列が柱の中央からずれていました（作者の報告）。回転させる箱の寸法を決めていなかったのが原因で、1文字角の正方形に固定しました。送りは1文字分になり、中心はフォントサイズに追従します。

## 0.24.0 - 2026-08-28

### 「AIに相談」から、設定資料の直しを頼めるようになりました

「アジャーノに殿下の情報が混ざっている」のように**設定資料の誤り・混入を相談**すると、「「アジャーノ」をAIで再読込」のボタンが返ります。押すと設定資料パネルが開き、留意点を引き継いで読み直しが走ります。**AIが資料を書き換えるのではなく**、結果は項目ごとの提案として並び、選んだものだけが反映されます。実在しない名前のボタンは出ません（AIの返した文字列が操作対象に直結しない、いつもの原則です）。

### 入力欄の角が丸くなりました

設定資料パネル・相談パネルの入力欄すべてです（作者の依頼）。

## 0.23.15 - 2026-08-28

### 「AIで項目を充実させる」が「AIで再読込」になりました

設定資料パネルで、**自由記載の「留意点」**を添えて読み直させられます（例：「他の登場人物〇〇の情報が混入しています。」空欄でも実行できます）。**混入と判断された記述は捨てず**、「はじいた情報の行き先」として並び、**本来のレコードへ挿入するか、新しいレコードを起こすか**を選べます（押すまで何も書き込みません）。アジャーノに皇子の情報が混ざった件は、この機能で直せます。

### 原稿の「読む」面に、用語のチップが出ます

用語にマウスを載せると、名前・種別・紹介のチップが出ます（縦書きでもチップは横書きで読めます）。

### 「書く」面で、範囲を選んで右クリックでも設定資料を引けます

選んだ範囲に重なる用語の「設定資料を見る」が出ます。

### 設定資料パネルの入力欄が、文字量に合わせて伸びます

パネルの幅を変えたときも測り直します（相談の入力と下書きは、これまでどおり固定です）。

## 0.23.14 - 2026-08-28

### 原稿の「読む」面で、三点リーダが行の中央に寄ります

「…」の位置はフォント任せで、環境によって横書きでは下に沈み、縦書きでは横倒しのまま出ていました。「読む」面では、横書きは中央を明示し、縦書きは90度回して縦の3点として出します（フォントが縦書き用の字形を持つかに依存しません）。

**「書く」面は対象外です。** 書く面は文字を直接打つ欄（textarea）で、文字単位の見た目の調整ができません。フォントの形のまま出ます（設定 `novelai.manuscriptEditor.fontFamily` でフォントを変えると改善することがあります）。

## 0.23.13 - 2026-08-28

### 「漢字をひらく」の判断が、品詞基準になりました

作者の指定どおり、ひらく対象を品詞で判断します：副詞（暫く・恐らく）／形式名詞・補助名詞（事・物・訳）／補助動詞（〜して置く・〜して見る）／連体詞（所謂・色んな）／接続詞（又・然し）／副助詞（迄・程）。**判断は絶対ではなく、作者の判断が優先です**（迷うものは挙げません）。

あわせて、**常用漢字表（平成22年内閣告示第2号、2,136字）との照合を拡張機能側に持ちました**。漢字ひらきの指摘の対象に表に無い字（例：悍）があるときは、「参考：「悍」は常用漢字表に無い字です」と説明に添えます。照合はAI任せにせず、表そのものと突き合わせます。

## 0.23.12 - 2026-08-28

### 設定資料の更新の「見送る」が、押しても効きませんでした

提案パネルの「設定資料の更新」で「見送る」を押しても、黙って何も起きませんでした（作者の報告）。見送りの処理そのものが繋がっておらず、押された指摘を探す順路からも漏れていました。見送ると承認待ちから片付き（レコードには触りません）、メニューの残数もその場で減ります。

### AIの助言が、作者の創作論を土台にするようになりました

作者の創作論（note「WEB小説再入門」全13回）を「AIに相談」の土台に入れました。「読まれない」「伸びない」系の相談には、読まれ方の5段階（見つけてもらう→興味→読み始め→読み続け→評価）のどこの問題かを切り分けて答えます。プロットやテーマの相談では、読者の欲求・対象年齢が決まっていなさそうなときだけ、AIから1つ質問することがあります。

あわせて、**作品紹介文は最初の1文に、キャッチコピーは最初の数語に作品の核**を置くようになりました（投稿サイトの一覧では後半が省略されて表示されるためです）。

### 新機能「冒頭を診断」

第1話の冒頭（約3,000字）だけを見て、**いつ・どこで・誰が・何を・なぜ・どのように、が読者に伝わるか**と、**続きを読みたくなる引きがあるか**を診断します（WEB小説は冒頭で読み続けるかが決まる、という創作論より）。結果は表つきの文書で開きます。本文は書き換えません。詳細メニューの「校正・校閲」と、ステップメニューの「4. 自己校正」にあります。

### 推敲の観点が6つになりました

「読みに詰まる漢字（然し・所謂など）」と「語尾の単調（〜た。の連続）」が加わりました（創作論の読みやすさ技術より）。指摘の上限（1000字あたり3件）は変わりません。**この2観点は実モデルでまだ測っていません**——おかしな指摘が出たらお知らせください。

## 0.23.11 - 2026-08-27

### 済んだ確認項目を引継ぎ書へ移しました

実機確認で済んだ15件（誤字脱字のまとめ送信・.txt→.mdの中身保持・書庫へのまとめ直し・矛盾検知の時系列・縦書きエディタの表示記憶）を、確かめた中身ごと `docs/進捗と引継ぎ.md` へ書き移し、確認リストから消しました。メニューの「テスト中」の件数もそのぶん減ります。

## 0.23.10 - 2026-08-27

### プロット逸脱を「無視」したときの記録が「設定」と書かれていました

編集履歴に残る作業記録の見出しが「設定／本文」で決め打ちされており、プロット逸脱の指摘を無視したときも「設定」と記録されていました。指摘と同じ見出し（プロットでは／この話では）で記録するようにしました。

## 0.23.9 - 2026-08-27

### 矛盾・プロット逸脱の指摘にも「再チェック」が付きました

「誤字をなおした後、再確認したい」（作者の依頼）。誤字脱字・推敲・表記ゆれには付いていた再チェックを、矛盾とプロット逸脱の指摘にも付けました。本文を手で書き直したあとに押すと、解消したかをその1件だけで確かめます（本文が変わっていなければAIを呼ばずにその場で分かります）。

- **確かめられるのは本文を直した場合です。** 設定資料のほうを直した場合は「本文が変わっていません」と出ます（照合が本文だけを見るため。設定側の直しも見る形は今後の課題です）

## 0.23.8 - 2026-08-27

### 別人に分けた人物が、次の抽出でまた混ざっていました

「別人に分ける」で分けた直後の抽出で、AIが2人を混ぜた結果（例：アジャーノに別名「殿下」を付けて、皇子の場面の情報を返す）をそのまま受け入れていました（作者の報告「アジャーノと殿下が混同されています」）。呼び名を足さない防御はありましたが、**候補の中身（紹介文・一人称・呼称）は取り込んでいた**ため、冒険者のレコードが皇子の紹介文に書き換わっていました。

これからは、**作者が別人と決めた呼び名が付いた候補は丸ごと取り込みません**。取り込まなかった場面は、抽出の完了報告に件数と例で出します（黙って捨てません）。

## 0.23.7 - 2026-08-27

### 設定資料パネルの「作品情報」に何も表示されませんでした

タブの件数（例：作品情報(2)）は出るのに、一覧が空のままでした（作者の報告）。一覧を描く変数の宣言漏れが原因で、最初の描画で止まっていました。作品紹介文・キャッチコピー・各話あらすじの3行が出るようになります。同じ型の間違い（宣言なしの変数）をパネル全体で検査するテストも足しました。

## 0.23.6 - 2026-08-27

### ステップメニューの見出しに、選んだ作品名が出ます

「ステップメニュー」の右に、いま対象になっている作品の名前が薄字で出ます（作者の依頼）。最上段の選択窓はスクロールで画面の外へ流れますが、見出しは常に見えます。作品が無ければ「未登録」、複数あって未選択なら「作品を選んでください」と出ます。

### 設定資料パネルの「名前で絞り込む」が、枠からはみ出していました

入力欄が一覧の右の仕切り線を越えていました（作者の報告）。余白のぶんだけ幅を引いて、枠の中に収めました。

### 開発用「実機確認を回す」に、戻る機能が付きました

項目一覧から節一覧へ、タイトル左の←で戻れます（作者の報告「戻る機能がありません」）。印を付けたあとも節一覧へ戻るので、続けて次の節を回せます。配布物には入らない開発用の道具です。

## 0.23.5 - 2026-08-27

### 「ステップメニュー」を新設し、旧「操作メニュー」は「詳細メニュー」になりました

作品づくりの流れに沿って主な操作を並べた「ステップメニュー」が、作品一覧の下に入りました（作者の依頼。設計書6.29）。**1. 作品登録 → 2. 新作構想 → 3. 作品執筆 → 4. 自己校正 → 5. 投稿脱稿 → 6. 編集部校正・校閲 → 7. 電子出版等**の7段です。

- **最上段が作品選択窓**です。作品が無ければ「未登録」、1作品なら自動で選ばれ、複数あれば押して選びます
- ステップ内の操作は、**選んだ作品に対してだけ**動きます（作品選択を訊かれません）。未選択のあいだは押せなくなり、理由が出ます
- 予定の機能（WEB投稿支援・EPUB出力）は「準備中」として押せない形で載せています
- 全操作はこれまでどおり**詳細メニュー**（旧・操作メニュー）にあります。AIの案内文なども新しい名前に直しました

## 0.23.4 - 2026-08-27

### Markdownの文書が、設定した既定の画面で開くようになりました

「反映待ちの更新」などの生成文書が、既定のMarkdown画面を無視して素のテキストで開いていました（作者の報告）。開き方を総点検し、次の4つを**設定した既定の画面**で開くようにしました：**反映待ちの更新／動作の診断／IME辞書の取り込み手順／セットアップで入れるものの一覧**。

- 生成文書には「反映待ちの更新.md」のような**名前が付きます**（保存されていない文書のままです。名前が無いと、既定の画面の紐付け `*.md` に当たらないためです）
- 「反映待ちの更新」が**プレビューを強制するのもやめました**。どの画面で読むかは作者の既定に従います——**既定を「テキスト エディター」にしている場合は、これまでのプレビュー表示から記法の見えるテキスト表示に変わります**
- 該当箇所へ飛ぶ操作（指摘の「本文を見る」など）は、位置指定にテキストエディタが必要なため従来どおりです

## 0.23.3 - 2026-08-27

### 作品一覧のタイトル部に「作品をすべて同期」を足しました

作品一覧の見出しの並び（追加・GitHubから追加・新規作成・再読み込み）に、⟳（作品をすべて同期）が入りました（作者の依頼）。再読み込みの手前です。ブラウザ版で押した場合は、これまでどおり理由の案内が出ます。

### 感情曲線の場所を、メニューの説明に書きました

「盛り上がり曲線がどこにあるか分かりません」（作者の報告）。感情曲線は独立した項目ではなく、**「紹介文・あらすじを閲覧」で開く文書の中の一節**です（紹介文の下・各話あらすじの上。あらすじを生成すると作られます）。メニューのどこにも「感情曲線」の言葉が無かったので、この項目の説明に書き足しました。

## 0.23.2 - 2026-08-27

### 「更新分を反映」の残数が、反映しても減りませんでした

提案パネルから設定資料の更新を反映すると、置き場からは正しく片付くのに、**メニューの残数の印を数え直す合図がパネルから出ていませんでした**（作者の報告「すべて更新しても0にならない」）。反映が成功するたびに数え直すようにしました。

### さくらのAIのモデル一覧の生応答を、一度だけ記録します

さくらのAIのコンテキスト長を自動で読めるか（LM Studioのように実値の欄があるか）を確かめるための実測用です。次に「AI接続の確認」か「AI設定」でさくらへ繋いだとき、モデル一覧の生の応答が1回だけログ（ヘルプ → ログを開く）に残ります。**鍵や原稿は含まれません。** 欄が見つかれば自動読取に切り替えます。

## 0.23.1 - 2026-08-27

### LM Studio の「文脈」が、実際に読み込んだ長さになりました

モデル選択に出ていた「文脈 8k」は、設定の既定値の表示にすぎませんでした（作者の報告。LM Studio側は131,072で読み込み済みでした）。LM Studio の native API（`/api/v0/models`）から**実際に読み込んだ長さ**を読むようにし、**読込済みのモデルはその値**を使います。文脈が正しくなるぶん、1回で送れる本文も増えます（131kなら約2万字ずつ）。

- 未読込のモデルと、native API の無い古い LM Studio は、従来どおり設定値です
- モデルの**最大対応長は使いません**——実際に読み込んだ長さより大きい想定は、入力が黙って切り捨てられる危険そのものだからです
- 複数のモデルを読み込んでいるときは、**いちばん短い長さ**を導入案内の初期値にします（長い方に合わせると、短いモデルで切り捨てが起きるため）

## 0.23.0 - 2026-08-27

### 配布のまとまり（VSIX）

0.22.11〜0.22.27 の内容をまとめた配布版です。主な中身（詳細は各版の項をご覧ください）：

- **手元のAIで入力が黙って切り捨てられていた問題の修正**（0.22.14。矛盾検知・推敲・あらすじ等の11か所）
- 誤字脱字・推敲・表記ゆれの指摘に**「再チェック」**（0.22.26）
- **LM Studio の導入案内**と Context Length 合わせ（0.22.22）
- 2作品同時の検知で**提案パネルが横取りされない**ように（0.22.24）
- 原稿エディタ「書く」面の**用語の色**の位置の修正（0.22.25）
- 作品を選ぶ画面に**作品ごとの残件数**（0.22.19）
- README と Marketplace の紹介文を、困りごと起点に書き直し（0.22.16）
- プロンプトキャッシュの読取量の記録（0.22.23）、独り言の調整（0.22.27）ほか

## 0.22.27 - 2026-08-27

### AIの独り言を、20秒の手待ちで話せるようにしました

「手が止まっている」と見なすまでの時間を2分から**20秒**にしました（作者の指定。話しかける保証はなく、同じことは1日1回まで・前の発言から間を置く抑えはそのままです）。

あわせて、設定の説明が「無料のOllamaを使っているときだけ」と古いままだったのを「**無料の手元AI（Ollama・LM Studioなど）**」に直しました。**動作そのものは以前から課金の有無で判定しており、LM Studioでも動きます**——説明だけが Ollama 決め打ちでした。

## 0.22.26 - 2026-08-27

### 指摘に「再チェック」を付けました（誤字脱字・推敲・表記ゆれ）

「提案の直し方が違う。自分で書き直したから、解消したか確かめたい」（作者の要望）。指摘ごとの「再チェック」ボタンで確かめられます。

- まず**本文と照合**します。指摘が引用している文がまだそのままなら、AIを呼ばずに「本文が変わっていません」とだけ返します（直し忘れがその場で分かります）
- 変わっていたら、**その指摘1件と前後の抜粋だけ**をAIへ送り、「まだ当てはまるか」を確かめます。解消なら一覧から外れ、未解消なら理由が書き添えられて残ります
- 有料のAIでは、最初の1回だけ確認します。指摘を消すかどうかを決めるのは、これまでどおり作者です

新しいプロンプトは P-23（プロンプト設計書に追記）。**実際のAIでの動きはまだ測っていません**——実機確認リストのA-1に確かめ方（4通りの直し方）を足してあります。

## 0.22.25 - 2026-08-27

### 原稿エディタの用語の色が、変な場所に浮くのを直しました

「書く」面の用語の色（本日入れた6.25.6）が、本文と無関係な場所（空行の上など）に浮いていました（作者の報告。縦書き・横書きの両方）。原因は2つありました。

- **縦書きの指定から、色を塗る裏の層だけが漏れていました。** 縦書きのとき裏だけ横書きで塗られ、まったく別の場所に印が出ていました
- **打つ面のスクロールバーの分だけ、裏の層の幅が広くなっていました。** 1行の字数がずれ、折り返しのたびに1行ずつ印が浮いていきます。裏の層の幅を打つ面の実測に常に合わせるようにしました

あわせて、**本文と目印の照合**を入れました。変換中などで本文の反映が待たされている間は目印を出さず、一致してから出します。「書く」面の案内文も「用語には色が付き、右クリックで資料を開けます」に改めました（右クリックは文字位置で判定しているため、縦書きでもそのまま働きます）。

## 0.22.24 - 2026-08-27

### 2作品を同時に検知しても、提案パネルが横取りされなくなりました

誤字脱字を2作品で同時に走らせると、片方を確認している最中に、後から終わった作品の結果へパネルが切り替わり、**適用済み・見送りの判断も含めて全部消えていました**（作者の報告）。指摘の置き場を作品ごとに分け、**別の作品の結果は画面を奪わず、通知でお知らせする**ようにしました（「表示する」を押したときだけ切り替わります）。

- 結果を持つ作品が2つ以上あるときは、パネルの上部に**作品の切り替え**が出ます。切り替えても、それぞれの作品の判断の途中経過はそのまま残ります
- 指摘が0件だったときは「指摘がありませんでした」とだけお知らせします

## 0.22.23 - 2026-08-27

### プロンプトキャッシュが効いているかを、記録で見えるようにしました

クラウドAIにはプロンプトキャッシュがあり、ChatGPT・Geminiでは自動で効いて入力が割引されます。ところが効いているかを知る術がありませんでした。送信量の記録（`usage.md`）に「キャッシュ」列を足し、**入力のうちキャッシュから読めた分**を残すようにしました。空欄は「対応していないAI」、0は「対応しているのに効かなかった」の意味です（効かない原因はプロンプトの並び順にあることが多く、この区別が調べる手がかりになります）。

**すでに `usage.md` がある作品では、見出しの列が古いままなので、新しい列は生のテキストでしか見えません。** きちんと見たい場合は、一度 `usage.md` を消してから測ってください（記録は使い捨てできるファイルです）。

## 0.22.22 - 2026-08-27

### LM Studio の導入案内を追加しました

操作メニューの「セットアップを開始」に「LM Studioのセットアップ」が入りました。接続の確認 → 導入（winget または配布ページ）→ 起動とサーバー開始・モデル読み込みの案内 → **Context Length 合わせ** → 使うAIとして選ぶ、まで通しで案内します。

Context Length 合わせは LM Studio 固有の大事な一歩です。**設定値が、LM Studio側でモデルを読み込んだときの長さより大きいと、入力が黙って切り捨てられます。** 案内の中で同じ値に合わせられます。

LM Studio はGUIアプリのため、サーバーの起動とモデルの読み込みは自動化せず、手順の案内と「もう一度確かめる」の往復で進めます（嘘の自動化をしないため）。ブラウザ版では出ません。

## 0.22.21 - 2026-08-27

### 実機確認中の使い勝手を2件直しました（作者の報告）

- **確認リストが素のテキストで開いていました。** 「実機確認を回す」の📖が、作者の既定のMarkdown画面を無視してテキストエディタ決め打ちで開いていました。既定の画面で開くようにし、既定がテキストエディタのときだけ従来どおり節の見出しへカーソルを置きます（開発ビルドのみの道具です）
- **右クリックメニューの並びを、設定情報を表示 → AIに相談する → 縦書きで開く の順にしました。** これまでは並び指定がなく、内部名のアルファベット順で「AIに相談する」が先頭に来ていました

## 0.22.20 - 2026-08-27

### 済んだ確認項目を、引継ぎ書へ移してリストから消しました

確認リストの決まり（済んだものは、確かめた中身を引継ぎ書へ書き移してから消す）が回っておらず、`[x]` が溜まっていました（作者の報告）。14件を引継ぎ書「実機（F5）で確認済み（2026-08-27）」へ書き移して削除し、全項目が済んだ表記ゆれ（A-2）は節ごと落としました（番号は振り直さず飛び番）。なお「テスト中」メニューと確認ランナーは元々未済（`[ ]`）だけを数えており、済んだ項目は出ていません。

この確認で、**ブラウザ版（vscode.dev）は作品の登録・一覧表示・GitHubからの登録まで実機で通ったことが確定**しました（0.15.2〜0.15.3の修正が効いています）。引継ぎ書の現状の記述も更新しています。

## 0.22.19 - 2026-08-27

### 作品を選ぶ画面に、作品ごとの残件数を出すようにしました

「重複をまとめる」はメニューに合計件数が出るのに、押した後の作品選択にはどの作品に何件あるかが出ませんでした（作者の報告）。作品ごとに「同じ人物とみられる組が N組」と出るようにし、同じ型の見落としをまとめて直しました。

- **提案を確認**：「未処理の提案 N件」／「未処理なし」
- **IME辞書を書き出す**：「設定資料が辞書より新しい」
- **GitHubへ送る／受け取る／同期**：「送信待ち N件」「受け取り N件」など。**書庫（1つの置き場に複数作品）では作品ぶんの数**を出し、置き場ぜんぶの数は括弧で添えます（全作品に同じ数字が並ぶ、作品一覧で実際に起きた失敗を避けるため）
- **分かれた分を合わせる**：分かれている作品が上に並び、見分けられます

候補が数えられなかった作品は無印になります（「0件」と言い切ると、読めなかった場合と区別が付かないため）。

## 0.22.18 - 2026-08-27

### 実機確認の操作ログを取るようにしました（開発ビルドのみ）

F5の開発環境で、拡張機能のコマンドを実行するたびに「いつ・何を」記録します（`logs/operations.jsonl`。Gitには入りません）。「実機確認を回す」の節一覧には「残りN／実行M回」と出るので、**どこをまだ触っていないか**が押した端から見えます。「操作ログを確認リストへ反映」を押すと、確認リストの節見出しの下へ `＊操作ログ：最終実行 …（計N回）` を書き足します（何度押しても増殖しません）。

**通ったかどうかの印（`[x]`）に、この仕組みが触れることはありません。** 判断は作者がします。配布物には入りません（本番の束に含まれないことを、束の中身と出口の見張りの両方で確認済みです）。

あわせて、作者が確認を済ませた3件（H節）を、確認リストの件数の生成物へ反映しました（残り350→347件）。

## 0.22.17 - 2026-08-27

### 矛盾検知の世界観に、上限を付けました

矛盾検知は世界観の全項目を毎チャンクそのまま送っており、送る材料の中でここだけ上限がありませんでした（設計書6.27.6の穴2）。上限（30,000字）を付け、超えるときは「本文に名前が出る項目 → その話に登場した記録のある項目 → 本文と語句の近い項目」の順で詰めるようにしました。**上限はいまの推定最大より上に置いてあるため、現在の規模の作品では送る内容は1文字も変わりません。** 送信量の実測（`usage.md`）を見てから絞るための安全弁です。

あわせて矛盾検知のプロンプト版を上げました。0.22.11の指紋の変更で次回はどのみち全チャンク送り直しになるため、**この版上げによる追加の送り直しはありません。**

## 0.22.16 - 2026-08-27

### 説明文を、困りごとから始まる形に書き直しました

READMEの冒頭とMarketplaceの紹介文を、機能の一覧からではなく「こんな困りごとのための道具です」から始まる形にしました。あわせて「大事にしている3つのこと」（原稿はあなたのもの／無料で完結できる／ふだんの書き方のまま）を明文化しました。機能そのものは変わりません。Marketplaceの表示に反映されるのは、次にVSIXを公開したときです。

## 0.22.15 - 2026-08-27

### 「実機確認を回す」が開かなくなっていました（0.22.13の退行）

0.22.13で節一覧の画面を作り直したとき、**画面を表示する一行（show）が抜けて**いました。組み立てて待つだけで一度も表示しないため、押しても何も起きず、エラーも出ませんでした。表示を戻し、「作った選択画面の数だけ表示しているか」を見張るテストを足しました。ご報告ありがとうございました。

### AIを切り替えても、「AIに相談」のエンジン表示が変わりませんでした

AIの選択は、変えても合図の出ない場所（globalState）に保存されており、開きっぱなしの相談パネルには切り替えを知る術がありませんでした。**実際に送る側は毎回選び直すので、古かったのは表示だけ**です。切り替えの合図を出すようにし、パネルがそれを聞いて表示（エンジン名と「有料」の印）を更新するようにしました。

## 0.22.14 - 2026-08-27

### 手元のAI（Ollama）で、多くの機能の入力が黙って切り捨てられていました

Ollamaには「どれだけの長さを読ませるか」（`num_ctx`）を毎回指定する必要があり、指定が無いときは8192トークンに固定していました。ところが指定して呼んでいたのは誤字脱字・抽出・設定パネルの一部だけで、**矛盾検知・推敲・逸脱検知・あらすじ・紹介文・プロット・AI相談など11か所は8192のまま**でした。チャンクが「モデルによって可変」（131kモデルで約2万字≒28,600トークン）になってからは、これらの機能で**入力の大半が読まれずに捨てられていた**計算になります。

指定が無いときは、**実際に送る文章の長さから必要量を見積もる**よう直しました（明示された指定はそのまま優先されます）。手元のAIで矛盾検知や推敲の結果が妙に薄かった場合、これが原因だった可能性があります。

## 0.22.13 - 2026-08-27

### 「実機確認を回す」から、該当機能へ直接飛べるようにしました（開発ビルドのみ）

節一覧の各行に▶ボタンを付けました。節へ入らなくても、その行から該当機能をすぐ実行できます。節の中の▶は右上の小さいアイコンで気づきにくかったため、案内文にも場所を書きました。実行する操作が無い節（見るだけ・環境が要るもの）には▶を出しません。この道具は配布物には入りません。

## 0.22.12 - 2026-08-27

### 相談の材料集めで、語句一致が取りこぼしていました

設定資料パネルの相談は、質問に近い場面を「語句一致」と「意味検索」の2系統で集めます。語句一致だけ、**作品全体から上位60件を引いてから**「その人物の場面」へ絞っていたため、人物名で母集団を数十件まで狭める相談では、絞った後にほとんど——ときには1件も——残りませんでした。意味検索は絞ってから引いており、非対称でした。語句一致も**絞った母集団から**引くよう直しました（設計書6.27.6の穴4）。修正前に落ちることを確かめた再現テストを4件足しています。

この修正で、語句一致側の材料はこれまでより多く集まります（渡す総量の上限は変わりません）。相談の答えの当たり方が変わって見えることがあります。

## 0.22.11 - 2026-08-27

### 入力が黙って切り捨てられうる見積りを、実測に合わせました

AIへ送る量から確保するコンテキスト長（`num_ctx`）を決めるとき、本文以外（指示・既知の名前リスト）の見積りが7,000字のままでした。プロンプトの改訂で指示が伸び、実測は約10,000字——**見積りが実測の半分**になっていました。見積りが足りないと入力は黙って切り捨てられ、「なぜか何も見つからない」という見え方になります。実測に余裕を足した12,000字へ上げました（設計書6.27.6の穴1）。

### 抽出を回すたびに、矛盾検知のキャッシュが全部飛んでいました

矛盾検知のキャッシュ鍵（設定の指紋）に、人物だけ**更新時刻**が混ざっていました。抽出は中身が同じでも更新時刻を書き換えるため、設定が何も変わっていなくても、抽出のあとの矛盾検知は全チャンクを送り直していました。指紋を「中身だけ」から作るよう直し、再現テストを足しました（設計書6.27.6の穴3）。

**この修正で指紋の作り方が変わるため、次に矛盾検知を回すときは一度だけキャッシュが効きません**（以後は安定します）。

## 0.22.10 - 2026-08-27

### 矛盾の裏取りに「いつ分かった設定か」が渡っていませんでした

矛盾検知は、指摘を採用する前にもう一度AIへ「これは本当に矛盾か」を確かめます（裏取り）。そのとき「この設定が分かる話：第◯話」を添える仕組みがあったのですが、**索引の書く側と読む側で鍵の区切りが違っており、一度も当たっていませんでした**。裏取りにはいつも「（不明）」が渡っていました。

- 「後の話で変わった値」を裏取りが見抜く材料が、これで初めて実際に渡ります
- あわせて、項目名（役割）決め打ちで引いていたのを、値だけで引けるようにしました（指摘には「どの項目の話か」が付いてこないためです）

### 見つけにくかった理由ごと直しました

このずれが見つからなかったのは、区切りに**生のNUL文字**がソースに直接書かれており、**gitとgrepがそのファイルをバイナリ扱いして、差分も検索も効かなくなっていた**ためです。同じ状態のファイルが4つありました（他の3つは動作に影響なし）。すべてエスケープ表記（`\u0000`）へ直し、**生のNULが混入したらテストで止まる**ようにしました。

## 0.22.9 - 2026-08-27

### 「重複をまとめる」の2つの不具合を直しました

文書の点検中に見つけたものです。どちらも0.22.7の「別人に分ける」に関わります。

**同じ名前のレコードが、まとめられませんでした。** 「同じ呼び名が両方に登録されています」という候補は、まさに同名レコードの組です（実データで「お母さん」が4件に分裂したことがあります）。ところが選んだ組を名前で引き当てていたため、同名だと同じ1件を2回つかんでしまい、「同じ人物どうしはまとめられません」と必ず断られていました。組をidで持つように直し、同名の組は選択肢にidを添えて見分けられるようにしました。

**まとめると、「別人だ」と決めた記録が消えることがありました。** 吸収される側が持っていた記録を引き継いでいなかったため、まとめた直後の抽出で、分けたはずの別名が戻る道がありました。両方の記録を重複なく引き継ぎ、互いを指す分だけを消すようにしました。

### 文書の記載漏れを直しました

- プロンプト設計書で、**実装していない・実装と違う8つの節**（P-01・P-04b・P-05・P-13・P-14・P-15・P-16・P-17）に実装状況を明記しました。P-04b（統合）とP-05（場所）はAIではなくコードで行っており、書かれていたプロンプトは使っていません
- 設計書6.5.8に、**誤って分けたときに元へ戻す手順**を書きました（分けた組は「重複をまとめる」の候補に出ないため、戻し方が文書のどこにも無い状態でした）

## 0.22.8 - 2026-08-27

### 手元のAIと、有料のAIの見分け方を直しました

**LM Studio（手元・無料）に「課金対象トークン量の目安」と出ていました。** 課金されないのに、です。「無料かどうか」をAIの名前で判定しており、**Ollama という名前のときだけ無料**と扱っていたためです。

- **無料かどうかは、そのAIが課金されるかで判断します**（名前では判断しません）
- **観点を絞るかどうかは、モデルの大きさで判断します。** これまでは LM Studio の小さなモデルに、矛盾検知の観点が7つとも渡っていました。一度にたくさん見せると、かえって見落としが増えます
- **プロット逸脱検知の「働きません」という断りも、モデルの大きさで出します。** これまでは Ollama のときだけ出ていましたが、同じことは LM Studio でも、クラウドの小さなモデルでも起きます

**何を絞ったかは、実行前の確認と記録に出します。** 指摘の件数が減ったとき、理由が分かるようにするためです。

**処理済みの記録（キャッシュ）について。** 観点が変わると答えも変わるので、その組み合わせは処理し直します。**大きなモデル（Claude・ChatGPT・Gemini など）で処理済みのものは、そのまま使えます**（観点が変わらないため）。手元のOllamaでは一度だけ送り直しになりますが、無料です（19話で2分ほど）。

## 0.22.7 - 2026-08-27

### 1つにまとめられた人物を、別人に分けられるようにしました

「アジャン」と「アジャーノ」が同じ人物として1件にまとめられていました。まとめる操作はありましたが、**逆向きがありませんでした。**

設定資料パネルで人物を開くと、「取り下げる」のすぐ上に**「別の人物に分ける」**が出ます。別名を押すと、その呼び名が別の人物になります。

- **中身は移しません。** 紹介・役割・性格・登場話・変化の記録は、元の人物に残ります
  - 第5話に「アジャン」と「アジャーノ」の両方が出るように、**どの記述がどちらのものかは機械では決められない**ためです。当てずっぽうで振り分けると、書いた文章が別人へ移ってしまいます
- 新しい人物は**名前だけ**です。「設定資料を抽出」をもう一度実行すると、本文から中身が入ります
  - **前と同じAI・同じモデルなら、AIは呼ばれません**（数十秒で終わり、料金もかかりません）
- **次の抽出で、また1人にまとめ直されることはありません。** この2人は別人だと覚えます
- 敬称違いの呼び方（「アジャーノさん」）は一緒に移ります。「アジャン様」は元に残ります
- 元の記録は、書き換える前に控えを取ります（`.novelai-recovery`）
- 確認画面で、**元の記録を以後AIに書き換えさせないかどうか**を選べます

### 設定資料を直したとき、本文の色分けがすぐ変わるようになりました

設定資料パネルで保存しても、**本文の用語ハイライトは古いままでした**（VS Codeを開き直すまで変わりませんでした）。0.17.1 で入れた「名前を別名から選ぶ」も、直したのに画面が変わらないので効いていないように見えていました。

保存・取り下げ・分ける、いずれの場合も**その場で引き直します。**

## 0.22.6 - 2026-08-27

### AIへ何字送っているかを、記録するようにしました

0.22.5 で「機能の一覧を毎回8,111字送っていた」ことが直せたのは、**先に測ったから**でした。ほかの機能については、まだ誰も測っていません。

**AIへ1回送るごとに、送った量を1行ずつ残します。** 場所は作品フォルダーの `.aiwriter/logs/usage.md` です。

- 機能ごとの**送信量**と、そのうち**本文が何割か**（小さいほど見落としが増えます）
- **確保したコンテキスト長**と**実際の入力トークン**（近すぎると、入力が黙って切り捨てられます）
- **応答が途中で切れた回**と、**失敗した回**
- 矛盾検知では、**世界観・人物・場所・あらすじ**を分けて数えます（世界観だけ上限が無いためです）

**本文そのものは記録しません**（字数だけです）。この場所はGitHubへ送られません。残したくない場合は設定 `novelai.usageLog.enabled` を切ってください。

**まだ何も減らしていません。** 減らすかどうかは、この数字を見てから決めます。

## 0.22.5 - 2026-08-27

### AIへ毎回送っていた説明を、37%短くしました

「AIに相談」では、**問いかけ1回ごとに機能の一覧（8,111字）を毎回送っていました**。1回で送る量の半分を占め、**肝心の本文は19%しかありませんでした**（クラウドのAIでは、そのぶん毎回課金されます）。

- 操作の**名前は全部**残します（欠けると「その機能はありません」と嘘を答えるため）
- 説明は**1文目**（何ができるか）だけにしました
- **「〜ません」で終わる文は残します**——「原稿は書き換えません」「AIは使いません」は、消すとAIが逆を答えかねないためです

5,097字になり、**相談1回で送る量が 15,791字 → 12,777字**になりました。答えられることは変わりません。

## 0.22.4 - 2026-08-27

### 「書く」面でも、設定資料の用語に色が付くようになりました

これまで用語の色分けは「読む」面だけでした。**打ちながらでも、登場人物・場所・能力・組織が見分けられます。**

- 文字の色ではなく、**うすい背景**で示します（変換中の文字と見分けが付かなくなるのを避けるためです）
- **打っている間は消えます。** 本文が変わると色の位置がずれるので、新しい位置が届くまで出しません（手を止めるとすぐ戻ります）
- 「読む」「並べる」では、これまでどおり文字に色が付きます

### 「書く」面の右クリックからも、設定資料を開けます

**用語の上で右クリック**すると、その名前と「設定資料を見る」が出ます。押すと**右側に設定資料パネル**が開きます（「読む」面と同じです）。

### 再変換（選んだ文字をもう一度変換する）について

**入れられませんでした。** Windowsの再変換はブラウザの上では使えず（Chromiumが対応していません）、VS Code本体の編集画面でも同じです。拡張機能から足せる類のものではありませんでした。

## 0.22.3 - 2026-08-27

### 横書きで開く入口を足しました

VS Code の「エディターを再度開く」に並ぶ名前を **「原稿（縦書）」「原稿（横書）」** の2つにしました（これまでは「原稿（縦書き）」1つでした）。

- **「原稿（横書）」で開くと、必ず横書き**で出ます。その原稿を前に縦書きで見ていても、選んだほうが優先されます
- 開いたあとに画面のボタンで切り替えられるのは、これまでどおりです

### 「最新話を書く」ボタンを足しました

原稿の画面の**下段**に置きました。

| いまの状態 | 押すとどうなるか |
|---|---|
| いちばん新しい話が**まだ白紙** | それを開きます |
| いちばん新しい話に**本文がある** | **次の話を新しく作って**開きます |
| 話が1つも無い | 第1話を作って開きます |

新しく作る話のファイル名は、「新規話数ファイルを追加」と同じ決まりです（桁数・拡張子の設定に従います）。**開くのは、いま使っている向きと同じ入口**です。

## 0.22.2 - 2026-08-27

### F5（拡張機能開発ホスト）で、開発用の道具が出るようにしました

**F5が本番ビルドを走らせていたため、「テスト中 → 確認を回す（開発用） → 実機確認を回す」が出ていませんでした。** 本番ビルドでは開発用の道具を落としているためです。

F5は開発ビルド（`npm run build:dev`）を使うようにしました。**配布物に入らないことは、これまでどおりです。**

（配布版には関わりのない直しです。）

## 0.22.1 - 2026-08-27

### 原稿の書体を、画面から選べるようにしました

**縦書きの原稿の画面に「書体」ボタン**を足しました（「＋」「ー」の隣）。押すと、本文に使う書体を選べます。

| 種別 | 選べるもの |
|---|---|
| 既定 | 明朝を自動で選ぶ |
| 明朝 | 游明朝／ヒラギノ明朝／ＭＳ 明朝／Noto Serif JP／BIZ UD明朝 |
| ゴシック | 游ゴシック／メイリオ／ヒラギノ角ゴシック／BIZ UDゴシック／Noto Sans JP |
| 等幅 | VS Codeの編集用フォント |

- **この端末に入っているかを調べて並べます。** 入っていないものには「この端末には入っていません」と添えます（**消しません**。なぜ選べないのか分からなくなるためです）
- 選んだ書体は**すべての原稿に効きます**。1話ごとに選び直す必要はありません
- **選ぶとすぐ変わります。** 開き直す必要はありません

設定 `novelai.manuscriptEditor.fontFamily` は今までどおりで、**手で書いた書体もそのまま効きます**。これまではこの設定を直しても開き直すまで効きませんでしたが、**その場で反映する**ようにしました。

## 0.22.0 - 2026-08-26

**配布版です。** 前回のVSIX（0.20.0）から先に入った直しがまとめて届きます。

| 版 | 何が変わったか |
|---|---|
| 0.21.1 | 「テスト中」は、配布版では**件数だけ**を出します |
| 0.21.0 | 操作メニューに**「テスト中」**を新設しました（まだ実機で確かめていない機能） |
| 0.20.4 | 「動作を診断」を、手元のVS Codeのメニューから隠しました |
| 0.20.3 | 作品一覧で、**どれが未同期か**が分かるようにしました（記録待ち・送信待ち） |
| 0.20.2 | 別のPCと**分かれてしまったとき、合わせられる**ようになりました |
| 0.20.1 | 「作品をすべて同期」が、記録するものが無いときに止まっていたのを直しました |

**入れ直し方**：`code --install-extension .\novel-ai-assistant-0.22.0.vsix --force`

## 0.21.1 - 2026-08-26

### 「テスト中」は、配布版では件数だけを出します

確認リストの項目には、**作品名のような外へ出すつもりのない言葉**が入ります（「じいちゃんの自分史で、文語体を…」）。配布する形にそのまま入れないようにしました。

| | 配布版 | 開発時（F5） |
|---|---|---|
| 「テスト中」の分類と、残り件数 | 出ます | 出ます |
| 何を確かめるのかの**文章** | 出ません | 出ます |

何を確かめるのかは、確認リスト（`docs/実機確認リスト.md`）と、開発時の「実機確認を回す」でご覧ください。

## 0.21.0 - 2026-08-26

### 操作メニューに「テスト中」を新設しました

**まだ実機で確かめていない機能**を、いちばん下の「テスト中」にまとめました。

    ■ テスト中
      ▸ 作品管理
          作品をすべて同期        残り16
          分かれた分を合わせる     残り11
      ▸ 執筆AI支援
          縦書きで開く            残り45
          矛盾を検知              残り18

- **並びは元のメニューと同じ**です（作品管理・執筆AI支援…）。探し慣れた場所と同じ順で並びます
- **押すと、その機能をそのまま実行します。** 元のメニューまで探しに行かなくて済みます
- 右の数字は、まだ確かめていない項目の数です
- カーソルを置くと、**何を確かめるのか**が読めます
- **確かめ終わって残りが尽きれば、この分類は消えます**

中身は `docs/実機確認リスト.md` から自動で作っています。押す操作の無いもの（見るだけ・AIの鍵が要るもの）はここから辿れないので、その件数は分類のカーソル説明に出しています。

## 0.20.4 - 2026-08-26

### 「動作を診断」を、手元のVS Codeのメニューから隠しました

作者の指摘：「操作メニューのヘルプの動作を診断ってまだ使うでしょうか？」

**ブラウザ版で保存できないときの切り分けに作ったもので、手元では出番がありません。** 操作メニュー（ヘルプ）に出すのは**ブラウザ版だけ**にしました。

**なくしてはいません。** 手元で切り分けが要るときは、コマンドパレット（`Ctrl+Shift+P`）で「動作を診断」と打てば呼べます。

## 0.20.3 - 2026-08-26

### 作品一覧で、どれが未同期かが分かるようにしました

作者の指摘：「未同期の作品がわかりません。横に数字をだせませんか？」

これまでは `↓3 ↑2` という矢印で出していました。**2つ足りませんでした。**

1. **記録していない変更が数に入っていませんでした。** 書いたまま記録も送信もしていない作品は、**印が何も付かず同期済みに見えていました**
2. **矢印が読み解けませんでした。** 印は出ていたのに伝わっていませんでした

**言葉と数字にしました。**

    いじめられっ子　　　　　12ファイル / 純41,218字 / 記録待ち2・送信待ち6
    短編　　　　　　　　　　 6ファイル / 純25,107字 / 送信待ち5

| 印 | 意味 |
|---|---|
| 記録待ち | 書いたまま、まだ履歴に残していない |
| 送信待ち | 記録したが、まだGitHubへ出していない |
| 受け取り | 別の環境で書かれた分。まだ取り込んでいない |
| 未送信 | まだ一度もGitHubへ送っていない |

**数は、その作品のぶんだけです。** 書庫（1つの置き場に複数の作品）では、置き場ぜんぶの数を出すと**全部の行に同じ数字が並び**、どの作品を送ればよいのか分かりません。実際に11作品すべてに「送信待ち13」と出ました。

**ただし、送るのも取り込むのも置き場ごとです。** 1つ送れば、同じ置き場の作品はまとめて出ていきます。作品名にカーソルを置くと、置き場ぜんぶの数も読めます。

## 0.20.2 - 2026-08-26

### 別のPCと分かれてしまったとき、合わせられるようになりました

**「作品管理 → GitHubで作品管理 → 分かれた分を合わせる」。**

別のPCとこちらの両方で書き進めると、履歴が分かれます。これまでは取り込みが止まり、「Gitのクライアントで見比べてから解決してください」としか出せませんでした。**そこが行き止まりでした。**

- **同じファイルが両方で書き換えられていないかを、先に調べます。** 調べるだけで、原稿には触れません
- 書き換えられていなければ、**そのまま合わせます**
- **原稿が両方で書き換えられていたときは、合わせずに手を引きます。** どちらを残すかは、書いたご本人にしか分かりません（そのときは、どのファイルかを一覧で出します）
- 執筆量の記録など**拡張機能が自動で書くファイル**だけが食い違っているときは、この端末の側を残して合わせます

**「名前が重なる」と「衝突する」は別です。** 実際にあった分かれ方では、名前が重なった6件のうち5件は**両方のPCで同じ原稿を取り込んだ**もので、中身まで同じでした。名前で見ていたら、合わせられる分まで行き止まりになっていました。

安全のために、次のことをします。

| | |
|---|---|
| 合わせる前 | 未記録の変更を記録します（書きかけを失わせないため） |
| 合わせる前 | **退避の枝を作ります**（あとから戻せます） |
| 合わせたあと | **競合マーカーが残っていないか**確かめます |
| 合わせたあと | **触っていない原稿が1バイトも変わっていないか**確かめます |
| 落ちたとき | **元に戻します。** 原稿は元のままです |

**GitHubへは送信しません。** 送信は「同期」から改めて行ってください。

取り込みが分岐で止まったときは、その場に「分かれた分を合わせる」のボタンが出ます。

ブラウザ版では使えません（gitコマンドを直に打つためです）。

## 0.20.1 - 2026-08-26

### 「作品をすべて同期」が、記録するものが無いときに止まっていたのを直しました

**「記録できませんでした: … nothing to commit, working tree clean」と出て止まっていました。**

gitは「記録するものがありません」を**失敗として返します**。それをそのまま失敗として扱っていました。

記録する件数を数えるのは**確認の画面を出す前**で、実際に記録するのは**押したあと**です。その間に、別の窓や前回の実行、あるいはご自身の「変更を記録する」が先に記録すれば、記録するものは無くなります。**それは失敗ではありません。**

**本当の実害は、その先が飛んでいたことでした。** 記録に失敗すると、その置き場はそこで打ち切る作りでした。**取り込みと送信までまとめて飛んでいました。**

- 記録するものが無いときは、そのまま**取り込みと送信へ進みます**
- 記録していないので、報告では「記録した」と数えません
- 判定は**gitの表示ではなく状態で行います**（表示は環境の言語で変わるため）

同じ問題が3か所にありました。**編集部へ渡す**処理と、**過去の版に戻す**処理も直っています（後者は、残すものが無いだけで復元そのものを中止していました）。

> **分岐しているときは、まだ同期できません。** 別のPCとこちらの両方で書き進めていると、取り込みは「どちらを残すか自動で決められない」として止まります。この置き場（novel）がその状態です。道筋は別に用意します。

## 0.20.0 - 2026-08-26

**配布版です。** 前回のVSIX（0.18.2、2026-08-23）から先に入った直しがまとめて届きます。**中身の新しいものはありません**（0.18.3〜0.19.7 をそのまま束ねたものです）。

| 版 | 何が変わったか |
|---|---|
| 0.19.7 | 設定資料の**紹介を80字**にし、作中の変化に**関与度**を付けました |
| 0.19.6 | **作品をすべて同期**できるようにしました |
| 0.19.5 | 同期のたびに再抽出を促すのと、**同期しても1件残る**のを直しました（裏では取り込みも塞がれていました） |
| 0.19.4 | 縦書きで**改行すると空行が入る**のを直しました |
| 0.19.3 | 縦書きの傍線を行の右にしました（**変換中の線には効きません**） |
| 0.19.2 | 縦書きの画面で**日本語入力を壊していた**のを直しました |
| 0.19.1 | 設定資料の**名前を変える道**を、見えるようにしました |
| 0.19.0 | **縦書きの原稿エディタ**を作りました |
| 0.18.3 | 説明書を、いまの中身に合わせました |

**入れ直し方**：`code --install-extension .\novel-ai-assistant-0.20.0.vsix --force`

## 0.19.7 - 2026-08-26

### 設定資料の「紹介」を80字までにしました

一覧で名前の下に出る短い紹介の上限を、**60字から80字**へ広げました。60字では「何者か」を書くだけで尽きてしまい、**作中で変わったこと**を書く余地がありませんでした。

### 作中の変化に「関与度」を付けました

設定資料には「新人職員（第1〜3話）→ 課長（第7、8話）」のような**作中での変化**が記録されています。ここに**0〜100の点**を付けました。**紹介に書くのは、点の高い変化だけ**です。

広げた20字を何に使うかが問題でした。変化の記録には「課長になった」も「髪を切った」も同じ形で並んでいます。区別が付かないまま渡すと、AIは**書きやすいほう（外見）から埋めます**。

| 点 | 段階 | 紹介での扱い |
|---|---|---|
| 60以上 | 高 | 紹介に書きます |
| 35〜59 | 中 | 字数が余ったときだけ書きます |
| 34以下 | 低 | 紹介には書きません |

点の付き方は次のとおりです。

| | 点 | なぜ |
|---|---|---|
| 役割が変わった | 55 | 立場が変われば、その人物の立ち位置そのものが変わるため |
| 性別・所属が変わった | 50 | この作品には転生・転性があり、起きたときは筋の中心にあるため |
| 性格が変わった | 35 | 物語の芯ですが、AIの言い換えでも動くため |
| 外見が変わった | 20 | 髪を切っても、多くの場合は話の筋が動かないため |
| 紹介・読み仮名が変わった | 0 | 紹介に「紹介が変わった」とは書けません。読み仮名が割れるのはAIの読み違いです |
| **作者が確定させた／補足を書いた** | **+20** | 作者がその変化を見て判断しているため。機械の重み付けより優先します |
| 3回以上変わっている | +5〜10 | 何度も変わる項目は、その人物を語るうえで外せないため |
| 変化した後の姿が2話以上続いている | +10 | 1話きりの変化は、変装や一時的な立場かもしれないため |
| いちばん新しい登場話で変わったばかり | +5 | まだ2話ぶん書かれていないだけで、それが今の姿のため |

**点は拡張機能が計算します。AIには測らせません。** AIに任せると、同じ人物でも押すたびに紹介の中身が変わります。

**点の低い変化も、資料からは消えません。** 設定資料集にも、AIへ渡す材料にも今までどおり全部載ります。**紹介に書かないだけ**です。相談で「髪を切った場面」を聞かれたときに答えられなくなるためです。

設定資料パネルの参考情報にも同じ数字を出しました（「変化（role）: 新人職員（第1〜3話）→ 課長（第7、8話）［関与度 65（高）］」）。紹介に何が書かれ、何が書かれなかったのかを、その場で確かめられます。

> **次に設定資料を抽出するとき、キャッシュが効きません。** 紹介の字数はAIへの指示に埋め込んでいるため、変えたときは版を上げる決まりです（上げないと、古い60字で書かれた紹介がキャッシュから返ります）。**全チャンクを読み直す**ので、クラウドAIをお使いの場合は1回ぶんの料金がかかります。

## 0.19.6 - 2026-08-24

### 作品をすべて同期できるようにしました

**「作品管理 → GitHubで作品管理 → 作品をすべて同期」。** 登録しているすべての作品を、ひと息で同期します。

- **置き場（リポジトリ）ごとにまとめます。** 1つのリポジトリに複数の作品を置いている場合（書庫）でも、同じ置き場を何度も処理しません
- **記録 → 取り込み → 送信**の順に行います。記録が先なのは、未記録の変更があると取り込みを行わない決まりのためです
- **何が起きるかを一覧で見せてから、1回だけ確認します。** 置き場ごとに訊くと、読まずに押すようになるためです
- 記録の説明（コミットメッセージ）は日付から自動で付けます
- **1か所が通らなくても、残りは続けます。** 最後に、通らなかった置き場とその理由をまとめて出します

次のものには触りません。

| 状態 | どうするか |
|---|---|
| 競合が解決していない | 何もしません（競合の印ごと履歴に入ってしまうため） |
| Gitで管理していない | 何もしません |
| 送り先が未設定 | 記録だけします |
| 記録する人の名前が未設定 | 飛ばします（ここで訊くと全体が止まるため） |

ブラウザ版では使えません（gitコマンドを直に打つためです）。

## 0.19.5 - 2026-08-24

### 同期のたびに「設定資料を抽出しませんか」と言われるのを直しました

**gitは、ファイルの更新時刻を保存しません。** 取り込み（pull）で書き直されたファイルの時刻は「取り込んだ今」になります。**中身が1文字も変わっていなくてもそうなります。**

これまでは更新時刻で「抽出したあとに書いた話」を数えていたため、**同期するたびに全話が「まだ取り込んでいない」ことになっていました。**

- **取り込んだ話を、中身で覚えるようにしました**（`.aiwriter/extracted.json`）。同期して時刻が変わっても、**中身が同じなら抽出済みのまま**です
- **この記録も同期されます。** 別のPCで抽出したなら、こちらでも抽出済みとして扱います
- モデルを変えても、抽出済みの記録は保たれます

（AIが勝手に動いていたわけではありません。抽出は操作したときだけ走ります。ただ、押せば本当に全話ぶん走るところでした。）

### 同期しても常に1件残るのを直しました

**正体は執筆量の記録**（`.aiwriter/stats/`）でした。保存のたびに拡張機能が書き換えるので、記録して送信した直後からまた「1件の変更」に戻っていました。

- **変更として数えないようにしました。** 記録（コミット）には今までどおり入るので、複数のPCで書いた量の合算は変わりません
- **競合しているときは数えます**（見逃せないため）

**これは表示だけの問題ではありませんでした。** 取り込みは未コミットの変更があると実行しない決まりのため、**保存するたびに取り込めなくなっていました。** これも直っています。

## 0.19.4 - 2026-08-24

### 縦書きの画面で、改行すると空行が入っていたのを直しました

**打った内容を原稿へ渡す順番が、追い越されていました。**

画面は打たれた本文をまるごと送り、受け取った側が「いまの原稿」と見比べて変わった1か所だけを当てます。**この見比べが終わる前に次の便が届くと、どちらも同じ「いまの原稿」を見ている**ので、同じ改行が2回当たります。

打つのが速いほど起きやすく、**変換の確定と次の打鍵が重なる日本語入力では、ふつうに起きていました。**

- **順番待ちの窓口を1つ置きました。** 当て終わるまで次を当てません
- 待っている間に届いたものは、**最後の1つに畳みます**（途中の状態を順に当てても、行き着く先は同じ本文です）
- **確定した文字が、確定の合図より後に入る**組み合わせ（Windowsの日本語入力）にも合わせました

**0.19.2 で直したのは半分でした。** あのときは画面の側だけを直し、受け取る側で同じことが起きるとは考えていませんでした。

### 左端の行を消したときのスクロール

上の2件を直したことで、**打っている最中に本文を入れ直すことがなくなりました。** 多くはこれで収まるはずです。あわせて、外から書き換えが届いて入れ直すときは、**見ている位置も戻す**ようにしました。

縦書きでは行が右から左へ並ぶため、左端がいちばん新しい行になります。**まだ直りきらないようでしたらお知らせください。**

### 変換中の傍線について

0.19.3 で「行の右に引く」指定を入れましたが、**変換中の線は左のままでした。** あの線を引いているのは日本語入力の層で、本文の下線とは別の道で描かれます。**こちらから動かす手立てがありません。** 確定すれば消える線で、原稿には残りません。

## 0.19.3 - 2026-08-24

### 縦書きのとき、変換中の傍線を行の右に引くようにしました

縦書きの日本語では、傍線（下線）は行の右側に引きます。**変換中に日本語入力が引く線が左に出ていると、隣の行に付いているように見えます。**

横書きのときは、これまでどおりです（左右の別がないため、この指定は使われません）。

## 0.19.2 - 2026-08-24

### 縦書きの画面で、日本語入力が壊れていたのを直しました

**入力中にカーソルが飛ぶ、文字が二重に入る、変換が途中で止まる——原因は1つでした。**

打った本文が原稿へ入ると、その原稿が画面へ送り返されます。**それを変換の最中に入れ直していました。** 変換中の入力欄を書き換えると、日本語入力はそこで壊れます。3つの症状は、どれもこれで説明が付きます。

- **変換中は、打っている面に一切触らないようにしました**
- **自分が打った内容が返ってきただけのときも、触りません**（カーソルが動かなくなります）
- **打った内容を、ためずにその場で原稿へ渡すようにしました。** これまでは0.2秒ためており、**打った直後に Ctrl+S を押すと最後の数文字が保存されない**状態でした
- 打っている間は、見えていない「読む」面を組み立てないようにしました（4万字の作品で画面がつかえ、それも変換が止まったように感じます）

### 打ちながら、組み上がりを見られるようにしました

**「並べる」ボタンを足しました。** 左で打ち、右にルビ・傍点・用語の色分けが出ます。**縦書きのまま並べられます。**

組み上がりの側は、**カーソルのある行を追いかけます。**

打つ面はこれまでどおりの入力欄なので、**日本語入力は何の細工もなく効きます。** ルビを出したまま打つ画面にしなかったのは、日本語入力のほかにもう1つ理由があります——**組み上がったルビを含む範囲を消すと、「灯あかり」のように読み仮名が地の文に混ざって原稿が壊れる**ためです。

## 0.19.1 - 2026-08-24

### 設定資料の名前を、書き換えられると分かるようにしました

**名前は前から書き換えられましたが、その道が見えていませんでした。**

- **別名を、押せる札にしました。** これまでは入力欄に候補を仕込んでいましたが、**画面には何の印も出ていませんでした**（打ち始めるまで候補が現れないため、「ドロップダウンが出ない」ように見えていました）。いまは名前の欄の下に別名が並び、**押すとその名前が入ります**
- **別名が無い人物でも、名前は直接書き換えられます。** そのことを欄の下に書きました
- **保存ボタンが画面の外にありました。** 人物の項目は10以上あり、名前を直しても保存ボタンはずっと下です。**変更があるときは、保存の帯が画面の下に貼り付くようにしました**
- **書き換えた項目に「（変更あり）」と出ます。** 何が保存されるのかが分かります
- 帯には「変更があります。「保存」を押すまで反映されません。」と出ます

別名を選んで保存すると、**いまの名前は別名へ移ります**（消えません）。本文の照合や用語ハイライト、IME辞書がその呼び方を拾えなくなるためです。

## 0.19.0 - 2026-08-23

### 縦書きで読み書きできる画面を作りました

**VS Code の新しいMarkdown編集画面では、この拡張機能の機能がどれも効きません。** 用語の色分けも、右クリックからの設定資料も、ルビの表示も出ません。あちらは拡張機能から手を出せる作りになっていないため、**読み書きする画面そのものを用意しました。**

操作メニューの「執筆AI支援 → その他支援 → **縦書きで開く**」、または本文の右クリックから開きます。

- **縦書きと横書きを、ボタン1つで切り替えられます。** 一度切り替えると、その原稿では次からもその向きで開きます
- **「書く」と「読む」を切り替えられます**
  - **書く**：ふつうに打てます。ルビは `{漢字|かんじ}` のまま見えます
  - **読む**：ルビが振り仮名として、傍点が圏点として出ます。登場人物・場所・能力・組織が色分けされ、**押すとその設定資料が開きます**
- **右クリックから、ルビ・傍点・投稿サイト用のコピー・AIへの相談**ができます
- 書体は明朝体です（設定 `novelai.manuscriptEditor.fontFamily` で変えられます）。文字の大きさは画面の＋ーで変わります
- 文字数が画面の右上に出ます（数え方はほかの画面と同じです）

**原稿の保存は VS Code に任せています。** 保存も、元に戻す（Ctrl+Z）も、外部ツールで書き換えられたときの読み直しも、ふつうにファイルを開いたときとまったく同じです。文字コードと改行もそのままです。

**この画面が既定になることはありません。** `.md` を開いてもふつうのエディタのままです。縦書きで見たいときだけ開いてください。

### 右クリックから、ルビと傍点を入れられるようにしました

**ふつうのエディタの右クリックに出ていませんでした。** 操作メニューからしか使えず、書いている流れの中では遠すぎました。いまは本文を選んで右クリックすると、「ルビを振る」「傍点を付ける」「投稿サイト用に変換してコピー」「縦書きで開く」が出ます。

### 確かめていないこと

**この画面はまだ1度も表示していません。** とくに**縦書きで日本語を打てるか（IMEの効き方）**は、実際に打ってみるまで分かりません。**まずは作品のコピーでお試しください。**

## 0.18.3 - 2026-08-23

### 説明書を、いまの中身に合わせました

**READMEに書かれていない機能がいくつもありました。** 使える機能が説明書から抜けていると、そこにあると分からないままになります。

- **書庫**（1つのリポジトリに複数の作品を置く）の説明を足しました。まとめ直す操作と、**Gitの履歴が引き継がれない**ことも書いてあります
- **ルビと傍点**の説明を足しました。入れ方、投稿サイト4種の記法への変換、設定資料の読み仮名から一括で振る操作まで
- **本文を .md にする**操作の説明を足しました。中身は変えず名前だけを変えること、投稿サイトの記法で入っていたルビ・傍点を読み替えることを書いています
- 「推敲・プロット逸脱検知・矛盾検知は未実装です」「ルビ機能は未実装です」という**古い記述を消しました**（どれも実装済みです）
- 代わりに、**LM Studio・ChatGPT・Claude は実際のサービスへ繋いで確かめていない**ことを制約として明記しました

設計書と引継ぎ書も、いまの実装に合わせて直しました（工程ごとの実装状況、AIの一覧、既知の課題）。**この版に機能の変更はありません。**

## 0.18.2 - 2026-08-23

### さくらのAI の書き方を、他とそろえました

AIを選ぶ画面で、さくらだけ**名前に無料枠の有無が入っておらず**、名前も長くなっていました。

| | 前 | 後 |
|---|---|---|
| 名前 | さくらのAI Engine（クラウド） | **さくらのAI（クラウド・無料枠あり）** |

名前だけ見ると有料に見えていました（説明のほうには書いてありましたが、並んだときに目に入るのは名前です）。

```
Ollama（ローカル）
LM Studio（ローカル）
Gemini（クラウド・無料枠あり）
さくらのAI（クラウド・無料枠あり）
ChatGPT（クラウド・有料）
Claude（クラウド・有料）
```

**接続に失敗したときのお知らせなどは「さくらのAI Engine」のままです。** そちらはサービスの正式な名前で、コントロールパネルを探すときや調べるときの手がかりになるためです。

## 0.18.1 - 2026-08-23

### LM Studio の書き方を Ollama とそろえました

AIを選ぶ画面で、Ollamaだけ書き方が違っていました。

| | 前 | 後 |
|---|---|---|
| 名前 | LM Studio（手元のPC） | **LM Studio（ローカル）** |
| 説明 | 無料・オフライン可。LM Studioで読み込んだモデルを使う | **無料・オフライン可。ローカル実行** |

同じ性質のものが並んでいるのに言葉が違うと、**違いがあるのかと考えさせてしまいます。**

## 0.18.0 - 2026-08-23

**配布版です。** 使えるAIに LM Studio を足しました。

### LM Studio を使えるようになりました

**AI設定**の一覧に「LM Studio（ローカル）」が出ます。**手元のPCで動くので、原稿は外へ出ません。無料で、インターネットに繋がっていなくても使えます**（Ollamaと同じ性質です）。

使う前に、LM Studio側で2つ済ませてください。

1. モデルを読み込む
2. 左の「Developer」からローカルサーバーを開始する

**APIキーは要りません**（手元のサーバーなので認証がありません）。

**コンテキスト長だけは自動で取れません。** LM Studioでモデルを読み込むときに指定した長さを `novelai.lmstudio.contextWindow`（既定8192）に入れてください。**実際より大きいと、入力が黙って切り捨てられます。**

つながらないときは、起動とサーバー開始の手順をお知らせします。

### まだ実機で確かめていません

**実際の LM Studio へ繋いだ確認はできていません。** OpenAI互換の口としてはごく標準的な使い方ですが、次の3つは実際に動かすまで分かりません。

- モデル一覧に出る名前が、そのまま指定して通るか
- JSON形式の強制を受けるか（受けない場合は自動で外して出し直します）
- 日本語の長い入力で、指定したコンテキスト長どおりに扱われるか

お試しいただいて、うまくいかない点があればお知らせください。

## 0.17.1 - 2026-08-23

### 設定資料の名前欄を、別名から選べるようにしました

名前の入力欄に**別名が候補として出ます**（人物・能力・組織・場所・世界観のすべて）。項目名も「名前（別名から選べます）」になります。

**選択肢ではなく候補です。** これまでどおり手で書くこともできます。選べるだけにすると、まだ別名に無い名前へ変えられなくなり、打ち間違いの修正すらできなくなるためです。

### 元の名前は消えません

別名から選んで保存すると、**元の名前は別名へ移ります。**

例：「マルキオ・イークェス」（別名：マルキオ、隊長）で「マルキオ」を選ぶと、
名前が「マルキオ」、別名が「マルキオ・イークェス、隊長」になります。

**元の名前を消すと、本文の照合から外れます。** 用語ハイライトもIME辞書も、その呼び方を拾わなくなってしまいます。

**打ち間違いの修正では、入れ替えは起きません。** 「太史」→「太志」のように別名に無い名前へ変えたときは、誤った名前が別名に残らないよう、元の名前を別名へ移しません。**元から別名にあったものを選んだときだけ**入れ替えます。

## 0.17.0 - 2026-08-23

**配布版です。** 矛盾検知を3段階にしました。

### あとで判明する事実と食い違う記述も、探すようになりました

0.16.4で「あとの話で明かされることを、前の話の矛盾にしない」ようにしました。ただ、**本当に両立しない場合は見つけたい**というご要望をいただきました。

**同じ材料を、逆向きにも使います。**

| どちらか | 例 | 扱い |
|---|---|---|
| **触れていないだけ** | 第4話で退学が判明。第3話はその件に触れていない | 矛盾ではありません |
| **両立しない** | 第4話で「3ヶ月前に退学」と判明。第3話（その後）の地の文に「今日も学校で授業を受けた」 | **矛盾として挙げます** |

**人物の発言は、嘘・思い違い・知らないことがありえます。** 地の文どうしが食い違う場合を優先し、発言の食い違いは確信度を低くして「発言者が知らない／偽っている可能性」を添えます。

### 検出した矛盾を、1件ずつ検証する工程を足しました

検出は本文を読みながら行うため、1回で何十行も見ます。**1件ずつを吟味する余裕がありません。** そこで検出のあと、**1件だけを見て「これは本当に矛盾か」を問い直します。**

却下の理由は決まった6つから選ばせます（まだ明かされていない／発言者の誤り／作中の変化／設定が古い／引用が本文と違う／そもそも食い違っていない）。

- **何件をどの理由で取り下げたか、完了時にお伝えします。** 内訳が見えないと、指摘が少ないのが「本当に無い」のか「消しすぎ」なのか分かりません
- **検証できなかったものは、そのまま残します。** 通信の失敗や応答の崩れで、本物の指摘を消さないためです
- 検証で分かったことは、指摘の補足に書き添えます

**AIの呼び出し回数は増えます**（本文1回 → 最大2回＋指摘1件につき1回）。0.16.3のまとめ送信で1回あたりの本文量が増えているぶん、全体の回数は抑えられています。

## 0.16.4 - 2026-08-23

### 矛盾検知が、あとの話で分かることを前の話の矛盾にしていました

**第4話で明かされる内容が、第3話の矛盾として出ていました**（作者の報告）。

原因は**設定資料が作品全体から作られている**ことです。第4話で「退学扱いになった」と明かされれば、資料の役割欄には「定時制高校生（退学扱い）」と入ります。それをそのまま第3話と突き合わせれば、**まだ明かされていない事実**と食い違って見えます。本文の誤りではなく、こちらが先を知りすぎていたのが原因です。

**その話の時点で分かっていることだけを突き合わせる**ようにしました。

- 資料には「その値が何話で出てきたか」が記録されています。それを使って**その話の時点の値へ巻き戻します**
- **その話までに登場していない人物・場所は、突き合わせません**
- 巻き戻して何も残らない記録は、材料として送りません
- AIにも「いま見ているのは第N話です。ここから先の話で明かされることを、この話の矛盾として挙げないこと」と伝えます
- **人物の身の上が先へ進むこと**（在学→退学、無職→就職など）は矛盾ではない、と明示しました

**記録の無い項目は落としません。** この仕組みより前に作られた資料や、作者が手で書いた項目には話数がありません。分からないものを消すと、作者が書いたものが黙って消えます。

**矛盾検知のプロンプトを変えたので、次の実行では作品全体を読み直します**（前回の結果は使い回せません）。

## 0.16.3 - 2026-08-23

### AIへ1回に送る量を「モデルによって可変」にしました（既定）

設定に **`Chunk Size Mode`** を足しました。既定は **「モデルによって可変」** で、使うAIのコンテキスト長から自動で決めます。**長く受けられるモデルほどまとめて送る**ので、呼び出し回数が減ります。

これまでも `Chunk Chars` を `0` にすると自動でしたが、設定画面には数値の欄しか出ず、`0` が「0文字」に見えました。選べる形にしました。文字数を自分で決めたい場合は「文字数を指定する」を選んでください。

### 同じ設定が、機能によって効いたり効かなかったりしていました

`Chunk Chars` を見ていたのは**誤字脱字検知と設定資料の抽出だけ**で、**推敲と矛盾検知は設定を無視していつも自動**でした。読むところを1つにまとめ、4つとも同じ答えを使うようにしました。

何を根拠に決めたか（モデルから／指定値）はログに残ります。

### 「矛盾を検知」もまとめて送るようになりました

これまで**話ごとに1回ずつ**AIを呼んでいました。矛盾検知は本文だけでなく**設定資料も一緒に送る**ため、1回あたりの指示がいちばん重い機能です。隣どうしをまとめることで、呼び出し回数と送信量（＝クラウドAIの料金）が下がります。

- 指摘の行番号は、**元のファイルと行へ戻します**（戻せないものは捨てます）
- AIへ渡す「いま見ている話」は「第1話〜第3話」のようにまとめて伝えます
- 応答が上限で切り詰められたときは、**話ごとに分け直して試し直します**

## 0.16.2 - 2026-08-23

### さくらのAI Engineに無料枠があることを明記しました

AIを選ぶ画面の説明が「国内のサービス。実行するたびに課金される」となっていましたが、**無料枠があります**（作者の確認）。「国内のサービス。無料枠あり。超えると課金される」に改めました。

**扱いは有料のままです。** 超えると課金されますし、**残りの枠はAPIから分からない**ため、実行前に処理量をお知らせします（Geminiも同じ扱いです。あちらにも無料枠があります）。使用量はさくらのクラウドのコントロールパネルでご確認ください。

あわせて、READMEと設計書のAI一覧にさくらのAI Engineが載っていなかったのを直しました（「対応しているAIは4つです」のまま、5つ目が抜けていました）。

## 0.16.1 - 2026-08-23

### 設定資料パネルに「ルビを追加」を足しました

一覧の下にあります。**設定資料に入っている読み仮名を、本文にルビとして振ります。** 人物だけでなく、能力・組織・場所の名前も対象です。

押すと、3つを順に選びます。

1. **どこに振るか** … すべての話／いま開いている話／話を選ぶ（複数選べます）
2. **同じ名前が何度も出てきたら** … 各話の最初の1回だけ／出てくるところすべて
3. **確認** … 話ごとに何件入るかを見てから決められます

### 振ってはいけないところには振りません

- **すでにルビや傍点になっているところ**（`{漢字|かんじ}`・`｜漢字《かんじ》`・`《《強調》》` など）には重ねません。二重になると投稿サイトで崩れます
- **長い名前を先に当てます。** 「ミナ」と「ミナモト」が両方登録されていても、「ミナモト」が「ミナ」＋「モト」に割れません
- **読み仮名の入っていないレコードは対象外です。** 別名（呼び方）も対象外です——別名には読み仮名の欄が無く、正しい読みが分からないためです
- ルビは Markdown（.md）でしか使えないため、`.txt` の話は選択肢に出しません（その場で「.md にする」へ進めます）

本文を書き換えるので、原稿を守る手順を通しています（読み込み時との照合、文字コードと改行の保持）。取り消したいときは、ファイルを開いて Ctrl+Z で戻せます。

## 0.16.0 - 2026-08-23

**配布版です。** 傍点を入れられるようになり、MD化のときに既存のルビ・傍点を取り込むようになりました。

### 傍点を付けられるようになりました

**執筆AI支援 → その他支援 → 「傍点を付ける」**（範囲を選んでから押してください）。

**「投稿サイト用に変換してコピー」は、傍点が入っているときだけ貼り付け先を訊きます。** 傍点はサイトによって書き方が違うためです。ルビはどのサイトでも同じなので、傍点が無ければこれまでどおり訊きません。

| 貼り付け先 | 傍点の出方 |
|---|---|
| カクヨム・ネオページ | `《《強調》》`（専用の記法） |
| 小説家になろう・アルファポリス | `｜強調《・・》`（記法が無いのでルビで代用） |

**ネオページはカクヨムと同じ記法です**（作者に確認。これまで「未確認」として保留していた項目です）。

投稿済みの原稿を持ち込んだときは、どちらの書き方も傍点として読み取ります。

### MD化のとき、中のルビ・傍点も直すようになりました

投稿サイトから持ってきた本文には、すでに `｜漢字《かんじ》` が入っています。**名前を .md に変えただけでは、プレビューでルビとして表示されず、「ルビを振る」の対象にもなりませんでした。**

MD化すると、中の記法もこの拡張機能の書き方（`{漢字|かんじ}` / `{{強調}}`）へ直します。何件直したかをお知らせします。

**ここは中身が変わります**ので、原稿を守る手順を通しています（読み込み時との照合、文字コードと改行の保持）。直せなかった場合はその旨をお伝えし、「投稿サイトのルビを取り込む」からやり直せます。

## 0.15.9 - 2026-08-23

### 本文を .md にする入口を、2か所に作りました

これまでは**「ルビを振る」を押したときにだけ現れる**救済の道でした。`.txt` でルビを使おうとした方に「ルビは .md でしか使えません」とお断りしたうえで、その場で変換を申し出る形です。

**MD化したい理由はルビだけではありません**（プレビューで読みたい、見出しを使いたい）。独立した入口を作りました。

- **作品一覧でファイルを右クリック → 「本文を .md にする」**（そのファイル1件）
- **執筆AI支援 → その他支援 → 「本文を .md にする」**（まとめて／開いている1件、を選べます）

**中身は1文字も変えません。名前だけを変えます。** 文字コードも改行も、書いた本文もそのままです。戻したくなったら、名前を .txt に戻すだけで元どおりです。同じ名前の .md がすでにあるものは、上書きせずに飛ばします。

まとめて変えるときは、件数を見せてから確認します。

## 0.15.8 - 2026-08-22

### 操作メニュー「校正・校閲」の並びを変えました

**「編集部からの提案を見る」をいちばん下、「校閲を始める／終える」をその上**に移しました（作者の指示）。

上に並ぶのは作者が1人で回す作業（誤字脱字・表記ゆれ・推敲・逸脱・矛盾）で、下の2つは相手のいる作業です。毎日通るのは上のほうなので、下に置いても埋もれません。

## 0.15.7 - 2026-08-22

### 書庫へまとめたあと、元のフォルダーを消す前にお知らせします

0.15.6で「書庫にまとめる」を入れたとき、**実行前の確認には「履歴は写りません」と書いていましたが、終わったあとの報告では「元のフォルダーはご自身で消してください」とだけ言っていました。**

履歴は写していないので、**元のフォルダーを消すと書き換えの記録がまるごと失われます。** 消してよいと言う前に、消すと何を失うかをお伝えするようにしました。

- 履歴を持っている作品を**名指しで挙げます**（「〇〇 は書き換えの記録を持っていますが、それは写していません」）
- 履歴を持つ作品が1つも無いときは、**この話を出しません**（Gitを使わずに書いている作品には関係がないためです）
- 実行前の確認でも、履歴を持つ作品を名指しで挙げるようにしました

## 0.15.6 - 2026-08-22

### 「作品集」を「書庫」と呼ぶことにしました

複数の作品を1つにまとめて置く場所の呼び名です。「作品集」は**作品を並べた1冊の本のようにも読める**ため改めました。置き場であって、作品そのものではありません。

画面の文言をすべて書き換えました（過去の更新履歴の記述はそのままにしてあります）。

### 別々に置いてある作品を、1つの書庫へまとめられるようになりました

**作品管理 → GitHubで作品管理 → 作品を書庫にまとめる**

すでに作品ごとにフォルダーを分けて管理している場合に、それらを1つの書庫へ寄せます。まとめたあと「GitHubに置く（はじめて）」を実行すれば、書庫まるごとを1つのリポジトリにできます。

**元のフォルダーは消しません。** 中身を写したうえで、作品一覧の指す先だけを新しい場所へ移します。見比べて納得できたら、元のフォルダーはご自身で消してください。

- **上書きは絶対にしません。** 書庫に同じ名前のフォルダーがあれば、その作品は移さずに理由をお伝えします
- **写したものを1ファイルずつ読み直して照合します。**「コピーした」と「同じものがある」は違うためです
- `.git` とキャッシュは写しません（写すとリポジトリが入れ子になり、書庫の側から見えなくなります）
- **書き換えの記録（履歴）は元のフォルダーに残ります。** 新しい書庫では、まとめた時点からの記録が始まります。古い版が要るときは元のフォルダーを開いてください

## 0.15.5 - 2026-08-22

### GitHubへ載せる入口を、作品管理の中に置きました

**「作品管理 → GitHubで作品管理 → GitHubに置く（はじめて）」** から操作できます。これまでは「拡張機能の設定 → セットアップを開始」の中にあり、作品管理の下を探しても見つかりませんでした。

### 複数の作品を1つのリポジトリに入れる形を、既定にしました

**これまでは、作品1つにつき1リポジトリしか作れませんでした。** 作品集の中の作品から実行すると、作品ごとにばらばらのリポジトリができてしまい、まとめる意図と逆になっていました。

いまは**同じフォルダーに並んでいる作品を、まとめて1つの置き場に入れます。** 別の環境へ移るとき、1回の取り寄せで全部そろいます。

- **分けることもできます。** すでに作品ごとに分けて管理している場合や、この作品だけを人に渡す場合は「この作品だけを置き場にする」を選べます
- **すでにGitHubにあるリポジトリなら、何も訊きません。** どこが置き場かは Git が知っているためです

### 一緒に送られる作品を、名前で出すようにしました

1つのリポジトリに複数の作品が入っていると、**1作品を選んで同期しても他の作品も一緒に送受信されます。** これは狙いどおりの動きですが、画面に作品名が1つしか出ていないと、そうは読めませんでした。

- はじめての送信では「入っている作品: いじめられっ子・教科書チート」を確認画面に出します
- 送信のときは「同じ置き場の『教科書チート』も一緒に扱われます。」を添えます

### まだできないこと

**すでに別々のリポジトリになっている作品を、1つへまとめ直すことはできません。** 作品フォルダーを動かす操作になるため、別に設計します。今回は「これから載せるとき」の既定を直したところまでです。

## 0.15.4 - 2026-08-22

### 他の検知を実行しても、それまでの作業が消えなくなりました

**誤字脱字を1件ずつ見ている途中で推敲を実行すると、それまでの作業が消えていました。** 適用済み・見送り済みの判断も、まだ見ていない指摘も、すべて失われていました。

いまは**分類ごとに分けて持ちます。** パネルの上にタブが出て、誤字脱字・推敲・表記ゆれ・矛盾・プロット逸脱・設定資料の更新を、残りの件数を見ながら行き来できます。

- **同じ分類をもう一度実行しても、済んだ判断は残ります。** 話を絞って2回に分けて実行しても、1回目の結果は消えません
- **0件だったときも、前の結果は消えません。** 「今回は見つからなかった」と「前の結果を捨てる」は別のことです
- **「一覧を空にする」を用意しました。** 足していく作りなので、片付ける口が要ります。まだ見ていないものが何件あるかを添えて確認します（本文は書き換わりません）
- パネルのタブに出る未処理の数は**すべての分類の合計**になり、説明に「誤字脱字 3件 / 推敲 1件」と内訳が出ます

### 推敲の指摘で、同じ文が2度並んでいたのを直しました

**適用ボタンのすぐ上の文が、その上の差分と同じ文になっていました。** 誤字脱字は直す語が行の一部なので、行まるごとを添えると前後が分かって役に立ちます。**推敲は一文まるごとが対象**なので、同じ文がもう一度並ぶだけでした。

空いたところに、**なぜ読みにくいのかを出すようにしました。** これまでは「冗長」の一語しか出ておらず、何と何の話なのか分かりませんでした。

- 「『まず』と『最初に』が同じ意味です」のように、**本文の語を挙げて**説明します
- 係り受けなら「『赤い』が『鞄』にも『少女』にも掛かって読めます」、同語反復なら繰り返しの回数、長文なら字数と読点の数を出します
- AIが説明を返さなかったときも、種類ごとの言葉が出ます（何も出ない、を作りません）

**推敲のプロンプトを変えたので、次の実行では作品全体を読み直します**（前回の結果は使い回せません）。

## 0.15.3 - 2026-08-22

### ブラウザ版でも「GitHubから作品を追加」が使えるようになりました

これまでブラウザ版では、この操作を押せなくして「アドレス欄を書き換えてください」とご案内していました。**行き止まりではありませんが、遠回りでした**——別のリポジトリを開くと、いま開いているものは閉じてしまいます。

いまは**開き直さずに登録できます。** 手元のVS Codeのように取り寄せる（`git clone`）のではなく、VS CodeがGitHubの中身を直に読む仕組みを使います。**作業としては同じこと**——別のリポジトリにある作品を、いまの画面から登録できます。

- リポジトリの指定は、**手元にあるものをそのまま貼れば通ります**（`nonahisa/HisasNovels` ／ GitHubのページのURL ／ `git clone` 用のURL ／ ブラウザのアドレス欄 ／ 診断結果に出る `vscode-vfs://github/…`）
- 場所が決まったあとは「フォルダから作品を追加」とまったく同じ道を通ります（作品集なら、中の作品を選べます）
- 読めなかったときは、**新しいタブで開く**か**いまの画面に足す**かを選べます（名前の間違い・非公開・GitHubへのサインイン切れは、どれも同じ「読めない」になるため、理由は当てにいきません）

## 0.15.2 - 2026-08-22

### 登録した作品が一覧に出ないことがあったのを直しました

ブラウザ版で作品を5件登録したところ、**作品フォルダーには設定ファイル（`.aiwriter/config.json`）ができているのに、作品一覧は空のまま**でした。原因は2つありました。

- **1件でも読めない作品があると、一覧全体が空になっていました。** 作品一覧は登録済みの作品を順に走査しますが、その1件が失敗すると一覧の作成ごと失敗し、VS Codeは「作品が0件」と見なして歓迎画面を出していました。**登録できていないのか、登録できたのに出せていないのかが、まったく区別できない見え方**でした。今は読めなかった作品も⚠付きで一覧に出し、開くと理由が読めます
- **`.gitignore` を整えられないと、登録そのものが失敗していました。** これは作業ファイルをGitに入れないための後始末で、作品を登録するのに要るものではありません。設定ファイルだけが作られて登録簿に入らない、という中途半端な状態の原因でした。今は登録を済ませたうえで、整えられなかったことだけをお知らせします

### 登録に失敗したとき、理由が読めるようになりました

これまでは作品名だけを並べていたため、次に何をすればよいか決まりませんでした。失敗の理由を画面に出し、「ログを表示」も選べるようにしました。

### 読み込めていないフォルダーを選べないようにしました

ブラウザ版では、GitHubの読み込みが終わっていない・失敗した状態でも、フォルダー名だけが一覧に並びます（エクスプローラーには「ワークスペース フォルダー (Canceled) を解決できません」と出ます）。この状態のフォルダーには⚠を付け、選んだ場合はページの再読み込み（F5）をご案内します。

### 動作診断に「登録済みの作品」を足しました

**ヘルプ → 動作を診断**で、登録簿の中身と、その1件ずつについて設定を読めるか・走査できるかが分かります。「登録できていない」のか「登録できたのに出せない」のかを見分けるためのものです。

## 0.15.1 - 2026-08-22

### 作品フォルダーに直接置いた話数を、作品として認識します

**複数作品の第1話が1つの一覧に並んでいました**（`novels` が328ファイル・996,040字の1作品として登録されていました）。

原因は、作品かどうかの判定が**「本文」フォルダか設定ファイルがあるか**しか見ていなかったことです。**「本文」フォルダを作らず、作品フォルダーへ直に `001.txt` を置く形**を見落としていました。そのため作品集の中の作品が1つも認識されず、作品集まるごとが1作品になっていました。

- 直下に話数ファイル（`001.txt`・`第1話 ○○.md`・`プロローグ.txt` など）があれば、**作品として認識します**
- 作品集そのものは作品と見なしません（`README.md` や `characters.json` は話数として読みません）

**すでに登録済みの作品集は、いったん登録を解除してから追加し直してください。** 作品ごとに選べるようになります。

## 0.15.0 - 2026-08-22

**配布版です。** ブラウザ版で作品を登録できるようになりました。

### 作品集の中の作品を、まとめて登録できるようになりました

**`HisasNovels` のような「作品を並べたフォルダー」が、1つの作品として扱われていました。** 診断で原因が分かりました——そのフォルダーの直下に `.aiwriter` の設定ファイルが残っていたためです（作品集の仕組みができる前に、全体を1作品として登録した名残です）。

これまでは「自分が作品なら、中は見ない」という作りだったので、**中の作品を登録する画面に進めませんでした。**

- **作品にも作品集にも見えるときは、どちらとして登録するかをお尋ねします。** 「中の◯件の作品を登録する」を先に出します
- ふつうの作品の判定は変えていません

### 「追加したのに何も起きない」を直しました

**登録は済んでいるのに、そのあとの文字数の集計で失敗すると、通知も一覧の更新も飛んでいました。** 押しても何も変わらないように見えて、次に開いたときだけ現れる、という分かりにくい壊れ方でした。

集計に失敗しても、登録したことをお伝えして一覧を更新します。

## 0.14.2 - 2026-08-22

### 診断でわかることを増やしました

0.14.1 の診断で、**ファイル操作はすべて動いている**ことがわかりました（フォルダーを作る・書く・読む・移す・消す、すべて○）。原因は別のところにあります。次の1回で突き止められるよう、診断に2つ足しました。

- **コマンドが登録されているか。** 拡張機能の起動が途中で止まると、画面は出るのにボタンだけが効かなくなります。この見分けが付きます
- **作品を探す道を、実際にたどってみせます。** 「フォルダから作品を追加」で何が起きるはずだったかを再現します（登録はしません）。作品が見つからない場合は、フォルダーを1つずつ見て「本文・設定・.aiwriter があるか」を表にします

## 0.14.1 - 2026-08-22

### 「動作を診断」を追加しました

**ブラウザ版で作品を追加できない**とのご報告を受けて作りました（0.14.0）。原因の候補が複数あり、どれも推測で書き換えてよい場所ではない（原稿を保存する仕組みの土台に関わる）ため、**まず何が起きているかを測る**ためのものです。

操作メニュー →「ヘルプ」→「**動作を診断**」

- いまの環境で、ファイルに何ができるかを実際に試して並べます（フォルダーを作る・書く・読む・一覧する・移す・消す）
- **原稿には触れません。** 使い捨ての場所で試して、終わったら消します
- 結果はMarkdownの文書として開くので、そのままお伝えいただけます

## 0.14.0 - 2026-08-21

**配布版です。** ブラウザのVSCode（vscode.dev）で、作品を登録して書けるようにしました。

### ブラウザで作品を登録できるようにしました

0.13.0 でブラウザから開けるようになりましたが、**作品を1つも登録できませんでした**（3つのボタンがいずれも進みませんでした）。

- **開いているフォルダーから作品を登録できます。** ブラウザにはフォルダーを選ぶ画面が無いので、いま開いているリポジトリから選ぶ形にしました。どこのリポジトリかも選択肢に出ます
- **場所の扱いに取りこぼしがありました。** ブラウザ上の作品の場所を覚えるとき、リポジトリの情報が落ちて別の場所を指していました（52か所）。手元のVS Codeでの動きは変わりません

### GitHubへの保存

- **「GitHubと同期」を押すと、保存の手順をお伝えして「ソース管理」を開きます。** ブラウザではこの拡張機能からGitHubへ送れないため（gitコマンドを起動できません）、VS Code本体の仕組みをお使いいただく形にしました
- ブラウザで使えない操作の案内を、**その場から取れる手**に書き直しました（別のリポジトリを開くにはアドレス欄を書き換える、過去の版はGitHubのサイトで見る、など）

### 直したこと

- ブラウザで「GitHubのセットアップ」を押すと、Ollamaの案内が出ていました

## 0.13.1 - 2026-08-21

### 説明文の直し

- 拡張機能の説明とREADMEに、**さくらのAI Engine** が使えることを書き足しました（対応していたのに、選べるAIの一覧から漏れていました）

### 開発用の文書の整理

使い勝手には関係しません。次に作業する人が現状を取り違えないよう、文書の古くなった記述を直しました。

## 0.13.0 - 2026-08-21

**配布版です。**0.12.0 から先に入った直しがまとめて届きます。

### ブラウザのVSCode対応（内部の作り替え）

**使い勝手は何も変わりません**（手元でお使いの場合）。vscode.dev / github.dev で実際にアクティブ化できるところまで進めました。

- 拡張機能が読み込まれた瞬間にNode専用の機能（git・Ollama・パッケージ導入）を巻き込んでいた箇所を、すべて「押したときだけ読み込む」形に直しました
- ブラウザ向けの束（`dist/browser-extension.js`）を作るようにしました
- 操作メニューで、ブラウザ版では使えない操作（GitHub同期・Ollama関連・編集部とのやり取りなど）が、押せない見た目になり理由が出るようにしました
- AI設定の選択肢から、ブラウザ版ではOllamaが出ないようにしました

**まだブラウザで実際に動かして確かめてはいません。** 今回のVSIXでは効果はありません（手元のVS Codeでの動きは変わりません）。

## 0.12.1 - 2026-08-21

### ブラウザのVSCode対応（内部の作り替え）

**使い勝手は何も変わりません**（手元でお使いの場合）。vscode.dev / github.dev で実際にアクティブ化できるところまで進めました。

- 拡張機能が読み込まれた瞬間にNode専用の機能（git・Ollama・パッケージ導入）を巻き込んでいた箇所を、すべて「押したときだけ読み込む」形に直しました
- GitHub同期の常時監視を、ブラウザでは何もしない代わりのものに差し替えられるようにしました
- ブラウザ向けの束（`dist/browser-extension.js`）を作るようにしました
- 操作メニューで、ブラウザ版では使えない操作（GitHub同期・Ollama関連・編集部とのやり取りなど）が、押せない見た目になり理由が出るようにしました
- AI設定の選択肢から、ブラウザ版ではOllamaが出ないようにしました（ローカルで動くAIのため）

**まだブラウザで実際に動かして確かめてはいません。** ここまでは、拡張機能を読み込む道筋にNode専用のコードが紛れ込んでいないかを機械的に確かめたところまでです。

## 0.12.0 - 2026-08-21

**配布版です。**0.11.0 から先に入った直しがまとめて届きます。

### ブラウザのVSCode対応（内部の作り替え、下ごしらえの続き）

**使い勝手は何も変わりません。** vscode.dev / github.dev で動く版を作るための下ごしらえです。

- ファイルの場所の扱いを、実際にブラウザ向けビルドで試して足りないところを直しました（端末IDの生成・`.gitignore`への追記・作品集の走査など）
- **Claudeを、公式SDKに頼らない形へ書き換えました。** ブラウザでもClaudeを使えるようにするための書き換えです。手元での使い勝手は変わりません。配線は公式SDKのソースを直接読んで確かめていますが、**実際のAnthropic APIへはまだ接続確認をしていません**（実キーが手元に無いため）。最初にお使いになるときは、簡単な操作から試すことをおすすめします
- 依存が7パッケージ減り、拡張機能自体も少し軽くなりました

**ブラウザではまだ動きません。** 常時監視の仕組み（GitHub同期の状態表示）とNode専用の機能（Ollama・git・パッケージ導入）が、まだ読み込みの時点でブラウザを止めてしまうためです。次の段階で対応します。

## 0.11.2 - 2026-08-21

### Claudeを、公式SDKに頼らない形へ書き換えました（内部の作り替え）

**使い勝手は何も変わりません。** ブラウザのVSCode対応の続きです。ブラウザではClaudeを使えなくする案もありましたが、**ブラウザでもClaudeを使えるようにする**ほうを選びました。

- OpenAI・Geminiと同じ、素の通信（fetch）で呼ぶ形に書き換えました。配線は公式SDKのソースを直接読んで確かめています
- **ブラウザから直に呼ぶと拒否される、という仕組みへの対応も入れました。** 手元では変わりません
- 使っていなかった機能（トークン数の実測）を1つ削除しました
- 依存が7パッケージ減り、拡張機能自体も少し軽くなりました

**まだ実際のAnthropic APIに繋いで確かめてはいません。** 最初にお使いになるときは、念のため簡単な操作から試すことをおすすめします。

## 0.11.1 - 2026-08-21

### ブラウザのVSCode対応（下ごしらえ、内部の作り替え）

**使い勝手は何も変わりません。** vscode.dev / github.dev で動く版を作るための下ごしらえの続きです。

- `path`（Node組み込み）への依存を、ブラウザ向けビルドだけ差し替えられるようにしました
- 端末IDの生成・`.gitignore` への追記・作品集の走査が、ブラウザでは使えない仕組み（`node:os`・`node:fs`）に直接触れないようにしました。手元での動きは変わりません
- 「誰が編集したか」を記録する仕組みが、関連の無いファイルまで巻き込んでいたのを直しました

実際にブラウザ向けにビルドを試し、**まだ足りないところを具体的に数えました**（詳しくは設計書5.8.4・5.8.5）。今回のVSIXでは効果はありません。

## 0.11.0 - 2026-08-21

**配布版です。**0.10.0 から先に入った直しと機能がまとめて届きます。

### 原稿を守るための直し（重要）

- **誤字脱字の適用で、本文が二重になる不具合を2度直しました。** AIが「直したい語」とその周りの文を取り違え、文まるごとの書き換えを修正案に入れてくることがありました。当てると重なる修正案を、適用の前に弾くようにしています（0.10.10・0.10.21）
- **押しても何も変わらない指摘を出さなくしました。** 修正案が原文のまま／Markdownの記号が混ざっている／対象より大幅に長い、の3つを検査で弾きます。実データ147件のうち画面に出るのは75件になりました（0.10.22）
- 適用した直しは「戻す」ことができます。書き戻す直前に本文を読み直し、修正案がまだそこにあるかを確かめます（0.10.12）

### 大きいもの

- **提案パネルで、違うところだけを塗るようにしました。** 推敲の指摘のような長い書き換えでも、実際に変わる文字だけに色が付きます。色はVS Codeの差分表示と同じものを使います（0.10.23）
- **対話でプロットを作れるようにしました。** まだ書けていない項目を、AIが1つずつ尋ねます。作者の中にあるものを引き出すためのもので、AIが筋書きを作ることはしません（0.10.20）
- **作品集（1つのリポジトリに複数の作品）を扱えるようにしました。** 取り寄せも同期も1回で全作品に効きます。編集者モードでは、渡り違えを防ぐため作品を1つしか登録できません（0.10.3）
- **編集部へ、作品を1つだけ渡せるようにしました。** その作品だけを入れた非公開リポジトリを作り、本文と設定資料を送ります。編集部の提案は取り込めますが、編集部が本文を書き換えることはありません（0.10.4）
- **誤字脱字・推敲が、隣どうしの話をまとめて送るようになりました。** 1話ずつでは指示のほうが本文より大きく、話数だけ同じ指示を送り直していました（0.10.8・0.10.9）
- **その作品の書き方（一人称・文語体かどうか・直さない語）を、AIへ先に伝えるようにしました。** 生まれる誤検出そのものを減らします（0.10.16）

### 使い勝手

- 提案タブに未処理の件数が出るようになりました（0.10.14）
- 本文の折り返しを既定で入にしました（0.10.13）
- 選択画面に「取りやめる」を出すようにしました。Escでも閉じられますが、それを知らないと出口が無いように見えるためです（0.10.15）
- GitHubへ初めて同期する際、VS Codeのアカウントを選ぶと、以後は聞き直されなくなりました（0.10.18）
- プロットが既定のエディターで開くようになりました（0.10.19）

### ブラウザのVSCode対応（下ごしらえ、未完成）

vscode.dev / github.dev で動く版を作るための下ごしらえを始めました。ハッシュ計算・ファイルの場所の扱いをブラウザでも動く形へ入れ替えていますが、**ブラウザ用のビルドと、実際のブラウザでの動作確認はまだできていません**（詳しくは設計書5.8）。今回のVSIXでは効果はありません。

## 0.10.24 - 2026-08-21

### ブラウザのVSCode（vscode.dev）対応の土台（内部の作り替え）

**使い勝手は何も変わりません。** ブラウザだけで動く版を作るための下ごしらえです。

- ハッシュの計算を、Nodeに頼らない実装へ入れ替えました。ブラウザ版のVSCodeには `node:crypto` が無く、代わりのWeb Cryptoは非同期でしか使えません。**原稿を守るハッシュ照合をそこに乗せ替えるのは危ない**ので、同期のまま動くものを自前で持ちました
  - 正しさは Node の実装と突き合わせて確かめています（境目の長さ・日本語・乱数300件で完全一致）
  - **キャッシュのキーも変わっていません。** 溜めたAIの応答はそのまま使えます
- ファイルの場所を、手元のファイルとブラウザ上の作品の両方で扱える形にし、実際に全体（324か所のファイル操作・191か所のファイルの場所指定）を差し替えました。ブラウザ上の作品は `vscode-vfs://github/...` という場所にあり、これまでの書き方のままでは正しく動きません
- 実行環境の判定を1か所にまとめました
- **ここまでは机上の置き換えです。** ブラウザ用のビルドや、実際のブラウザでの動作確認はまだできていません（詳しくは設計書5.8）


## 0.10.23 - 2026-08-21

### 提案パネルで、違うところだけを塗るようにしました

作者の指摘「差異のある部分をマーカーで色分けしてください」に対応しました。

これまでは、行まるごとに取り消し線を引いて赤くし、直したあとの行をまるごと緑で出していました。誤字脱字は直す語が短いので気になりませんでしたが、**推敲の指摘は文まるごとが対象になる**ため、全部が赤で消えて全部が緑で出て、どこが変わるのか目で追えませんでした。

- **本文の直しは、変わる文字だけを塗ります。** 「呪詛だらけの学校は視界が悪いので、引き寄せて視界を確保する。」なら、塗られるのは消える「視界を」の3文字だけです
- **設定資料の更新も、変わるひと言だけを塗ります。** 紹介文は長いので、前後をまるごと並べると読み比べになっていました。項目ごとに「現在／更新案」を上下に並べ、違うところに色を付けます
- 消える側には**取り消し線**も引きます。色を見分けにくくても、どちらが消えるか分かるようにするためです
- 色はVS Codeの差分表示と同じものを使うので、**お使いのテーマに合います**

まるごと書き換わる指摘では、これまでどおりまるごと塗ります。**たまたま同じ文字が一致しただけの箇所を「変わりません」と出すと、かえって読み違えるためです。**

矛盾の指摘（設定では／本文では）は、これまでどおりです。あちらは「これがこう変わる」ではなく「どちらが正しいかを作者が決める」ものなので、片方を消える側として塗ると意味が変わってしまいます。


## 0.10.22 - 2026-08-21

### 誤字脱字の「押しても何も変わらない指摘」を出さなくしました

作者の指摘「修正案が原文と同じ場合は表示しなくていいのではないでしょうか」に対応しました。実データ147件を測って、3つの検査を足しています。

- **修正案が原文のままのものを出しません。** AIが「直したい語」ではなく「その周りの文」を修正案に入れると、画面には同じ文が2つ並びます。押しても何も変わらないうえ、当てれば二重になります
  - 例：原文「相当なお金持ちらしく、恨みも」／対象「お金持ちらしく」／修正案「相当なお金持ちらしく、恨みも」
- **Markdownの記号が入った修正案を出しません。** 足した箇所を強調して返してくることがあり、当てると本文にアスタリスクが入ります
- **対象より大幅に長い修正案を出しません。** 誤字脱字の直しは語を1つ直すものです。文の書き換えを当てると、前後が二重に残ります
  - 実データで、他の検査を通った80件のうち伸びは75件が3文字以内でした。そこから7・12・18・23・26文字と飛び、**その5件すべてが本文を壊しました**。上限は5文字にしています

この結果、147件のうち画面に出るのは75件になりました。**消えた分はすべて、押しても意味が無いか、本文を壊すものです。**


## 0.10.21 - 2026-08-21

### 原稿が壊れる不具合を直しました（重要）

- **誤字脱字の適用で、また本文が二重になりました**（作者の報告、2度目）。0.10.10で入れた検査に穴がありました
  - 検査が見ていたのは、AIが渡す**抜粋**だけでした。抜粋が対象のすぐ後ろで切れていると、「後ろに何があるか」を見られません
  - 例：本文「それって取り憑くってこと？」に対し、抜粋「それって取り」・対象「取り」・修正案「取り憑く」。抜粋の後ろが空なので素通りしていました
  - **本文のその行を見て確かめる**ようにしました。当てるのは行に対してなので、確かめるのも行に対して行います

### 直したこと

- **「戻す」が押せないように見えていました。** 適用済みの指摘を薄く出す指定が、ボタンにも掛かっていたためです（実際には押せていました）。薄くするのは中身だけにしました

## 0.10.20 - 2026-08-21

### 増えたこと

- **対話でプロットを作れるようにしました**（設計書6.4.7）。操作メニュー →「執筆AI支援」→「対話でプロットを作る」
  - まだ書けていない項目を、AIが**1つずつ尋ねます**。答えを整えてプロットへ書く案を出すので、押せば入ります
  - **AIが筋書きを作るのではありません。** 作者の中にあるものを引き出します。プロンプトにも「作者が言っていないことを足さないこと」と明記しています
  - 尋ねる順は ログライン → テーマ → 世界観 → 舞台 → 人称 → 主人公の行動原理 → あらすじ → 主要登場人物 → モチーフ。**話を一言で言えると、あとが決まりやすい**ためです
  - どの問いにも**押すだけで答えられる例**と「この項目は飛ばす」を出します
  - 書き込みが済むと、次の項目へ進みます

## 0.10.19 - 2026-08-21

### 直したこと

- **プロットが既定のエディターで開いていませんでした**（設計書6.17.6、作者の報告）。`plot.md` だけが「テキスト エディター」で開き、記法がそのまま並んでいました
  - `openTextDocument` + `showTextDocument` は、**設定の割り当て（`workbench.editorAssociations`）を通りません**。作品一覧から開いた場合は通っていたので、同じ「開く」で経路が2通りありました
  - プロット・第1話・設定資料集・競合ファイル・統計からのジャンプを、**すべて既定のエディター経由**にしました
  - 該当行へ飛ぶ操作（提案パネル・ルビ・相談の引用）は、エディターの実体が要るのでそのままです

## 0.10.18 - 2026-08-21

### 直したこと

- **「VS Codeのアカウントを使う」と答えたのに、送信でまた聞かれていました**（設計書5.5.12、作者の報告）。リポジトリを作るのはGitHubのAPIで、送るのは別プロセスの git なので、鍵が引き継がれていませんでした
  - リポジトリを作った直後に、**gitが使う保管場所（Windowsなら資格情報マネージャー）へ鍵を入れる**ようにしました
  - **鍵を平文で置きません。** 作品フォルダーには残らず、保管場所はOSに任せます
  - 保管の仕組みが無い環境では、その旨をお伝えします（失敗はしません）

## 0.10.17 - 2026-08-21

### 直したこと

- **表記ゆれが提案パネルに出ないことがありました**（作者の報告）。原因は3つでした
  - **組を選ぶと、その数だけ「どちらに揃えますか」を聞かれていました。** 14組なら14回です。途中で閉じると、それまでの分しか残らず0件になります。**「多い方の表記に揃える」を選べば1回で決まる**ようにしました
  - **1組も選ばずにOKを押すと、黙って終わっていました。** 選ばれていないことを伝えます
  - **終わったときに何も報告していませんでした**（誤字脱字にはあるのに）。何組を揃えて何件出したか、0件ならその理由を出します

## 0.10.16 - 2026-08-21

### 増えたこと

- **その作品の書き方をAIへ先に伝えるようにしました**（設計書6.8.14、作者の問いから）。誤字脱字と推敲の両方に渡します
  - **語り手の一人称**……地の文（台詞を除く）から数えます。競っているときは決めません
  - **文語体かどうか**……旧字・文語形の多さで見ます。実データの自分史で正しく判定しました
  - **作者が「直さない」と決めた語**
  - これまではモデルが知りようのないことを、コード側で後から弾いていました。**先に伝えれば、そもそも生まれる誤検出が減ります**

### 直したこと

- **作者が「直さない」と決めた語が、AIに届かないことがありました。** 固有名詞の辞書へ混ぜて200語で切っていたため、固有名詞の多い作品では1語も届きません。独立した枠へ移しました

### 注意

- **プロンプトを変えたので、次の1回はすべて送り直します**（誤字脱字・推敲とも）。効果は完了時の「除外 N件」で、前と比べられます
- **コード側の検証は外していません。** 作法を渡すのは「生まれる数を減らす」ためで、原稿に入れないのはコードの仕事です

## 0.10.15 - 2026-08-21

### 直したこと

- **選択画面に「取りやめる」を出すようにしました**（設計書6.17.3、作者の指摘）。38か所のうち23か所に閉じる道が出ていませんでした
  - Escでも閉じられますが、**それを知らないと出口が無いように見えます**
  - 複数選択の画面は、VS Codeが「OK」「キャンセル」を出すのでそのままです
  - 守られているかをテストが見張るようにしました（規則も部品もあったのに、機械が見ていませんでした）

## 0.10.14 - 2026-08-21

### 増えたこと

- **提案タブに、未処理の件数を出すようにしました**（設計書6.8.13、作者の指摘）。ターミナルや出力へ切り替えていても、残りがあることに気づけます
  - 数えるのは**まだ手を付けていないもの**だけです（適用済み・見送り済みは含みません。失敗したものは含みます）
  - 0件になると印は消えます

### 直したこと

- **パネルの見出しの件数が、全部を適用しても減りませんでした。** 「見送っていないもの」を数えていたためです。タブの印と同じ「残り」の数え方に揃えました

## 0.10.13 - 2026-08-21

### 変えたこと

- **本文の折り返しを既定で入にしました**（`.txt` と `.md`）。小説は1行が長く、折り返さないと横スクロールしながら読むことになります
  - **既定値を変えるだけ**です。すでに `editor.wordWrap` を設定している方は、そちらが勝ちます
  - 折り返したくない場合は、設定で `editor.wordWrap` を `off` にしてください

## 0.10.12 - 2026-08-21

### 増えたこと

- **適用した直しを「戻す」ようにしました**（設計書6.8.12、作者の指摘）。提案パネルの適用済みの指摘に「戻す」が出ます
  - **適用の鏡像です。** 書き戻す直前に本文を読み直し、修正案がその行にまだあるかを確かめます。文字コードと改行もそのまま保ちます
  - **そのあと手で直した行は戻しません。** 既に別の文になっているので、理由を出して止めます
  - **ファイルまるごとは戻しません。** その1か所だけです（あとから別の箇所へ書いた分が消えないように）
  - 戻した指摘は未処理へ戻るので、考え直してもう一度適用できます

## 0.10.11 - 2026-08-21

### 変えたこと

- **サイドバーの見出しを「小説執筆」から「統合小説執筆環境」にしました**（作者の指摘）。通知には「ソース: 統合小説執筆環境」と出るのに、サイドバーだけ別の名前になっていました
  - 拡張機能の名前と同じであることを、テストが見張るようにしました
  - コマンドパレットの見出し（「小説執筆: 誤字脱字を検知」）は**そのままです**。すべての操作の頭に付くため、長くすると読みにくくなります

## 0.10.10 - 2026-08-21

### 原稿が壊れる不具合を直しました（重要）

- **誤字脱字を適用すると、本文が二重になることがありました**（設計書6.8.11）。AIが「直したい語」と「その周りの文脈」を取り違え、**文まるごとの書き換えを修正案に入れてくる**のが原因です。コードは語の位置だけを置き換えるので、修正案が抱え込んだ前後の文がそのまま残っていました

  ```
  元:   「あんたが望むなら、夢で会わすぐらいのことはできるんだがね」
  ↓ 適用
  壊れ: 「あんたが望むなら、夢で夢で会わせるくらいのことはできるんだがねのことはできるんだがね」
  ```

  **当てると重なる修正案を、適用の前に弾く**ようにしました。作者の作品で実際に壊れた4件で確かめてあります

### 直したこと

- **ファイル名にサブタイトルを付けると、話数が二重になっていました。** 投稿サイトのヘッダーには「第15話　イジメっ子襲撃」と話数込みで入っており、そのまま足すと `episode_0015_第15話　イジメっ子襲撃.txt` になっていました。話数を落としてから足します（作者の指摘）

## 0.10.9 - 2026-08-21

### 速くなったこと

- **推敲も、隣どうしの話をまとめて送るようになった**（設計書6.8.10）。誤字脱字（0.10.8）と同じ仕組み。返ってきた行番号は元のファイルの行へ戻してから使う
- 推敲でも、出力が入り切らずに切り詰められたら**話ごとに戻して試し直す**。これまでは読み取り失敗として捨てていた

### 直したこと

- **ダイアログの文言に、まだ `**` が8か所残っていた。** 前回（0.10.1）は二重引用符の文字列しか見ておらず、`` `**${AI名}** は課金されます` `` のようなテンプレート文字列が素通りしていた。番人のテストがテンプレート文字列も見るようにして、残りを落とした
  - あわせて、**ツールチップのように記号が正しい場所を、その場で見分ける**ようにした（`MarkdownString` を組み立てている最中かどうかを見る）。ファイル単位で許すと、同じファイルへ後から足したダイアログを見張れなくなる

### 注意

- **推敲も、次の1回だけすべてのチャンクを送り直します**（分け方が変わるため）。確認画面にその旨を出します

## 0.10.8 - 2026-08-21

### 速くなったこと

- **誤字脱字検知が、隣どうしの話をまとめて送るようになった**（設計書6.8.10）。1話ずつでは指示（約5,600字）のほうが本文より大きく、19話で19回ぶん同じ指示を送り直していた（作者の指摘）。まとめる幅は設定 `novelai.mergeChunkChars`（既定6,000字。抽出と共通）
  - **返ってきた行番号は、元のファイルの行へ戻してから使う。** まとめた本文の通し番号のまま使うと、2話目以降の指摘が1話目の別の行を書き換える
  - 出力が入り切らずに切り詰められたら、**話ごとに戻して試し直す**。これまでは捨てていた

### 直したこと

- **無視した指摘が、思ったより早く復活していた。** 鍵にチャンクのハッシュを使っていたため、本文をどこか1文字直すだけで鍵が変わっていた。ファイルを基準にした
- チャンクを半分に割って再試行するとき、**後半の行番号が前半のものになっていた**（推敲・抽出では行番号を使わないため表に出ていなかった）

### 注意

- **次の1回だけ、すべてのチャンクをAIへ送り直します。** 本文の分け方が変わるため、これまでのキャッシュが使えません（確認画面にもその旨を出します）。同じ理由で、**これまで「無視」した指摘が一度だけまた出ます**

## 0.10.7 - 2026-08-21

### 直したこと

- **提案パネルに誤字脱字が出てこなかった。** 設定資料の更新を一度でも表示すると、以後の誤字脱字・表記ゆれ・推敲・矛盾・プロット逸脱・編集部からの提案が**すべて隠れていた**（見出しと件数だけ変わり、中身は前のまま）。表示を切り替える5つの入口のうち4つが、前の中身を空にし忘れていた（作者が実機で発見）
- 表示の入れ替えを1か所（`replaceContents`）に集めた。**渡さなかったものは空になる**ので、入れ物を増やしても書き忘れが起きない

## 0.10.6 - 2026-08-21

### 直したこと

- **文字数の数え方（純／総）の設定が、ステータスバーにしか効いていなかった。** 総文字数を選んでいると、右下と作品一覧で違う数字が出続けていた。**作品一覧も設定に従う**ようにした（作者の指摘）
- 総文字数を選んでいるときは、数字の前に「総」と添えるようにした（作品一覧・ステータスバーとも）
- **設定を変えても、その場では何も変わらなかった。** 設定変更の受け口が1つも無く、ファイルを開き直すまで古い数字のままだった。切り替えたらすぐ反映するようにした
  - ルビの扱い（`excludeRubyFromCount`）は走査のときに効くので読み直し、純／総の切り替えは描き直すだけ、と分けている

### 変えていないこと

- **執筆量の記録と目標は純文字数のまま。** 記録は日ごとに積んであるので、途中で基準を変えると過去の記録と地続きでなくなり、「今日の増分」が数え方の変更ぶんだけ跳ねる。執筆統計パネルの一覧は、見出しに「純文字数」と明記してある

## 0.10.5 - 2026-08-21

### 直したこと

- **作品一覧で、行によって「1話ぶん」が付いたり付かなかったりした。** 投稿サイトのダウンロードには、1話ずつ別ファイルなのに区切り行（`------- エピソードN開始 -------`）が入っている形がある。区切り行が1つでもあれば合本と見なしていたため、その形の作品だけ全ファイルに「1話ぶん」が付いていた。**2話以上のときだけ出す**ようにした（作者が実機で発見）
- 執筆統計パネルの話ごとの一覧にも同じ印が出ていたので、揃えた

## 0.10.4 - 2026-08-21

### 増えたこと

- **編集部へ、その作品だけを渡せるようにした**（設計書5.7.5）。操作メニュー →「作品管理」→「編集部とやり取り」
  - 「編集部へ渡す」…… この作品だけを入れた非公開リポジトリを作り、本文と設定資料を送ります。**ほかの作品は渡りません**
  - 「編集部の提案を取り込む」…… 編集部が書いた提案を取り寄せて提案パネルへ並べます。**本文には触りません**
  - 本文と設定は送り出すたびにまるごと置き換えます（編集部はそこへ書かないため）。**提案だけは両方向で混ぜます**（承認・却下は作者が書くので、見送ったことが編集部へ伝わる必要があるため）
  - 過去の履歴は渡しません（作者の判断）

## 0.10.3 - 2026-08-21

### 増えたこと

- **作品集（1つのリポジトリに複数の作品）を扱えるようにした**（設計書5.7）。取り寄せも同期も1回で全作品に効く
  - 「フォルダから作品を追加」「GitHubから作品を追加」が、**渡されたフォルダーが作品か作品集かを自分で見分ける**。ボタンは増やしていない
  - 作品集だったときは一覧を見せて、選んだものをまとめて登録する。既定は全部チェック済み
  - 1件失敗しても残りを続け、最後にまとめて報告する
- **編集者モードでは作品を1つしか登録できないようにした**（設計書5.7.4）。取り違えて別の作品へ提案を書かないための仕切り。**守りではないこと**も画面に書いている（守るのはGitHubの招待範囲）

## 0.10.2 - 2026-08-21

### 直したこと

- **操作メニューの未反映件数のオレンジが薄く、読みにくかった。** VS Code の既定色（`charts.orange` = `#d18616`）をそのまま使っており、白地のテーマで埋もれていた。自前の色（`novelai.pendingBadge`）を定義し、明るいテーマでは濃く、暗いテーマでは明るくした（作者の指摘）
- 色IDが `package.json` に無くても VS Code は黙って色を付けないため、コードと定義を突き合わせるテストを足した

## 0.10.1 - 2026-08-21

### 直したこと

- **提案パネルに `###` や `##` がそのまま出ていた。** 設定資料の更新の差分を、文書に貼る形（Markdown）のまま画面へ流していた。パネルはHTMLを自分で組み立てるので、記法は解釈されない。パネル用の形（`diffLinesForPanel`）を別に用意した（作者が実機で発見）
- **ダイアログの文言36か所に `**` が混ざっていた。** VS Code のダイアログと選択肢の説明はプレーンテキストで、記号がそのまま出る。すべて落とし、見出しだった行は `【 】` で区切り直した
- 提案パネルのビュー名が「誤字脱字」のままだった。中身は表記ゆれ・推敲・矛盾・編集部からの提案も並ぶので「提案」にした

### 増えたこと

- **画面に出す文字列へMarkdownの記号を混ぜていないか、テストが見張るようにした**（`test/unit/plainTextUi.test.ts`）。Markdownとして読まれる先（`.md` の書き出し・ツールチップ・プロンプト）だけを一覧で許し、それ以外は落とす

## 0.10.0 - 2026-08-19

**配布版です。**0.7.0 から先に入った直しと機能がまとめて届きます。

### 大きいもの

- **編集者モード**：操作メニュー →「拡張機能の設定」→「**作者／編集者を切り替える**」から入れます。編集部の方と一緒に使えます。本文を書き換えず、
  **提案として置きます**。校閲中のファイルを押さえることもできます
- **「提案」パネル**：作者への提案が1か所に集まります（誤字脱字・表記ゆれ・
  推敲・矛盾・プロット逸脱・編集部の提案・設定資料の更新）
- **ルビ**：`{漢字|かんじ}` で書くと、プレビューに振り仮名が出ます。
  投稿サイト用の変換もできます
- **合本ファイルの分割**：全話が1つのファイルを、話ごとに分けられます
- **さくらのAI Engine** が使えるようになりました（国内のサービス）
- **編集履歴**：誰が何を直したかを、作者・編集者・AIで色分けして見られます

### 原稿を守るための直し

- 推敲の修正案に「空文字」という3文字が入り、**適用すると本文がその3文字に
  置き換わる**状態でした
- 誤字脱字の誤検出を、実データで **62件 → 27件** に減らしました
- **方言や口癖を「直さない語」に登録**できます
- **一人称を書き換える指摘**を出さなくなりました

### 直したこと（実機で見つかったもの）

- **「まとめて適用」が働いていませんでした。**押しても何も起きない状態です。
  対象が無いときに黙っていたのと、設定資料の更新では中身を見ていなかった
  ためです。**なぜ対象が無いのか**と、**何件入ったか**をお伝えするように
  しました
- ボタンの名前を「表示中をまとめて適用」から「**まとめて適用**」に改めました。
  確信度が低いものは、表示していても対象外だったためです
- 各話あらすじの見出しで、話数が二重に出ていました（「第1話 第1話」）

### 使い勝手

- **はじめて開いたときにAIを選べます**
- 誤字脱字で「**前回から書いた分だけ**」を選べます
- 作品一覧から**GitHubの作品を直接追加**できます
- ファイルを右クリックして、**サブタイトルや本文を投稿用にコピー**できます


## 0.9.6 - 2026-08-19

**各話あらすじの見出しで、話数が二重に出ていたのを直しました。**

```
誤：第1話 第1話　気がついたら幽霊
正：第1話 気がついたら幽霊
```

投稿サイトからダウンロードした原稿は、題そのものに「第1話」が入っています。
見出しにも話数を出すため、重なっていました。

作品一覧と文字数の表では正しく出ていましたが、**あらすじだけが漏れていました。**

## 0.9.5 - 2026-08-19

**誤字脱字の検知で「前回から書いた分だけ」を選べるようになりました。**

2回目からは、実行時にどこまで見るかを聞きます。

```
前回から書いた分だけ（3話）
作品全体（219話）
```

**一覧が短くなり、待ち時間も減ります。**前に読んで「直さない」と決めた箇所
（無視は押していないもの）が毎回並ぶこともなくなります。

**作品全体を選んでも、AIは呼び直しません。**変わっていない話は前の結果を
使います。

**新しく書いた話が無いときは、そうお伝えします。**黙って全体を見ると、
「差分だけのはずが全部出た」と見えてしまうためです。

## 0.9.4 - 2026-08-19

**残っている作業の一覧を、実態に合わせました。**（動きは変えていません）

「未着手」の一覧に、**すでに実装済みのものが5件**残っていました（推敲・矛盾検知・
ルビ・締切からの逆算・送り仮名ゆれ）。開発の記録なので、お使いの動きには
影響しません。

## 0.9.3 - 2026-08-19

**送り仮名のゆれも見つけるようになりました。**

「行う／行なう」「打ち合わせ／打合せ／打合わせ」のように、**同じ語なのに
送り仮名の付け方が違う**組を、表記ゆれの検知で拾います。

**「行った／行く」のような活用形は拾いません。**それは直すものではありません。

> **迷いやすい語を選んで持っています**（15組）。ここに無いゆれは見つかりません。
> 文章から語の切れ目を自動で見つけるには辞書だけで数百MBの仕組みが必要で、
> それを入れずに済ませています。**見つからないほうが、無いものを見つけたと
> 言うよりよい**と考えました。

## 0.9.2 - 2026-08-19

**AIの独り言が、まだ設定資料に取り込んでいない話を教えてくれるようになりました。**

「まだ設定資料に取り込んでいない話が3話あります。空き時間に資料抽出やって
おきましょうか？」のように声をかけます。

**言い過ぎないようにしています。**

- **一度も抽出していない作品では言いません。**登録した直後に「19話ぶん
  抽出しませんか」と言われるのは、申し出ではなく催促です
- **1話だけでは言いません。**書いた直後に毎回言われては落ち着きません

## 0.9.1 - 2026-08-19

**投稿するときの手作業を減らしました。**作品一覧でファイルを右クリックしてください。

- **サブタイトルをコピー** … 題だけをクリップボードへ入れます
- **本文を投稿サイト用にコピー** … 本文だけを、**ルビを直して**入れます
- **ファイル名にサブタイトルを付ける** … `episode_0001.txt` →
  `episode_0001_転生.txt`

投稿欄はサブタイトルと本文が別々の入力になっています。毎話、ファイルを開いて
ヘッダーを避けて本文を選んで、ルビを書き換えて……を繰り返すのは大変です。

**本文だけをお渡しします。**【タイトル】などのヘッダー、前書き、後書き、
リアクションは含みません。**前後の空行も落とします**（投稿欄の先頭に空行が
入ると、1行目が空いた状態で公開されることがあるためです）。

**原稿には触りません。**

**改名は話数の部分を変えません。**そこは並び順を決めているためです。

## 0.9.0 - 2026-08-19

**全話が1つになっているファイルを、話ごとに分けられるようになりました。**

作品一覧でファイルを**右クリック**して「**話ごとのファイルに分ける**」です。
小説家になろうのダウンロード形式（「エピソードN開始」の行があるもの）に
対応しています。

分けると、**話数と話ごとの文字数が出るようになり**、設定資料の抽出でも
「何話に出てくるか」が付くようになります。直した話だけを processing し直せる
ので、AIの処理も軽くなります。

**前書き・後書き・リアクションも、そのまま残します。**

**書き込む前に検算します。**切ったものを繋ぎ直して元と1文字も違わないことを
確かめ、**合わなければ分けません。**

**元のファイルは消しません。**回復用の場所へ移し、どこへ移したかをお伝えします。

## 0.8.9 - 2026-08-19

**取り込みで改行コードが変わることを、お伝えするようにしました。**

GitHubから取り込む（pull する）とき、Gitの設定によっては**改行コードが
書き換えられます。**Windows では既定で有効なことが多く、LFで書いた原稿が
取り込んだだけでCRLFに変わります。

**この拡張機能は改行を保ったまま書き戻しますが、Gitによる書き換えまでは
止められません。**そこで、取り込みの前に**作品ごとに一度だけ**お知らせします。

気になる場合の直し方（`git config core.autocrlf false`）も、クリップボードへ
入れられるようにしました。

## 0.8.8 - 2026-08-19

**操作メニューの「相談で使う検索」を「AI相談の強化」に改めました。**

検索は手段であって、**得たいのは相談の答えがよくなること**です。
手段の名前が出ていても、押すかどうかの判断ができません。

**「検索用の索引を削除する」の説明も書き直しました。**

これまでは「置き場所を空けます」としか書いておらず、**消すと何が悪くなるのかが
分かりませんでした。**いまは、**相談が語句一致に戻る**こと（言い換えで聞いた
ときに見つけにくくなること）、**本文や設定資料は何も変わらない**こと、
**いつでも作り直せる**ことをお伝えします。

## 0.8.7 - 2026-08-19

**操作メニューの「AI」の印から、色を外しました。**

行の右端に「AI」と出る位置はこれまでどおりです。**紫色だったのをやめ、
ほかの文字と同じ色にしました。**

（0.8.6 で四角に囲む形を試しましたが、**位置が変わってしまう**ため
取りやめました。どちらも配布していません。）

## 0.8.5 - 2026-08-19

**作品一覧から、GitHubの作品を直接追加できるようになりました。**

「フォルダから作品を追加」の隣にボタンが並びます。これまでは
「操作メニュー → 作品管理 → 既存作追加」まで辿る必要がありました。

## 0.8.4 - 2026-08-19

**Mac と Linux でも、Ollama の入れ方をご案内します。**

これまで自動で入るのは Windows だけで、ほかの環境では配布ページを開くだけでした。

- **Windows**：`winget` で入ります（従来どおり）
- **Mac**：Homebrew があれば `brew install ollama` で入ります
- **Linux**：**打つべき1行をお見せします**（自動では実行しません）

Linux の公式の入れ方は、ネットから取ってきたスクリプトをそのまま実行する形です。
**それを拡張機能が黙って走らせるのは筋が悪い**と考えました。何が走るのかを
お見せして、判断していただきます。**コマンドはクリップボードへ入れられます。**

Homebrew が入っていない Mac でも、同じように手順をお見せします。

## 0.8.3 - 2026-08-19

**はじめて開いたときに、使うAIを選べるようになりました。**

これまでは作品一覧の歯車から自分で開く必要がありました。**入れたばかりでは、
そこに何があるのか分かりません。**

はじめて開いたときに一言お伝えし、「AIを選ぶ」を押すと選択画面が出ます。
**1度だけです。**毎回出ては邪魔ですし、あとから「AI設定」でいつでも開けます。

**AIを選ばなくても使えます。**作品の管理と文字数の集計は、AIなしで動きます。

**Ollamaを選んで、つながらなかったときの案内を変えました。**

これまでは「設定を開く」と出していましたが、**まだ入れていない方は、設定を
開いても何もできません。**何がどれだけ要るのかを一覧でお見せする画面へ
ご案内します。

作品一覧の歯車（AI設定）は外しました。操作メニューからは今までどおり使えます。

## 0.8.2 - 2026-08-19

**説明書の反映漏れを直しました。**（動きは変えていません）

設計書に、誤字脱字と推敲で増やした検査が**丸ごと書かれていませんでした**。
また「推敲・矛盾検知は未実装」という古い記述が残っていました。**どちらも
実装済みで、公開もしています。**

開発用の指示書（CLAUDE.md）も、新しく足した19個のファイルが一覧に無く、
現在の状況が古いままでした。

## 0.8.1 - 2026-08-19

### 「AI指摘」パネルを「提案」に改めました

編集部からの提案も同じ場所に出るようになったため、**「AI指摘」という名前が
中身と合わなくなりました。**

**作者への提案は、すべてこの1か所に集まります。**

- 誤字脱字・表記ゆれ・推敲の指摘
- 矛盾・プロットからの逸脱
- 編集部からの提案
- **設定資料の更新**（これまでは別のダイアログでした）

設定資料の更新は「反映待ちの更新があります」というダイアログで出ていましたが、
**提案の窓口が2つあると片方を見落とします。**同じパネルに並べました。

## 0.8.0 - 2026-08-19

**編集部の方と一緒に使えるようになりました。**

### 編集者モード

設定 `novelai.mode` を `editor` にすると、その環境では**本文の校正・校閲だけ**が
使えます。誤字脱字・表記ゆれ・推敲・直さない語の管理・原稿の同期と競合解決・
文字数の確認・設定資料を読むこと。

**設定資料の抽出、プロット、あらすじ生成、執筆統計、新規作品の作成は押せません。**
消えるのではなく、押せなくなって理由が出ます。

押せてしまうと、いつか押されます。設定資料の抽出を編集部が走らせると、
**作者の資料がAIの読みで書き換わりかねません。**

### 編集部は本文を書き換えません

**編集者モードで「適用」を押しても、本文には書き込みません。提案として置きます。**

作者側で「校正・校閲 → **編集部からの提案を見る**」を開き、1件ずつ**採るか見送るか**を
決めます。**本文に入るのは、あなたが採ると決めたものだけです。**

この形なので、**編集部との競合は起きません。**編集部が触るのは提案のファイルだけです。

### 校閲中のファイルを押さえられます

編集部が「**校閲を始める／終える**」を押すと、いま開いているファイルが「校閲中」に
なります。**ファイル単位です**ので、第3話を見ている間も第20話は書けます。

作者側でそのファイルを直そうとすると、**誰がいつから見ているか**が出ます。
止めはしません。**あなたはいつでも外せます** — 編集部が外し忘れて連絡が付かないとき、
自分の原稿を触れなくなっては困るためです。

### 誰が直したかが残ります

**作者（●）・編集者（▲）・AI（■）**の3つに分けて記録します。
「執筆データ → **編集履歴を見る**」で、色分けして並べて見られます。

AIの分は「AIの提案のうち、**あなたが承諾して反映したもの**」です。

記録は **GitHubで同期されます。**離れた場所にいる編集部の直しも見えます。
**この画面から履歴は変えられません。**後から直せると、履歴の意味が無くなるためです。

### 競合したとき、誰の版かが出ます

同じ場所を別々に直した場合、それぞれの版を**最後に触った人の名前**が出ます。
「別環境の版」とだけ出ていると、**編集部の直しが自分の書き忘れに見えて、
うっかり捨ててしまいます。**

> **履歴を残しているのは、いまのところAI指摘パネルの操作だけです。**
> 本文の直接編集や設定資料の書き換えは、まだ記録されません。順に足します。

## 0.7.5 - 2026-08-19

**編集部が直した設定資料を、AIが上書きしないようにしました。**

設定資料が拡張機能の外で変更されたときのお知らせに、「**この変更を守る**」を
足しました。押すと、その作品の設定資料を「人が確定させたもの」として扱い、
**以後AIの抽出で上書きしません。**

編集部はGitHub経由で直すため、拡張機能の画面を通りません。そのままだと、
AIが作った項目を編集部が直しても印が付かず、次の抽出で消えてしまいます。

**作者ご自身が直した記述は、もともと守られています。**今回足したのは、
画面を通らない直しにも同じ印を付けられるようにするものです。

**自動では付けません。**別のAI拡張機能（Copilotなど）が書いた場合も同じお知らせが
出ます。機械が勝手に決めると「AIの書いたものをAIから守る」ことになるためです。

## 0.7.4 - 2026-08-19

**設計の前提を直しました。**（動きは変えていません）

これまで「編集するのは同一人物」を前提にしていましたが、**編集部が別の
GitHubアカウントで編集することもある**、に改めました。

この変更で、競合（同じ場所を別々に直した状態）の意味が2つに分かれます。

- **自分と自分（別のパソコン）** … 同期を忘れた事故。**防ぐべきもの**
- **自分と編集部** … 事故ではありません。**意図があって直されています**

後者で機械が片方を選ぶと、**編集部の意図が黙って消えます。**そこで、
どちらの版かをはっきり見せて選んでいただく方針にしました。

> **競合の画面が「誰の版か」をまだ出していません。**いまは「この環境／別環境」
> としか出ないため、編集部の直しが自分の書き忘れと同じに見えます。
> **設計書に未実装として書き残しました。**編集部が実際に入る前に直します。

## 0.7.3 - 2026-08-19

**さくらのAI Engine が使えるようになりました。**

「AI設定」から選べます。**国内のサービスなので、原稿の送り先が国内で
完結します。** 手元のパソコンで動かすOllamaほどではありませんが、
海外のクラウドへ送るより気が楽な場合の選択肢です。

APIキーは、さくらのクラウドのコントロールパネルで発行できます。
**入力した鍵はsettings.jsonには書かれません**（OSの資格情報ストアに預けます）。

**実行するたびに課金されます。** 無料の枠があっても超えれば費用がかかるため、
有料のAIとして扱います。実行前に処理量をお見せします。

**モデルの大きさに見合った扱いをします。** さくらが提供しているのは、
名前に大きさの入ったモデル（`preview/gemma-4-31B-it` など）です。そこを読んで
能力を見積もります。「クラウドだから何でもできる」とは扱いません。
大きなモデル向けの長い指示を小さなモデルへ送っても、うまくいかないためです。

> **まだ実際の鍵で試せていません。** 手元に鍵が無いため、つなぐところまでを
> 用意した状態です。うまくいかない場合は、出たメッセージをお知らせください。

## 0.7.2 - 2026-08-19

**ルビ（振り仮名）が使えるようになりました。**

本文で文字を選んで「**ルビを振る**」を押すと、読みを聞かれます。
漢字のすぐ後ろにカーソルがあれば、**選ばなくても拾います。**

```
{魔導書庫|まどうしょこ}へ向かう
```

**プレビュー（Ctrl+Shift+V）で振り仮名として表示されます。**新しい画面は
作っていません。いつも使っているMarkdownプレビューにそのまま出ます。

**投稿するときは「投稿サイト用に変換してコピー」**を押してください。
`｜漢字《かんじ》` の形に直してクリップボードへ入れます。**なろう・カクヨム・
アルファポリスのいずれでも、そのまま貼り付けられます。**

**原稿は書き換えません。**貼り付ける先はサイトの投稿欄であって、手元の原稿を
投稿サイトの記法にしてしまうと、次に書くときに困るためです。

すでに投稿した原稿を持ち込んだときは「**投稿サイトのルビを取り込む**」で
`{漢字|かんじ}` の形に直せます。何件変わるかを先にお見せします。

**テキスト（.txt）で使おうとしたときは、Markdownへの変換をご提案します。**

ルビはMarkdown（.md）でしか使えません。テキストは投稿サイトから持ってきた形を
そのまま保つためです。ただ、そう言われても困るだけですので、その場で
「このファイルを .md にする」「このフォルダーの .txt をまとめて .md にする」を
選べるようにしました。

**中身は1文字も変えず、名前だけを変えます。**文字コードも改行も、書いた本文も
そのままです。取り消したくなったら、名前を .txt に戻すだけで元どおりです。

**同じ名前の .md があるときは変換しません。**上書きすると、そちらに書いてあった
本文が消えてしまうためです。

## 0.7.1 - 2026-08-19

**一人称を書き換える指摘を出さなくなりました。**

誤字脱字の検知が、こう指摘してくることがありました。

```
「僕が所属する」→「私が所属する」
「僕ら」→「私たち」
```

**一人称は作品の根幹です。**「僕」で書かれた小説を「私」に直したら、
語り手が別人になります。誤字ではありません。

方言や口癖は「直さない語」に登録していただく形ですが、**一人称はどの小説にも
必ずあります**ので、登録を待たずに最初から守ります。二人称（「君」「お前」）も
同じです。

**本物の誤字は今までどおり出ます。**「僕の手足がｈっきりと」の「ｈっきりと」も、
「私わ行く」の「わ」も、直す対象として挙がります。変えているのが人称そのもの
かどうかで分けています。

実データ（10作品）で測り、2件が落ちることを確かめました。

## 0.7.0 - 2026-08-18

**Marketplace へ配布する版です。**0.6.35 から先に入った直しがまとめて届きます。

とくに大きいのは次の3つです。

**方言や口癖を「直さない語」として守れるようになりました**（0.6.36）。
「はよ」→「早く」、「急いどるんやろ？」→「急いでるんやろ？」のような、
**直すと人物が変わってしまう指摘**を止められます。指摘を見たその場で
「今後直さない」を押すだけです。

**推敲の修正案が本文を壊しかねない状態でした**（0.6.33）。AIが「空文字」という
3文字を修正案として返し、適用すると本文の一文がその3文字に置き換わるところでした。
本文へ書き込む手前で止めるようにしました。

**誤字脱字の誤検出を大きく減らしました**（0.6.34）。作者の10作品・44,000字で測り、
通ってしまう指摘を62件から27件にしました。「保険」→「保険」のような直しようの
ないもの、「然し」→「しかし」のような正しい文語を直そうとするものを弾いています。

**設定画面が開けなくなっていたのを直しました。**「拡張機能の設定」を押しても
項目が1つも出ない状態でした。拡張機能のIDを  と
書き下しており、Marketplaceへ出すために発行者名を変えたときに追随して
いませんでした。IDを書かず、VS Codeから受け取る形にしました。

このほか、台詞の中の繰り返しを指摘しない、README のインストール手順の整理など
（0.6.36〜0.6.38）。

## 0.6.38 - 2026-08-18

**文書が実態と食い違っていたのを直しました。**（機能は変えていません）

引継ぎ書に「VSIX化はまだ。0.0.2 が最後の配布」と書いてありましたが、**すでに
Marketplace で公開済み**でした。ここは次の作業で最初に読む場所なので、そのまま
信じて「まだ公開できません」と誤った説明をしていました。

- 引継ぎ書の「いまの状態」を、手元の版と公開中の版の両方が分かる形にしました
- READMEの「現在の配布版」は手元の版を指していて紛らわしかったので、
  「このリポジトリの版」に改め、Marketplace の版は遅れることがあると添えました

## 0.6.37 - 2026-08-18

**READMEのインストール手順を直しました。**（機能は変えていません）

- **存在しないリリースを名指しで案内していました。** VSIXの手順が `v0.6.36` を
  決め打ちしており、そのタグが無い間は失敗します。最新のリリースを見つけて
  落とす形に変えました
- **インストールの直後に開発者向けの手順が挟まっていました。** `npm install` と
  CIのシークレット設定が、小説を書く方が最初に読む場所に入っていたので、
  使い方の後ろへ移しました
- **「今後の実装予定」に、実装済みのものが残っていました。** 推敲・プロット逸脱
  検知・矛盾検知は 0.6.29〜0.6.32 で実装しています。予定のままだと、使える機能を
  探しに来た方が見落とします
- 「できること」に、推敲・逸脱・矛盾の検知と「直さない語」を書き足しました

**はじめての方向けのインストール手順を足しました**（`docs/note-インストール手順.md`）。
パソコンに詳しくない前提で、VS Codeを入れるところから順に書いてあります。
## 0.6.36 - 2026-08-17

**方言や口癖を「直さない語」として登録できるようにしました。**

誤字脱字の検知が、こう指摘してくる問題がありました。

```
「はよ」→「早く」                  「あらへんで」→「あらへん」
「なんゆうてまんのや？」            「急いどるんやろ？」→「急いでるんやろ？」
```

どれも関西弁の台詞で、直せば人物が変わってしまいます。**人物や場所を抽出して
固有名詞を113語渡しても、これは消えませんでした。方言は固有名詞ではないからです。**

**足し方は2つあります。**

- **AI指摘パネルの「今後直さない」**。指摘を見たその場で押せます。
  以後どの話でも指摘されなくなります
- **「執筆AI支援 → 校正・校閲 → 直さない語を管理」**。一覧を見て、
  消すこともできます

**誤字脱字と推敲の両方で効きます。**推敲は一文まるごとを置き換えるので、
守る語が含まれていればその指摘自体を出しません。

登録した語は **`設定/keep_words.json`** に控えます。設定資料と同じ場所なので、
テキストエディタで開いて手で直せますし、GitHubで共同作業者にも渡ります。
手で書くなら `["はよ", "せやな"]` のように語を並べるだけで構いません。

**活用しても守ります。**「急いどる」を登録すれば「急いどるんやろ？」も守られます。
方言は形が変わるためです。

**1文字は登録できません。**「の」を登録すると本文のほとんどが守られてしまい、
本物の誤字も出なくなるためです。

実データ（10作品）で測り、方言4件が落ちることを確かめました。
## 0.6.35 - 2026-08-17

**設定資料の抽出を、10作品で測りました。**（動きは変えていません）

これまで抽出を測ったのは1作品だけでした。作風の違う10作品・44,000字で走らせ、
**壊れた挙動は見つかりませんでした。**同じ日に測った誤字脱字・推敲・逸脱検知とは
違い、抽出はそのまま使えます。

10作品から**人物59人・能力8・場所28・組織20**を拾い、名前もほぼ正しく取れて
います（ホンゴー、ナイン、太志、ヴォイド、コリンナ…）。除外もきちんと働いて
いました（`null`・「僕」・「主人公」・「男」・「兄」・本文に無いもの）。

**「設定資料を先に抽出すれば誤字の誤検出が減る」を確かめました。**

0.6.34 でそう書きましたが、根拠がありませんでした。実際に抽出した113語を
辞書として渡して測り直したところ、

| | 辞書なし | 辞書113語 |
|---|---|---|
| AIが挙げた | 70件 | **46件** |
| 残った | 27件 | **24件** |

**減りますが、方言は減りません。**「はよ」→「早く」、「急いどるんやろ？」→
「急いでるんやろ？」は辞書があっても出ます。**方言は固有名詞ではないので、
固有名詞の辞書では守れません。**

作品ごとに「直さない語」を登録できるようにするのが、やはり必要です。
## 0.6.34 - 2026-08-17

**誤字脱字の検知が、直す必要のないものを大量に出していました。**

作者の10作品・44,000字で実際に走らせたところ、AIが挙げた64件のうち
**62件がそのまま通っていました**。中身を見ると、直す必要のないものばかりです。

**4つ、機械的に確かめられるものを弾くようにしました。**

**1. 同じ語を「修正案」として返してくるもの**（いちばん多く、30件）

「保険」→「保険」、「跨いだ」→「跨いだ」。押しても何も起きないのに、
作者は1件ずつ見て消さなければなりませんでした。推敲にはこの検査があったのに、
誤字脱字には無かったためです。

**2. 正しい文語・旧字を「誤変換」として直そうとするもの**（6件）

```
然し → しかし        於いて → において     可成り → かなり
与へて呉れた → 与えてくれた              聯隊 → 連隊
```

**どれも正しい日本語です。**戦前の文語体で書かれた作品では、直せば元の文章が
壊れます。よく出る形を一覧にして守るようにしました。

**3. 読みが同じで書き方だけ違うもの**（2件）

「ハメになった」→「はめになった」、「2回転」→「二回転」。これは表記ゆれで
あって誤字ではありません（別の機能で扱うものです）。**片仮名と平仮名**、
**算用数字と漢数字**という、読みが同じと言い切れる2つだけを見ています。

**4. 末尾に句読点を足すだけのもの**（3件）

「会頭だ」→「会頭だ。」。**日本語の小説では台詞の末尾に句点を打ちません。**

**結果：通る指摘が 62件 → 27件になりました**（同じ本文・同じモデル）。

**まだ残っている問題**

設定資料をまだ抽出していない作品では、固有名詞の辞書が空になります。その状態だと
**方言を誤字として指摘してきます**（「はよ」→「早く」、「急いどるんやろ？」→
「急いでるんやろ？」）。作品ごとに「直さない語」を登録できるようにするのが
次の手だと考えていますが、まだ実装していません。

**設定資料を先に抽出しておくと、この誤検出は減ります。**
## 0.6.33 - 2026-08-17

**推敲の修正案が、本文を壊しかねない状態でした。直しました。**

作者の10作品・44,000字で推敲を実際に走らせたところ、AIがこう返してきました。

```
原文: 当然のことだが、帝国内の商会は相互に競争をしている。……
提案: 空文字
```

こちらのプロンプトに「直し方が文体の書き換えになるものは、修正案を**空文字**に
してください」と書いてあり、AIはその言葉を**修正案の中身として書いてきました**。
そのまま「適用」を押すと、**本文の一文が「空文字」という3文字に置き換わります。**

**本文へ書き込む手前で止めるようにしました。**「空文字」「なし」「変更不要」
`null` のような、中身が無いことを言う言葉を修正案として受け取りません。

- **推敲**：指摘そのものは残し、修正案だけを空にします（読みにくい箇所を指す
  ことには意味があるため）
- **誤字脱字**：指摘ごと落とします（誤字には必ず直し方があるはずで、
  無いならそれは指摘ではないため）

プロンプトの書き方も直しましたが（「空文字」と書かず `""` と示す）、**言い方を
変えても別の言い方で返ってくる**ので、止めるのは検査の側です。

**台詞の中の繰り返しを、指摘しなくなりました。**

同じ測定で、`同語反復` として挙がったものの多くが**人物の喋り方**でした。

- 「あんた、クォーターやろ？　なんゆうてまんのや？」→ **関西弁**
- 「わた、く、しは、で　んかを、あいして　い ます……」→ **わざと崩した喋り**
- 「商人は帝国を打倒したりせぇへん。……商人は商人らしく」→ **強調の反復**

どれも直したら人物が変わってしまいます。**台詞は文章の癖ではありません。**
地の文の対句（「ある者は……ある者は」）は今までどおり出します。直すかどうかは
作者が決めることだからです。

**測って分かったこと**（`gemma4:e4b`、10作品・17チャンク）

| | 手当ての前 | 後 |
|---|---|---|
| AIが挙げた | 83件 | 74件 |
| 残った | 30件 | **18件** |
| うち修正案あり | 2件 | 2件 |

**残った指摘の9割に修正案がありません。**「ここが読みにくい」と指すだけです。
これは意図した動きですが（直し方は作者が決めること）、手元のAIでは
**機械的に直せる指摘がほとんど見つからない**ということでもあります。

また、この測定で `冗長` と `長文` の指摘が初めて出ました。それまでは1作品でしか
測っておらず、その作品には当てはまる箇所が無かったためです。
## 0.6.32 - 2026-08-17

**プロット逸脱・間延び検知を実装しました**（「執筆AI支援 → 校正・校閲 → **プロットからの逸脱を検知**」）。

書いたプロットと本文を照らし合わせ、**プロットに無い展開**や**物語が前へ進んでいない箇所**を探します。話ごとに見ます。

**本文は書き換えません。プロットのほうが古いこともあります。**「プロットでは／この話では」を並べ、「本文を見る」「プロットを見る」を出すだけです。

**プロットが無いと実行しません。**照らし合わせる相手が無いと、AIは本文だけを見て「逸脱していそうなこと」を作り出すためです。見出しだけのテンプレートも「無い」と見なします。

> ### ⚠ この機能は、手元のAI（Ollama）ではほとんど働きません
>
> **実データで5回測りましたが、プロットに載せた話と外した話を見分けられませんでした。**抑える指示を足すと全話0件、外すと全話に1件ずつ付ける、という具合で中間がありません。
>
> **Claude・ChatGPT・Gemini をお使いください**（プロンプト設計書もこの機能にはClaudeを推奨しています）。Ollamaで実行しようとすると、事前にその旨をお伝えします。
>
> なお、**間違った指摘をはじく仕組みは働いています**（測定では悪い3件を3件ともはじきました）。

## 0.6.31 - 2026-08-17

**操作メニューから項目が消えていた**のを直しました。

作品を登録していない状態では、6つある分類のうち**3つが丸ごと消えて**いました（執筆データ・執筆AI支援・資料管理）。中身が全部「作品を必要とする操作」だったためです。

これは当時の設計どおりの動きでしたが、**初めて使う方には、そもそも何ができる拡張機能なのかが分かりません**でした。

**消さずに出して、押せなくします。**

| | 作品なし | 作品あり |
|---|---|---|
| 分類 | **6つ**（変わりません） | 6つ |
| 押せる | 13件 | 49件 |
| 押せない | 36件 | 0件 |

押せない項目は**灰色になり「作品を登録すると使えます」**と出ます。カーソルを合わせると、どこから登録すればよいかも出ます。

## 0.6.30 - 2026-08-16

**推敲支援を実装しました**（操作メニューの「執筆AI支援 → 校正・校閲 → **推敲する**」）。

**読みにくい箇所だけ**を指摘します。見るのは4つです。

| | |
|---|---|
| 冗長 | 同じ意味の言葉が重なっている（「まず最初に」） |
| 同語反復 | 近い範囲で同じ語が繰り返されている |
| 係り受け | 修飾の関係が2通りに読める |
| 長文 | 一文が長すぎる（80字超かつ読点5個以上） |

**語彙や文体、描写の増減には触れません。** 体言止めや短文の連続といった書き方の癖も、直す対象にしません。

> **指摘は1000字あたり3件までに絞ります。** AIはどの文にも何かしら言えるので、放っておくと全部の文に提案が付き、読むだけで疲れてしまうためです。絞ったときは件数をお知らせします。

**修正案が付かない指摘もあります。** 長すぎる文をどう割るか、繰り返しをどう変えるかは**文体の書き換え**になるので、直し方は作者が決めるものだからです。その場合は「本文を見る」だけを出します。

実データ（3話・5,551字）で測ったところ、AIが挙げた9件のうち**6件を機械的にはじき**、残ったのは3件でした。はじいたのは「長文でないのに長文と言う」「繰り返しが無いのに同語反復と言う」「札は正しいが中身が語彙・文体の話」の3種類です。

## 0.6.29 - 2026-08-16

**VS Code Marketplace へ出せる形にしました。**

- `private` を外しました（付いていると公開を拒まれます）
- **ストア用のアイコン（PNG 256×256）**を作りました。`media/icon.svg` から起こすので、アクティビティバーのアイコンとずれません（`npm run build:icon`）
- リポジトリ・不具合報告先・「無料」の表示・一覧カードの色を書きました
- `npm run publish:marketplace` を用意しました。足りないものがあれば公開前に止まります

> **publisher の作成だけは、こちらではできません。** [Marketplace の管理画面](https://marketplace.visualstudio.com/manage)で作ってください（無料）。世界で1つしか取れない名前で、あとから変えるには出し直しが要ります。

あわせて、実接続の試験から**作者の手元のパス**を外しました。環境変数（`NOVELAI_LIVE_WORK`）で渡す形にし、指定が無ければ試験を飛ばします。

## 0.6.28 - 2026-08-16

### 取りやめ方が分かるようにしました

**入力欄や選択画面を閉じられない**というご指摘への対応です。

VS Codeの入力欄と選択画面には**×ボタンがありません**。閉じるには `Esc` を押すか外側をクリックしますが、この拡張機能は**入力を失わせないために「外側をクリックしても閉じない」設定**を多く使っていました。つまり **`Esc` が唯一の出口なのに、それを書いていませんでした。**

- **入力欄には必ず「（Escキーで取りやめられます）」を出します**（21か所すべて）
- **選択画面には「取りやめる」を項目として置きました。** 一覧の中に見える出口です
- 多段の流れ（形式→ジャンル、目標→応募先）では「これで決定」と「すべて取りやめる」を書き分けました

外側のクリックで閉じる設定には**戻していません**。作品名を入れている途中で別のウィンドウへ目を移した拍子に消えると、最初からやり直しになるためです。

### ログを自動で消すようにしました

`.aiwriter/logs/` の**7日より古い行**を、VS Codeの起動時に自動で消します（`novelai.logs.retentionDays`。`0` にすると消しません）。

**ログには原稿の一部が入ります**（相談の内容、AIの応答）。置きっぱなしにするほど、何が入っているか分からなくなるためです。

消すのは古い行だけで、ファイルは残します。日時が読めない行や、日時のある行が1つも無いファイルには触れません。

## 0.6.27 - 2026-08-16

- 著作権者を **`nonahisa, kmizu` の連名**にしました（`LICENSE`）

## 0.6.26 - 2026-08-16

**矛盾検知を実データで測って直しました。**

前の版は、**設定と明らかに食い違う文を仕込んでも1件も見つけられませんでした（0/3）。** AIは3件とも正しく見つけていたのに、**受け取る側の不具合で全部捨てていた**のが原因です（分類名の欄に選択肢がそのまま返ってくるのを想定していませんでした）。

**0/3 → 3/3 になりました。**

あわせて、実際に返ってきた「矛盾ではないもの」を2種類はじくようにしました。

| はじくもの | 実際に返ってきた例 |
|---|---|
| AI自身が否定しているもの | 補足に「**矛盾していません**」と書きながら指摘として並べる |
| 照らし合わせる設定が無いもの | 設定の欄に「**設定情報なし**」「本文からは読み取れない」と書いて指摘する |

実データ3話での誤検出は **0件**（AIが1件挙げ、はじきました）。

> 前の版で検知を実行済みの場合、**もう一度実行してください。** プロンプトを直したので結果が変わります（キャッシュは自動で作り直されます）。

## 0.6.25 - 2026-08-16

**矛盾検知を実装しました**（操作メニュー「執筆AI支援 → 校正・校閲 → **矛盾を検知**」）。

設定資料と本文が食い違っている箇所を探します。指摘は下段の「AI指摘」パネルに出ます。

> **人物**　007.txt 12行目　確信度: 高
> 「わたくしが参りますわ」
> **設定では**　一人称は「僕」
> **本文では**　「わたくし」と言っている

**本文は書き換えません。** 誤字脱字と違い、設定と本文のどちらが正しいかは作者にしか決められないためです。設定資料はAIが抽出したもので、**設定側が古いこともあります**。そのため置き換え案は出さず、「本文を見る」「設定資料を見る」の2つを出します。

| | |
|---|---|
| 見る観点 | 人物・呼称・状態・場所・世界法則・時系列・既出情報（**Ollamaでは人物・状態・時系列の3つに絞ります**。7つ一度に見せると見落としが増えるためです） |
| 渡す設定 | **その話に出てくる人物・場所だけ**です。全部渡すと、出てこない人物の設定まで見て「登場していない」を矛盾にしてきます |
| あらすじ | **その話より前だけ**渡します。後の話を渡すと、まだ書いていない展開との食い違いを矛盾と言い出します |

**意図した変化と未回収の伏線は矛盾として扱いません。** 判断が付かないものは確信度「低」になり、既定では隠れます（チェックを入れると出ます）。

> **先に設定資料を抽出しておいてください。** 照らし合わせる相手が無いと、AIは本文だけを見て「矛盾していそうなこと」を作り出します。設定が1件も無い作品では、抽出への案内を出して実行しません。

## 0.6.24 - 2026-08-16

ライセンス周りを整えました（拡張機能の動きは変わりません）。

- **著作権者を `nonahisa` にしました。** これまで `novel-ai-assistant contributors` という仮の名前のままで、「誰の著作物か」を示せない状態でした
- **同梱しているライブラリのライセンス表示を作り、配布物へ入れました**（`THIRD-PARTY-NOTICES.md`）。`@anthropic-ai/sdk`（MIT）・`iconv-lite`（MIT）・`diff`（BSD-3-Clause）は `dist/extension.js` へ束ねて配布しており、**どちらのライセンスも「著作権表示とライセンス本文を配布物へ添えること」を条件にしています。** これが抜けていました
- READMEにライセンスの節を足しました

> **作者が書いた小説・プロット・設定資料は、このライセンスの対象外です。** 作品の権利は作者のものです。この拡張機能が作ったファイル（設定資料集・IME辞書・あらすじなど）も同じです。

## 0.6.23 - 2026-08-16

**SNS記事を投稿日で管理できるようにしました。**

これまで `2026-08-16.txt` は **「第2026話『08-16』」** として読まれており、一覧にも並び順にも出鱈目な話数が出ていました。日付として読みます。

| ファイル名 | 読み方 |
|---|---|
| `2026-08-16.txt` | 2026年8月16日の投稿 |
| `2026-08-16_海辺の話.txt` | 日付＋題 |
| `2026-08-16_2.txt` | **同じ日の2件目** |
| `2026-08-16_1230.txt` | 同じ日の、時刻で並べたもの |
| `2026-08-16_2_海辺の話.txt` | 並び＋題 |

`2026_08_16` `2026.08.16` `20260816` も同じように読みます。

**1日に何本でも書けます。** 一覧の見出しは `2026-08-16`、2本目以降は `2026-08-16（2）` です。

形式を「SNS記事」にしてある作品では、**「新規話数ファイルを追加」の既定名が今日の日付**になります。その日が埋まっていれば `_2`, `_3` と足します（1本目に `_1` は付けません）。

並び順は 日付 → その日の中の並び です。話数のファイルと混ざっている作品では、日付のものを後ろへ置きます。

## 0.6.22 - 2026-08-16

**作品ごとに目標を決められるようになりました。** 操作メニューの「執筆データ → **この作品の目標を決める**」です。

これまで目標（1日・1月）は**登録している全作品で共有**する1つの値しかありませんでした。

### 1記事あたりの目標文字数

決めると、文字数一覧の「長い・短い」を**平均ではなく目標と比べます**。「1話3,000字で書く」と決めているのに平均と比べても、全部が短い作品では「どれも平均どおり」としか出ませんでした。

目標があるときは、**話数が少なくても印を付けます**（平均は少数だと当てになりませんが、目標は1話目から決まっているためです）。

### 応募先の締切と進捗

**締切日・作品の文字量（下限／上限）・日間目標**を入れると、執筆量パネルのいちばん上に出ます。

> 第40回ファンタジア大賞：締切まであと16日、残り60,000字。1日あたり3,750字です。
> 締切 2026-08-31 ／ 目標 100,000字 ／ 現在 40,000字 ／ 1日 3,750字

| | |
|---|---|
| 必要な日割り | **書いた分を差し引いてから**割ります。目標を日数で割るだけだと、書いても数字が減りません |
| 締切当日 | **1日として数えます。** 「残り0日」では今日書けないことになります |
| 日間目標 | 入れればそちらを使います。「平日は書けないので土日で稼ぐ」など、割り算では出ない事情があるためです。空にすると自動で割り出します |
| 上限超過 | 「削る必要があります」と出ます。応募規定を外れるので、達成ではありません |

**コンテストの一覧は同梱していません。** 募集は日々変わるので、古い一覧を抱えるより募集要項をご覧いただくほうが確かです。入力を始めるときに、公募一覧サイトを開くボタンを出します。

## 0.6.21 - 2026-08-16

**プロットに書いた「形式」が、実際の表示と動きに効くようになりました。**

- **SNS記事では「第3話」ではなく「投稿3」と出ます。** 作品一覧・投稿ごとの文字数一覧・執筆量パネルのタブ名が揃って変わります。同じアカウントの投稿を並べたもので、続きものではないためです
- **形式に合わない機能には、実行前に断りを入れます**（止めはしません）

| 機能 | 断る形式 | 理由 |
|---|---|---|
| 各話あらすじ・感情曲線 | 短編 | 1話で完結するので、あらすじは1件だけ。感情曲線も点が1つでは線になりません |
| 本文からプロットを起こす | 短編集・SNS記事 | あらすじを時系列に並べて筋を組み立てるため、話が続かない作品では実際には無い話が出てきます |

**「それでも実行する」で進めます。** 形式はプロットに書いた申告なので、実際の中身と違うこともあるためです。

形式は `## 形式` から読みます。「長編（連載中）」のように 但し書きを添えても読み取ります。**書いていない作品は、これまでどおりの動きです。**

## 0.6.20 - 2026-08-16

- **ジャンルに「アルファポリス」と「ネオページ」を加えました。** 「形式とジャンルを決める」で選べる投稿先が4つになります

| 投稿先 | 階層 | 件数 |
|---|---|---|
| 小説家になろう | 大ジャンル5つの下 | 20 |
| カクヨム | 並列 | 12 |
| アルファポリス | 並列 | 16 |
| ネオページ | ジャンル12の下にサブジャンル | 59 |

階層のある投稿先は上の段まで書きます（「異世界恋愛 > 悪役令嬢（ネオページ）」）。ネオページの「SF > 宇宙」と「SF」のように、上の段が無いと選び分けられないためです。

## 0.6.19 - 2026-08-16

**プロットに「形式」と「ジャンル」を書けるようにしました。**

操作メニューの「執筆AI支援 → プロット → **形式とジャンルを決める**」で選べます。プロットは自由に書けるので、手で書いても構いません。

| 形式 | 何を指すか |
|---|---|
| 短編 | 1話で完結する作品 |
| 短編集 | 独立した短編を1つの作品にまとめたもの |
| 長編 | 続きもの |
| 大長編 | 長期の連載 |
| SNS記事 | 同じアカウントの投稿を1つのフォルダーにまとめたもの |

いまの分量から当てはまりそうな形式を既定に出しますが、選び直せます。

**ジャンルは投稿先ごとに体系が違う**ため、どちらの体系も持っています（小説家になろうは大ジャンル5つの下に20、カクヨムは12）。「恋愛」も「ホラー」も両サイトにあって指すものが違うので、**どこのジャンルかを添えて書きます**。

```markdown
## 形式
短編集

## ジャンル
- 文芸 > ヒューマンドラマ（小説家になろう）
- 現代ドラマ（カクヨム）
```

両方のサイトへ出す作品は、続けて選べます。プロットの他の部分には触れません。

## 0.6.18 - 2026-08-16

**プロット（`設定/plot.md`）を、自由に書けるMarkdownにしました。**

- **新規作成時に空欄を10個並べなくなりました。** 題と、自由に書いてよい旨の案内、見出し2つ（ログライン・あらすじ）だけです。使える見出しの名前は案内に並べてあります
- **AIが書き足しても、書いた形が変わらなくなりました。** これまでは決まった10個の見出しが上に並び、ご自分で立てた見出しは末尾へ寄せられていました。順番を入れ替えても、次に書き足した瞬間に元へ戻っていました

| 書き足すとき | 動き |
|---|---|
| その見出しがある | **その場所で**中身を入れ替えます。位置は動きません |
| その見出しが無い | **末尾へ足します。** 決まった順に割り込ませません |
| 触らない箇所 | 1文字も変えません。空行も改行コードもそのままです |
| 消した見出し | **復活しません** |

見出しを付けずに思いついたことだけ書き並べても大丈夫です。

## 0.6.17 - 2026-08-16

### AIの独り言

書いている最中に、AIが「AIに相談」パネルへひとこと差し込むようになりました。

- 「本日の執筆文字数が1,000文字を超えました！」「おめでとうございます！　本日の文字数目標を達成しました」
- 「承認待ちの更新が3件たまっています。空き時間に見ておきますか？」「書き終えたところ、誤字脱字じゃないですか？」など、**押すとその場で実行できる申し出**

**うるさくならないようにしてあります。**

| | |
|---|---|
| ポップアップは出ません | 相談パネルに残るだけです。手を止めさせません |
| 手が止まっているときだけ | 書いている最中（直近2分）は話しかけません |
| 立て続けに喋りません | 前に言ってから10分は空けます |
| 同じことは1日1回 | 節目は、越えたうちのいちばん大きいものだけ言います |
| **無料のOllamaのときだけ** | 有料のAIでは動きません。頼まれていない発言に料金はかけません |
| AIが仕事中なら黙ります | 抽出の最中に割り込みません |

`novelai.chatter.enabled` を `false` にすると止まります。

### 新作のプロット相談

「プロットから始める」で新規作品を作ると、**相談パネルからプロット作りの相談が始まります。** 聞ける例が並ぶので、押すだけで始められます。

このとき作品一覧と操作メニューが引っ込み、相談パネルに場所を譲ります。**戻すときは相談パネルの見出しにあるボタン**（作品一覧と操作メニューを出す）を押してください。

## 0.6.16 - 2026-08-16

- **用語にカーソルを合わせたときの枠から、「変化かもしれない」の印も外しました。** 残るのは名前・種別・紹介の3つだけです。書いている最中に警告が出ると、思い出す助けではなく手を止めさせるものになるためです
- 食い違いは、これまでどおり設定資料集パネルの「参考」で見られます（値と話数も並びます）

## 0.6.15 - 2026-08-16

- **用語にカーソルを合わせたときの枠を、紹介だけにしました。** 以前は役割・性格・外見・一人称・能力・関係・登場話・作者メモまで並べており、本文の上に十数行かぶさっていました。詳細は右クリック →「設定情報を表示」で見られます
- 食い違いの印（「変化かもしれない」）は残しますが、**どの項目かだけ**を出します。値は設定資料集パネルで見てください
- 設計書の版を、拡張機能の版に合わせました（以前は文書だけの通し番号で、どちらが新しいか分かりませんでした）。引継ぎ書の「いまの状態」が 0.6.9 のまま古くなっていたのも直し、以後ずれたらテストで止まるようにしました

## 0.6.14 - 2026-08-16

- **設定資料集パネルから、記録を取り下げられるようにしました。** 各記録の一番下に「取り下げる」ボタンがあります。AIが作った中身の無い記録（名前が `null` の組織など）を、ファイルを直接触らずに片づけられます
- **取り下げても、ファイルは消えません。** 回復用の場所（`.novelai-recovery`）へ移すだけなので、あとから戻せます。押す前に確認も出ます

## 0.6.13 - 2026-08-16

- **名前が「null」の組織ができていた**のを直しました。人物の所属からの組織の補完に、名前の確かめが入っていませんでした（抽出の経路には入っていました）。「なし」「不明」「N/A」なども同様に作らなくなります

> **既にできてしまったものは自動で消しません。** 設定資料集の組織タブに `null` が並んでいる場合は、`設定/organizations/` の中の該当ファイル（`org_XXX_null.json`）を削除してください。中身は空なので、消しても失われる情報はありません。

## 0.6.12 - 2026-08-16

設定資料集パネルを見やすくしました。

- **一覧（左側）を畳めるようにしました。** 「◀ 一覧を畳む」で右側が全幅になります。畳んだ状態は覚えます
- **タブが詰まって「登場人／物(84)」のように語の途中で折り返していた**のを直しました。折り返して2行に並べ、語の途中では切りません
- **作品紹介文・キャッチコピー・各話あらすじを、設定資料集の中で読めるようにしました**（「作品情報」タブ）。これまでは `設定/synopsis.md` を自分で開くしかありませんでした
  - まだ無いものは「どこで作れるか」を書いています
  - ここは**読むだけ**です。書き換えは「作品紹介文を生成」「各話あらすじを生成」から行います
- 作品紹介文を採用したあと、**VS Codeで設定した既定のエディター**で開くようにしました（これまでは生のMarkdownが開いていました）

## 0.6.11 - 2026-08-16

**メモリの消費を大きく減らしました。**

相談で使う語句検索の索引が、78.5万字の作品で**54.5MB**（本文の25倍）を使っていました。同じ文字の組み合わせを、それが出てくるチャンクの数だけ重複して抱えていたためです（5.8万種類に対し81.7万件、平均14.1回ずつ）。

持ち方を裏返して **3.1MB（17分の1）** になりました。**検索も8倍速くなり**、結果は実データ8問すべてで以前と一致しています。

> 配布物に設計書などの文書は含まれていません（`docs/` は除外済みで、VSIXの中身は6ファイルです）。

## 0.6.10 - 2026-08-16

開発用の文書の整備です（拡張機能の動きは変わりません）。

- 引継ぎ書（2,776行）を読める大きさに整理しました。**日付順の作業記録94件が全体の78%を占めており**、肝心の「次にやること」が2,614行目にありました。記録を末尾の「8. 作業の記録」へ移し、**533行目**まで繰り上げています
- 冒頭に**「いまの状態」**（版・テスト数・動くもの・触るときの注意）と**目次**を付けました。ここだけで現状が掴めます
- 目次の抜けと飛ばないリンクは、設計書と同じく単体テストで止まるようにしました

## 0.6.9 - 2026-08-16

- 操作メニューの「ヘルプ」で、**バージョンを確認**を一番下にしました（ヘルプは最後の分類なので、メニュー全体でも一番下になります）

## 0.6.8 - 2026-08-16

開発用の文書の整備です（拡張機能の動きは変わりません）。

- 設計書に**目次**を付けました。章と節がリンクで並び、押すとその節へ飛びます
- 目次を作ったところ、**同じ番号の節が2つ**あることが分かりました（`6.18` が「作中の時間」と「相談で使う検索」の両方）。コードと引継ぎ書の16か所から番号で参照されていたので、後から足したほうを `6.19`／`6.20` へずらし、参照側も揃えました
- 番号の重複・目次の抜け・飛ばないリンクは、単体テストで止まるようにしました

## 0.6.7 - 2026-08-16

- 操作メニューの「執筆データ」で、**全作品の執筆統計を一番上**にしました。目標（1日・1月）は作品を問わず共有する値なので、達成率はこちらが正確です

## 0.6.6 - 2026-08-16

**AIへの相談が「分かりません」で突き放さなくなりました。**

「この作品のテーマは何だと読めますか？」「足りていない設定はありますか？」のような、**考えるための問い**に対して「分かりません」の一言しか返らないことがありました。原因は、幻覚を防ぐための指示が**事実の問いにも解釈の問いにも同じ強さで掛かっていた**ことです。

- **出来事・数値・固有名詞**のように本文を見れば決まることは、これまでどおり無ければ「見当たりません」と答えます
- **テーマ・人物の捉え方・足りない設定**のような問いは、**推測だと分かるように書いたうえで答えます**

実データ（18話）で測ったところ、**幻覚は増えていません**（答えの無い問い3件すべてを正しく否定）。禁止を強めても幻覚は減らず、答えだけが痩せていました。

> 設定資料の**抽出**は厳しいままです。抽出結果は保存されて後の判断の土台になるので、相談とは扱いを分けています。

## 0.6.5 - 2026-08-16

- **姓名を繋げた名前と、名だけの名前を「同じ人物かもしれない」として拾う**ようにしました（「三門太志」と「太志」）。日本語の名前は空白で区切らないことが多く、これまでは別人として登録されたまま気づけませんでした
  - 見つかった組は「執筆AI支援 → **重複をまとめる**」に件数付きで並びます。**まとめるかどうか、どちらの名前を残すかは作者が決めます**（自動ではまとめません）

## 0.6.4 - 2026-08-16

設定資料の「変化かもしれない」が積み上がって読めなくなる問題を直しました。

- **違う話で違う値が出たら、作中での変化として自動でまとめる**ようにしました。これまでは全部が「変化かもしれない」として並び、実際の作品では1人の紹介文に**9段**も積み上がって、肝心の内容が埋もれていました
  - 資料には「A（第1話）→ B（第7話）」と並び、**値はどれも消えません**。項目そのものには、いちばん後ろの話の値が入ります
  - **同じ話の中で矛盾しているものだけ**、これまでどおり「変化かもしれない」として残します。両方が同時に正しいことはないので、作者の判断が要ります
  - **読み仮名はまとめません。** 作中で変わるものではないので、違う値が出たらAIの読み違いです
  - まとめた件数は抽出の完了報告に出します
- 人物の**関係**に、憑依・入れ替わり・変装のような「見た目と中身が食い違う」結びつきを書くよう指示を足しました（P-04a version 5.0）。**抽出をやり直すと反映されます**

## 0.6.3 - 2026-08-15

「頼んでも起動ボタンが出ない」を、確実に出るようにしました。

- **作業を頼まれたことを拡張機能側で見分ける**ようにしました。これまではAIに「機能名を返して」と指示していましたが、AIは会話の中で作業を終わらせようとして返さないことが続いていました。「〜を抽出して」「あらすじを作って」のような頼み方は、**AIの返事に関わらず起動ボタンを出します**（押すのは作者です）
  - 「抽出ってどうやるの？」のように**使い方を聞いているときは出しません**
- **作品のファイルを開いていなくても相談できる**ようにしました。これまでは何も開いていないと、起動ボタンも作品の材料も一切出ませんでした
  - 登録が1作品ならそのまま使います。複数あるときは「執筆AI支援 → **相談する作品を選ぶ**」で決められます

## 0.6.2 - 2026-08-15

相談パネルの続きの修正です。

- 「抽出して」と頼んだときに、**AIが会話の中で書き出してしまい、起動ボタンが出なかった**のを直しました。その場で分かる範囲を短く答えつつ、**専用の機能を起動するボタンも出す**ようにしています
- 選択肢が「【次のアクションを提案する】」のような**項目名**になっていたのを直しました。押すと何を頼んだのか分からない会話になっていました
- 相談のログに**プロンプトの版**を残すようにしました。「直したはずの指示が効いていない」ときに、拡張機能開発ホストが古いビルドのままなのかを切り分けられます

> **F5で試すときの注意**：ビルドし直しても、拡張機能開発ホストのウィンドウは**再読み込みするまで古いコードで動きます**（`Ctrl+R`）。

## 0.6.1 - 2026-08-15

実際に使って見つかった不具合の修正です。

**相談パネル**

- 選択肢と同じ文が返事の中にも出て、**同じ文が二度表示されていた**のを直しました
- 返事のMarkdownが記号のまま（`**強調**`）出ていたのを、書式を付けて表示するようにしました
- **作業を頼んでも押せるボタンが出なかった**のを直しました。相談から起動できる操作を3件から15件へ広げています（資料抽出・設定資料集の出力・各話あらすじ・紹介文・プロット逆算など）。**消す操作と作品の登録を変える操作は入れていません**
- 開いている画面の前後だけでなく、**作品の全体像**（話数の一覧・紹介文・プロット）を毎回渡すようにしました

**設定資料**

- **姓が同じ別人が1人にまとめられていた**のを直しました（「〇〇・コンストラクタ」の家族3人が1件になっていました）。あわせて爵位（男爵・伯爵など）を敬称として扱うようにしました
- 「重複をまとめる」に**件数の印**が出るようにしました
- 重複をまとめたあと、開いたままの設定資料パネルが更新されないのを直しました

**そのほか**

- 「ヘルプ → **バージョンを確認**」を追加しました。版と環境（VS Codeの版・選んでいるAI・意味検索の入切）を、そのまま貼り付けられる形でコピーできます
- 資料を開くときに、**VS Codeで設定した既定のエディター**を使うようにしました
- 「開く」を押しても各話あらすじが開かなかったのを直しました
- 珍しい漢字がバイト表記（`<0xE5><0x9B><0xAE>`）のまま保存されるのを直しました

## 0.6.0 - 2026-08-15

相談のやり取りを記録し、後から読み返せるようにしたリリースです。

- AIとの相談を `.aiwriter/logs/chat.md` に残します。操作メニューの「ヘルプ → **相談のログを開く**」で読めます
- 残すのは、質問・返事・**AIに渡した場面**・検索語・選択肢・提案・所要時間・トークン数です
- **失敗した回も残します**（答えが返らなかった理由を後から追えるように）
- 渡した場面は折りたたんであるので、質問と返事が埋もれません
- **原稿の一部を含みます。** `.aiwriter/logs/` にあるのでGitHubへは送られません。残したくない場合は設定 `novelai.chatLog.enabled` を切ってください

**修正**

- 資料を開くときに、VS Codeで設定した既定のエディターを使うようにしました（これまではプレビューに固定していました）

## 0.5.0 - 2026-08-15

**必要なものを、拡張機能の中から入れられるようにしたリリースです。**

これまでは「Ollamaを入れてください」と配布ページを開くだけで、入れ終わったらもう一度セットアップを実行し直す必要がありました。

- 操作メニューに「拡張機能の設定 → セットアップを開始 → **セットアップ（必要なものを入れる）**」を追加しました
- **何が足りていて、それぞれ何のために要るのか**を一覧で見せます。入れない場合に何ができないのかも書いています
- 選んだものをその場で入れます（**Ollama本体**・会話モデル・埋め込みモデル・Git・GitHub CLI）
- **入れる前に、何を・どれだけ取得するかを必ず確認します。** 途中で中止できます
- 入れ終わると次に足りないものへ進みます。実行し直す必要はありません

| 何 | 何のために | 要否 | 大きさ |
|---|---|---|---|
| Ollama本体 | 手元でAIを動かす（無料・原稿を外へ送りません） | 必須 | 約1GB |
| 会話モデル | 設定資料の抽出・あらすじ・誤字脱字・相談 | 必須 | 約9.6GB |
| 埋め込みモデル | 相談の意味検索 | 任意 | 約1.2GB |
| Git | 複数のパソコンで同期・過去の版へ復元 | 任意 | 約60MB |
| GitHub CLI | 非公開リポジトリの作成・取り寄せ | 任意 | 約12MB |

**すべてを入れる必要はありません。** クラウドのAI（Claude・ChatGPT・Gemini）だけを使う場合、Ollamaとモデルは要りません。

- モデルの取得の進み具合が画面に出るようになりました（これまでは何も出ませんでした）

## 0.4.0 - 2026-08-15

相談のときに「質問に近い場面」を作品全体から探して渡すようにしたリリースです。

- **相談が質問を使って場面を探すようになりました。** これまでは名前が出てくる場面を作品全体から等間隔に抜いて渡していたので、どんな質問をしても渡る場面が同じでした
- 探す先は**本文・設定資料・各話あらすじ**の3つ。渡すときに出どころを書き、**食い違うときは本文を優先**します（設定資料とあらすじは本文からAIが作ったものなので、本文と対等には扱いません）
- **追加のものは何も要りません。** 語句一致だけで動きます
- **意味検索（ベクトルDB）は任意**です。入れると「妬ましさを感じる場面」のような言い換えでも見つかりやすくなります。「AI設定 → 相談で使う検索」から準備できます
  - 埋め込みは手元のOllamaで行うので**料金はかかりません**
  - 既定は「切」です。**非力な機械では切ったままで構いません**
  - 索引は `.aiwriter/cache/` に置くのでGitHubへは送りません
  - 途中で詰まったら自動で処理量を減らして続けます

## 0.3.0 - 2026-08-15

各話の起伏を図として見られるようにしたリリースです。

- **感情曲線**を追加。各話あらすじの生成時に「盛り上がり（0〜10）」と「明暗（−5〜+5）」を測り、作品紹介文と各話あらすじの間にグラフとして置きます。話ごとの主だった感情（喜怒哀楽）と、そう判断した理由も一覧に出します
- **あらすじと同じ1回の呼び出しで測るため、AIの料金は増えません**
- グラフは図（SVG）と文字の2通りで置き、図が表示されない場所でも起伏が読めるようにしました
- 縦軸は実際に出ている幅へ引き伸ばし、その幅を図の横に明記します。感情値は1話ずつ独立に測るため値が狭い範囲に固まりやすく、固定の目盛りだと起伏がつぶれて読めないためです

> **既にあらすじを作ってある場合**：感情値を出すには各話あらすじを一度作り直してください（あらすじの作り方を変えたためです）。Ollamaなら無料です。

## 0.2.0 - 2026-08-15

プロットの逆算、AIとの相談、表記ゆれ検知、執筆統計を追加したリリースです。原稿を勝手に書き換えない方針は変わりません。

**書く前・書いたあとを支える機能**

- 本文からプロットを起こす（P-02）。各話あらすじを材料にするためAIの呼び出しは1回で済み、作者が既に書いた項目は確認せずに書き換えない
- 新規作品を「プロットから始める」「本文から書き始める」で選べるようにし、あとからどちらへも移れるようにした
- 執筆量の記録・グラフ（日次・週次・月次・年次）・目標と達成率・話ごとの文字数一覧。全作品を合わせた統計も追加
- 各話あらすじと作品紹介文を1つの文書（`設定/synopsis.md`）にまとめた

**AIとの相談**

- 相談パネルを新設。いま開いている画面について日本語で質問でき、返事には次の一手の選択肢が付く
- 相談からプロット・紹介文・あらすじへ書き込める（作者がボタンを押したときだけ）。本文は対象外
- 相談から誤字脱字・表記ゆれの検知を起動できる（承諾制）
- 該当箇所を開いて光らせる。引用が本文に実在するか照合してから光らせる
- この拡張機能の使い方も答えられる

**校正・校閲**

- 表記ゆれ検知を追加。AIを使わず、組ごとに「どちらへ揃えるか」を作者が選ぶ
- 誤字脱字検知に文章作法（三点リーダー・ダッシュの偶数使用、鉤括弧内文末の句点、感嘆符・疑問符後の空白）を追加
- 話数ファイルを右クリックして、その話だけを検知できるようにした

**資料**

- 設定資料の抽出を種別ごとに実行できるようにした。応答はキャッシュを共有するため、2種類目からAIを呼び直さない
- 抽出プロンプトで「要約は推測ではない」を明示（v4.0）。性格や外見が本文の切り貼りになっていたのを、要約として書けるようにした
- IME辞書にATOK形式を追加

**そのほか**

- 操作メニューを3階層（分類→小分類→操作）に組み替え、AIを呼ぶ操作に印を付けた
- 有料のAIを使う前に、トークンを消費することを必ず知らせるようにした
- GitHubリポジトリの新規作成を、GitHub CLIなしでVS Codeのアカウントから行えるようにした

## 0.0.2 - 2026-08-07

原稿と作者編集データを保護し、登場人物抽出と継続的な品質検証を強化したリリースです。

- Shift_JISを含む元の文字コード、混在改行、末尾改行、未変更箇所のCP932バイト列を保持した手動適用用の提案生成を追加
- 未保存変更、外部変更、競合マーカー、表現不能文字を検出し、安全に書けない場合は原稿更新を拒否
- 公開ファイルAPIに原子的CASがない既存パスは自動置換せず、正規ファイルを書換・削除しないまま `not_saved` と手動適用用の回復パスを返すfail-closed方式へ変更。新規人物と名前変更先の作成は引き続き自動保存
- 人物設定と抽出キャッシュの同時変更・破損・保存競合を検出し、名前変更先の配置後に旧パスを読めない場合も現在人物を曖昧、後続人物を未保存として型付き報告
- 新規登録に加え、登録済み作品も起動時に `.gitignore` を冪等移行し、作者の既存バイト列を置換せず `.novelai-recovery/` を一度だけ追記
- AI出力を人物種別、名前を含む本文中の逐語根拠で決定的に検証し、代名詞・集団・場所・根拠不一致を保存前に除外
- 固定fixtureで同一人物の呼称、同名別人、非人物、作者確定項目、複数章の呼称期間を実経路評価し、recall 1、false positives 0、false merges 0、author protection violations 0を検証
- 保存できない未保存文書では抽出を開始せず、キャッシュ保存失敗、回復パス、Claudeの認証・権限・レート制限を安全な個別案内として報告
- pull requestと`main`で型検査、単体・品質・coverage・VS Code 1.90.0統合テスト、ビルド、依存監査を実行するCIを追加
- VSIX名と導入済みextension identityをpackage metadataから導出し、許可リスト8ファイル、秘密情報・ローカルパス不在、隔離インストールを検証

本バージョンも原稿をAIで自動修正しません。Ollama、Claude、Sakura AI Engineの実接続は資格情報やローカルモデルを必要とする手動確認であり、通常CIの成功だけでは実モデルの品質や可用性を保証しません。

## 0.0.1 - 2026-08-07

最初の配布版です。

- 複数作品の登録、新規作品フォルダ作成、作品ツリー表示
- txt / Markdownの話数認識、投稿サイトヘッダー解析、文字数計測
- 新規話数ファイル追加、ステータスバーでの文字数表示
- Ollama / Claudeの設定、接続確認、モデル選択
- チャンクキャッシュ付き登場人物抽出と1人1JSON保存
- 作者編集項目、競合マーカー、不正・重複した人物JSON、既存フォルダの保護
- AI応答の実行時検証、キャンセル時の部分保存防止
- キャッシュ済み抽出結果からの人物設定再構築と、Claude実行前の課金トークン量表示
- 既存作品の原子的な登録と手動チャンクサイズ設定
- 権限・一時的なファイルシステム障害を「未存在」と誤認しない安全な話数追加・走査
- Sakura AI手動スモークワークフローの起動条件・権限・実行環境・Secret範囲の回帰検証
- `code --install-extension .\novel-ai-assistant-0.0.1.vsix --force` による新規・上書きインストール手順
- 単体テスト、VS Code 1.90.0統合テスト、VSIX配布工程
